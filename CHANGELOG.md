# Changelog

> 所有显著变更均记录在此文件。
> 格式基于 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.0.0/)，
> 版本号遵循 [Semantic Versioning](https://semver.org/lang/zh-CN/)。

---

## [Unreleased]

### Added — 降级原因现在说得清了（`degraded_reason`）

`GET/POST /api/v1/projects/{id}/ai-brief` 新增 `degraded_reason` 字段。

此前响应里只有 `mode: "llm" | "rule"`，而回退到规则引擎有**四种原因**：
没配密钥、日预算用完、预算账本故障、所有接口都挂了。
四种在响应里长得完全一样，于是前端对所有 `mode === "rule"` 一律显示
「当前未配置大模型密钥」。

**在密钥已配好、只是预算耗尽的时候，那句话是错的**，
而且会把人引向完全错误的排查方向 —— 去检查密钥，而问题在预算。
**降级本身不是问题，把降级原因说错才是问题。**

四个取值：`llm_disabled` / `budget_exceeded` / `ledger_unavailable` / `llm_error`，
前端逐个给出对应的处置提示（比如预算耗尽会说明 UTC 零点自动恢复、
要立即恢复就调大预算并重启）。

实现上把 `try_llm_brief()` 的返回类型从 `str | None` 改成
`tuple[str | None, str | None]`，并从 `llm_chat_simple()` 换回 `llm_chat()` ——
`llm_chat_simple` 只返回文本，`refused_reason` 在那一层就被丢掉了。

4 个变异全部被抓到，其中一个专门盯"后端算对了但路由没透传"：
前 4 条断言测的是 `generate_project_brief()` 的返回值，
而前端读的是 HTTP 响应体，中间还隔着一层字段拼装 ——
路由里漏掉一行，那 4 条断言全绿而前端什么都拿不到。

### Fixed — API_SPEC 里 `ai-brief` 的响应体是虚构的

文档写的是 `{"brief": ..., "llm_available": ..., "generated_at": ...}`。
实测响应里**没有 `brief`、也没有 `generated_at`**：正文在 `display_text`，
而且**根本没有生成时间字段**。

**一个字段名写错的文档，产生的是空白页面，不是错误信息** ——
照文档写的前端会拿到 `undefined`，渲染成空白，没有任何报错。

同时更正另一处：`GET /ai-brief` 在代码里就是 POST 的别名，
**每次都重新生成、每次都花 LLM 额度**，不是"读缓存"。
这一点值得单独写清楚，因为「GET 安全、无副作用」是个很强的默认预期，
而这里它不成立。

### Added — LLM 日预算真正会拦了（2026-08-24，所有者决策：实现而非删除）

`LLM_DAILY_BUDGET_USD` 此前是**装饰性配置**：能填、能在两个接口里查到，
但代码里没有任何地方按它拦截调用。这个问题比"配置键完全没被读"更难发现 ——
grep 能搜到 3 处引用，看着像实现了，实际那 3 处全是回显。

**判据必须落在「有没有人在累计花费」上，而不是「有没有人读这个配置」。**
没有累计就无从超限。

所有者在两个选项间选了实现（另一个是删掉配置项并从文档里去掉这层保护）。
理由：这个系统确实需要一个成本上限，而在此之前 `/api/v1/run` 的频率限制
（LLM 开启时 1 次/小时）是**唯一**的成本闸门 —— 它管的是请求频率，不是花了多少钱。
而 API_SPEC §3 里"这两条 LLM 路径不按角色锁，成本由预算门统一拦截"这句话，
当时是把一个鉴权决策的理由建立在一个尚不存在的控制上。

**新增的东西**

- `backend/app/llm/pricing.py` — 单次调用成本估算。约 27 个模型前缀的价格表
  （OpenAI / DeepSeek / Anthropic / qwen / glm / moonshot / gemini / llama / mistral），
  **最长前缀匹配**（`gpt-4` 是 `gpt-4o` 的前缀，短前缀命中差 200 倍）。
- `backend/app/llm/budget.py` — 日花费账本与判定。
- `backend/alembic/versions/0004_llm_spend_daily.py` + `llm_spend_daily` 表。
- `app/llm/client.py` — 在**发出任何网络请求之前**查当日累计，超预算直接拒绝，
  降级回规则引擎；成功后记账。
- 6 个新指标 + 3 条新告警（见下）。
- `GET /api/v1/llm/status` 新增 `budget_enforced` / `spend_today_usd` /
  `calls_today` / `ledger_error`；设置页与洞察页显示"今日已用"。

**成本估算有一种特有的失效方式：算错会被发现，算成 0 不会。**

成本永远 0 → 日累计永远 0 → 预算永不超 → 拦截逻辑写了、测了、跑着，
效果和没写一样，而且**比没写更坏**，因为文档会说它在保护你。
三条静默归零的路径逐条堵掉：

| 路径 | 不处理的后果 | 处理方式 |
|---|---|---|
| 模型不在价格表里 | 换个模型名就等于关掉预算 | 走 `LLM_FALLBACK_PRICE_PER_1M_USD`（新配置项，默认 10.0），且该值配成 0 或负数时仍有非零下限 |
| 接口不返回 `usage` | 缺 usage 就当 0 token | 按字符估算（2 字符/token，中文偏保守），并把记账标记为 `estimated_tokens` |
| 金额在 SQL 里累加漂移 | 预算判断的那个 `>=` 不再可信 | 账本存**纳美元整数** |

最后一条是**测试当场抓到的真实错误**，不是预防性设计。第一版账本用 REAL 存美元、
Python 侧全程 `Decimal`，测试跑出 `0.1 + 0.2 = 0.30000000000000004`。
教训：**Decimal 只保证"读出来的类型对"，累加发生在 SQL 的 UPSERT 里，
Python 的类型管不住那个加号。** 单位选纳而不是微，是因为一次便宜调用约
1.5e-5 美元，微美元下会被舍成个位数，而任何舍到 0 的路径都回到"成本静默归零"。

一律取向**宁可高估**：未知模型走偏高兜底价，缺 usage 按保守字符数估，
纳美元换算向上取整。高估 → 少花钱 + 一条明确日志；低估 → 账单。

**三个诚实的边界**（写进文档，避免被当成 bug 排查）

1. **软上限**。拦截在调用前、成本在调用后才知道，所以最后一次被放行的调用
   必然把当日花费推过预算线，超出量上界 = 单次调用成本（由 `LLM_MAX_TOKENS` 决定）。
   这是"事前拦截 + 事后计费"的必然结果。
2. **价格表是手工维护的近似值**，会过时。用途是"够准地估出能触发熔断的量级"，
   不是账单核对。
3. **账本读不出来时拒绝调用（fail closed）**。理由不是"安全优先"这种口号，
   而是这个系统的具体形状：LLM 是可选增强，规则引擎是永远可用的默认路径
   （ADR-001），拒绝一次 LLM 的代价远小于放行一次超预算的账单。

另外**按进程计的预算不是预算** —— 所以账本落库而不是内存计数：内存计数在重启时
归零，而"花超了"恰好最可能伴随重启；多 worker 时每个进程各记一份、每份都没超，
合起来是 N 倍预算。累加用 UPSERT 单语句完成，先 SELECT 再 UPDATE 在并发下会丢
记账，而路由处理器跑在线程池上，这是真实存在的并发。

### Fixed — 三个 LLM 指标从注册那天起就没被递增过一次

`airdrop_llm_requests_total` / `airdrop_llm_errors_total` /
`airdrop_llm_duration_seconds` 全部注册了、暴露在 `/metrics` 里、
被 OBSERVABILITY.md 记录，还有一条 `HighLLMErrorRate` 告警建立在其上 ——
但代码里**没有任何递增点**，所以那条告警永远不会触发。

**一个存在但永不增长的指标，在面板上是平直的 0 线、在告警里是永不触发，
两者看起来都像"系统很健康"。这比指标名写错更坏** ——
名字写错时查询查不到数据，还有机会被发现。

新增 6 个指标：`airdrop_llm_cost_usd_total{model,basis}`、
`airdrop_llm_tokens_total{model,direction}`、
`airdrop_llm_budget_blocked_total{reason}`、
`airdrop_llm_spend_record_failures_total`、`airdrop_llm_budget_usd`、
`airdrop_llm_spend_today_usd`。`basis` 与 `reason` 都是**闭合词表**，
禁止运行时拼字符串（否则标签基数随模型数与错误文案爆炸）。

新增 3 条告警，级别差异是有理由的：
`LLMBudgetExhausted`（warning，这是设计好的降级不是故障）、
`LLMBudgetLedgerUnavailable`（critical，基础设施问题，别先怀疑预算配置）、
`LLMSpendNotRecorded`（critical，钱花了没记账 = 预算静默失效）。

最后那个尤其值得说：记账失败被**有意做成不抛异常** ——
记账发生在 LLM 已成功返回之后，为了记账失败而丢弃一个已付过钱的结果是纯亏损。
代价是这类失败只在这个指标里可见，所以它必须有告警。

### Changed — 两条 CI 门禁整体转向（它们原本会阻止这次修复）

`test_operations_doc_parity.py` 与 `test_security_doc_parity.py` 里各有一条门禁，
断言"预算拦截必须**仍然不存在**"—— 用来保护文档里「只展示不拦截」那句话的真实性。

**一个断言"这个功能必须仍然缺失"的测试，在功能实现后就成了反向的假绿：
它会让 CI 拒绝真正的修复。** 两条都改为断言拦截真的接在调用路径上，
并且**在发请求之前**（顺序反了就是先花钱再检查）。

`test_observability_doc_parity.py` 的 `known_fictional` 集合里移除
`llm.budget.exceeded` —— 这个事件名现在真的会被发出。

### Fixed — 又抓到两个自身的假绿

1. **门禁的解析器切错了对象。** 新写的"拦截必须在发请求之前"那条断言，
   第一版直接在整个 `client.py` 里找 `await _try_single(` 的位置，
   命中的却是 `_RawCompletion` docstring 里举例用的那一行（第 169 行），
   于是报告"预算检查在调用之后"——**一个完全错误的诊断**。
   解析器出错时的表现（断言失败）和被测对象真的有问题**长得一模一样**。
   修法：先切出 `llm_chat` 函数体，并断言切片长度合理。

2. **禁词禁得太宽，误伤了正确的句子。** 一条门禁禁止 SECURITY.md 出现
   "装饰性配置"，结果打中了 `DUNE_API_KEY` 那一行 —— 它**确实**还是装饰性配置，
   那句话没错。**一个过宽的禁词会把正确的句子也判成错的，然后逼人去改一句
   本来对的话。** 改为只检查提到 `LLM_DAILY_BUDGET_USD` 的那几行。

### Changed — 从「未实现」清单里移出四条，都留了痕

- OPERATIONS §11：移出「LLM 成本预算真实拦截 ❌」与「LLM token / 成本指标 ❌」，
  §11.1 从三条扩到五条。
- OBSERVABILITY §3.3 与 OPERATIONS §12.1：两份"这些指标不存在"的清单里
  各移出两个 LLM 成本/token 指标名。
- SECURITY §10.7：「预算耗尽测试 ❌ 机制不存在」改为已覆盖。

**直接删行是最省事也最糟的做法 —— 读者分不清"修好了"和"被悄悄拿掉了"。**
所以每一条都在 §11.1 / §12.1 / §3.2 留了移出记录。

顺带发现一个反向的坑：那两份"不存在"清单**整块会被门禁当作真值核对**，
所以移出记录里**不能写出真实的指标名**，否则门禁立刻变红。这正是它该做的事。

### Verification

11 个变异逐个植入并跑测试，**全部被抓到（0 漏）**：移除 `check_budget` 调用、
成本估算恒为 0、兜底价降为 0、边界 `>=` 改 `>`、账本失败改 fail open、
不再记账、缺 usage 时 token 估 0、账本改浮点存、以及三个指标的递增点各删一个。

新增 `backend/tests/test_llm_budget_enforcement.py`（35 条），
其中最关键的一条是**超预算时断言零次网络请求** ——
一个正确的 `check_budget()` 加上一个忘了调用它的 `llm_chat()`，
会让所有单元断言全绿而预算毫无作用。反向断言同样必要：
只测"超了被拦"分不清"拦对了"和"全都拦了"。

### Security — 生产环境强制关闭种子数据（2026-08-24，所有者已批准）

`SEED_ON_STARTUP` 与 `SEED_FALLBACK_ENABLED` 此前只是 `.env.example` 里的
一句「**生产环境建议**设为 false」。建议的执行率是不可观测的。

现在生产环境（`APP_ENV=production` / `prod`，含大小写变体）会**强制**把这两个
开关置为 `false`，配置里显式写 `true` 也不生效，并写回字段本身 ——
所以 `/api/v1/settings/config` 回显的就是真实生效值，不留"配置说开着、
实际关着"的落差。

**危害不是"多了 8 条假数据"，而是它让故障看起来像正常**：外部采集全挂时，
库里仍然有项目、Dashboard 仍然有数字。**没人会去查一个看起来有数据的系统。**
静默的错误状态比明确的空状态坏得多。

为什么是强制改而不是拒绝启动：另外几条生产自检（空 `API_KEY`、localhost
`CORS_ORIGINS`）拒绝启动是因为它们**无法自动修正** —— 密钥和域名只有部署者
知道。种子开关不一样，生产环境的正确值只有一个，就是关；忘了改不代表配置
冲突，为此拒绝启动是把一个能自动修好的问题变成一次上线失败。

非生产环境（development / staging / testing）行为不变 —— 本地开箱演示和
空库兜底正是这两个开关存在的理由。

### Added — 日志轮转（两条无界路径都堵上）

**第一条：应用文件日志。** `LOG_FILE` 此前是无上限追加写，实测
`logs/backend.log` 6 天长到 3.97 MB（约 240 MB/年），代码无轮转 handler、
compose 无 `max-size`、宿主无 logrotate —— **三层都没有**。

真实后果不是"日志丢了"：DB 和日志在同一块盘上，写满盘会让**数据库写入
开始失败**。

新增 `LOG_MAX_BYTES`（默认 10 MiB）/ `LOG_BACKUP_COUNT`（默认 5），
磁盘占用上界约 60 MiB。三个容易写错的地方都做了：

- 只换文件不删旧的**不算轮转** —— 那只是把一个大文件换成无数小文件；
- 进程重启后按**已有文件大小**继续计数，不从 0 重新开始（从 0 数的话，
  一个已经很大的文件会被认为"还没到上限"而永远不轮转，而长期运行的服务
  恰好重启过很多次）；
- **轮转失败不抛异常**，降级为继续写当前文件 —— 轮转发生在 `write()` 里，
  抛异常会打断正在处理的业务请求。一个为了保护磁盘而存在的机制，不该成为
  线上故障的来源。磁盘满是慢性问题，请求 500 是即时问题。

没有用 stdlib 的 `RotatingFileHandler`：structlog 走的是 `WriteLoggerFactory`，
硬接 stdlib handler 就要把整条 processor 链（含脱敏）搬到 stdlib formatter 里，
等于新增第二条渲染路径。**日志渲染路径每多一条，脱敏就多一处可能漏掉的地方。**

**第二条：容器 stdout。** 补完应用侧很容易以为问题解决了，但后端**同时**往
stdout 和文件写，而 docker 默认的 `json-file` 驱动**没有任何大小上限** ——
每一行都无限追加到宿主的容器日志文件，只有删容器才释放，流量和应用日志一样大。
`docker-compose.prod.yml` 现在给**全部 11 个服务**配了 `max-size: 10m` /
`max-file: 3`（`x-logging` YAML 锚点复用），合计上界约 330 MiB。

> 只修一条路径就在清单上打勾，会让同一个症状在完全相同的地方复发一次，
> 而那时清单显示"已修复"。

### Added — 生产硬约束门禁 `test_production_hardening.py`（24 条）

新增测试把上面两项从"建议"变成"改不回去"。几条是专门针对容易写出的假绿：

- **只测默认值不够**：把默认值改成 `False` 也能让"生产是 False"通过，
  但那和"强制"是两件事 —— 部署时一个环境变量就能打开。所以必须有显式传
  `True` 的用例。
- **反向断言**：只验证"生产关掉了"分不清"关对了"和"到处都关了"，
  后者会让本地演示一起失效。
- **判据要接进入口**：轮转类写对了不等于 `configure_logging()` 用上了它 ——
  有一条专门断言装上的确实是轮转流，而不是回到 `path.open("a")`。
- **`APP_ENV` 大小写变体**：compose 里 `APP_ENV=${APP_ENV:-production}` 直接
  取操作员的 shell 变量，只覆盖小写等于漏掉真实部署路径。

同步更新 `.env.example`（新键 + 强制关闭说明）与 `OPERATIONS.md` §12.6
（从「完全没有日志轮转」改为已实现，含排障提示），两处都有门禁反向钉住。

### Fixed — 清理「未实现」清单里的三条假记录，其中一条一直是**反向**错的

`OPERATIONS.md` §11 是一张"别当成能用的功能"清单。清单里有三条已经不成立：

| 曾经的记录 | 真实情况 |
|---|---|
| 「日志轮转 ❌ 完全没有」 | 本次已实现 |
| 「`SEED_FALLBACK_ENABLED=false` 生产收紧 ⚠️ 待所有者决定」 | 已决定并实现 |
| 「`RATE_LIMIT_ENABLED` / `_REQUESTS` / `_WINDOW` ❌ 三个配置项无任何代码读取，限流未实现」 | **这条一直是错的** |

第三条最值得单独记：`app/rate_limit.py` 从头到尾都在读这三个键
（`:105` / `:128` / `:129`），中间件真实生效，`/api/v1/run` 还另有分档配额。
文档把一个**正在挡攻击的控制**写成不存在。

**两个方向的错误代价不对称但都真实**：把未实现写成已实现，会让人在风险评估里
数进一个不存在的控制；把已实现写成未实现，会让人重做一遍，或者放弃一个其实
可用的控制。按第三条做风险评估，会得出"需要新加限流"的结论，
而真实缺口在别处（`/metrics` 无鉴权、`/docs` 生产未关）。

三条没有被直接删掉，而是移进新的 §11.1「从这张表里移出去的三条」并写明原因。
直接删是最省事也最坏的做法：读者记得上次看到过这条，找不到之后无法判断
是"修好了"还是"被人悄悄拿掉了"。

新增 4 条门禁（`TestTheUnimplementedListStaysHonest`）：清单里不许再出现这三项、
§11.1 必须留有移出记录、**反向查代码**确认限流那三个键确实还被读着
（否则 §11.1 自己就变成新的假记录）、以及种子强制必须写进 runbook 排障段。

### Fixed — 又抓到四个自身的假绿

1. `monkeypatch.setattr(redact.Path, ...)` 打在一个不存在的属性上，
   配 `raising=False` 会**静默什么都不做** —— 测试测的是正常路径，
   却看起来在测失败路径。改打 `pathlib.Path.rename`，并补一条
   "rename 被打成必失败却仍产出 `.1`" 的反向断言。
2. 文档断言只钉 `max-size` 出现过就假绿：这一节的**问题描述**段落本来就写着
   「compose 里也没有 max-size」。变异掉修复说明那一句时断言照样绿（实测）。
   **钉"某个词出现过"永远不够，只能钉只可能出自那一句的内容。**
3. class-scoped fixture 定义成实例方法触发 `PytestRemovedIn10Warning`，
   而 CI 把 DeprecationWarning 当错误 —— 本地 `--no-cov` 跑不出来，
   加 `-W error::DeprecationWarning` 才现形。
4. 用 `text.index("---", start)` 切 markdown 小节，被表格分隔行 `|---|---|`
   截断，切出来的"节"只剩标题和表头 —— 断言失败，而失败原因和文档内容
   毫无关系。改成切到下一个 `## ` 级标题，并加一条"切出的长度必须够"的自检。
   **解析器出错时的表现是"断言失败"，看起来和"被测对象真的有问题"一模一样。**

### Security — 关掉三个"匿名就能花钱/改数据"的口子（2026-08-24，所有者已批准）

三个写端点此前**匿名 token 实测返回 200**，而且不是"能看"而是"能做"：

| 端点 | 匿名可调的后果 |
|---|---|
| `POST /api/v1/collections/{id}/trigger` | **真的去打外部 API 并写库**（同步跑完整采集，消耗第三方配额） |
| `PATCH /api/v1/collections/{id}` | 开关采集源、改 cron —— 运维动作 |
| `PATCH /api/v1/projects/{id}/funding` | 改融资数据并触发重算 |

现已收紧为管理员专用，它们的 `GET` 保持开放。

**为什么不是简单加两行前缀**：`ADMIN_ONLY_PREFIXES` 只支持整前缀匹配，
这三个都表达不了 ——

- `/collections/sources` 是只读的采集源就绪状态，首页和 `/discoveries` 页在读，
  整前缀锁会让匿名角色的页面直接空掉；
- `funding` 的通配段在路径**中间**（`/projects/{id}/funding`），
  前缀匹配根本写不出来，而同一路径的 `GET` 应当保持开放。

因此加了一层**按方法**的规则（`app/auth.py` 的 `ADMIN_ONLY_METHOD_RULES`）：
GET/HEAD 照旧开放，POST/PATCH/PUT/DELETE 一律要管理员。
`/collections/` 用的是**方法白名单取反**而不是逐条列 trigger 和 PATCH ——
新加写端点时默认就是受保护的。

前端不受影响：`frontend-next/proxy.ts` 在服务端注入 `X-API-Key`，
页面走的是管理员凭据；前端从不签发或使用匿名 token（实测全仓 0 处）。

### Added — 写操作鉴权的双向登记表门禁

真正的问题不是"漏了两个端点"，而是**没人核对过完整的写操作清单**。
`ADMIN_ONLY_PREFIXES` 是一张"记得来登记才生效"的白名单，
而这类白名单的失效方式是沉默的：漏一条，没有任何东西会变红。

新增 `backend/tests/test_admin_only_rules.py`（32 条），把判据反过来写 ——
从 OpenAPI 枚举**全部 21 个写操作**，每一条都必须有归属：
要么受管理员保护，要么在 `ANON_WRITABLE` 里带一句为什么。
当前分布：管理员 7 / 公开 2 / 匿名可写 12。

双向核对（缺一个方向就漏一类错）：

- 登记条目必须在 OpenAPI 里真实存在 —— 否则陈旧条目会让人以为"这个端点审过了"；
- OpenAPI 里每个写操作必须有归属 —— 否则新增写端点会静默漏网。

登记只允许**逐条 (方法, 路径)**，不允许按前缀或按文件豁免。
每条都必须写理由，空理由直接失败：**一张无法评审的白名单等于没有白名单。**

顺带同步了三份文档并给它们各自加了门禁：`docs/API_SPEC.md` §2.1 的
分布表与总数、`docs/OPERATIONS.md` §5.2 的按方法规则表（受限/开放方法逐项比对）、
§12.7 从「尚未修改」改为「已修」。

### Fixed — 两个自身被变异测试抓到的假绿

1. **判据没接进中间件也能全绿**：把 `app/auth.py` 中间件里那行
   `if requires_admin(...)` 改成 `if False:`（等于整个管理员鉴权失效、
   匿名可以打任何端点）之后，前 18 条测试**全部通过** ——
   因为它们只调判定函数，没发过一个真实请求。
   补 `TestMiddlewareActuallyEnforcesTheRules`（端到端发请求）后该变异立刻变红。

   这和本轮早先在 `check_encoding.py` 上踩的坑是同一个：
   **判据写得再对，没接进主流程就等于门禁不存在。**

2. **只验证"锁住了"分不清"锁对了"和"锁多了"**：所有"应当 403"的断言
   在"把整个 `/api/v1/collections` 前缀锁掉"时同样全绿，
   代价是首页对匿名角色空掉。补了反向断言（GET 必须仍然 200）
   和"管理员仍能用"的断言。

变异测试共构造 12 个变异（规则表清空、忽略按方法规则、正则丢锚定、
丢边界导致 `/api/v1/collectionsfoo` 被误锁、把 GET 也锁进去、删 funding 规则、
整前缀锁失效、中间件绕过、中间件全锁、文档数字写错 ×4），全部被至少一条测试抓住。

### Fixed — 备份脚本改完之后「跑成功了」，实际什么都没备份（2026-08-24，实测复现）

修 `scripts/auto_backup.ps1` 的空目录问题时撞上一个更严重的事：
**改完的脚本跳过了前 150 行直接执行末尾，返回 exit 0 说"备份成功"，
而 Docker 根本连不上、一个文件都没生成。**

根因不在代码逻辑，而在**文件编码**：Windows PowerShell 5.1 在文件
**没有 UTF-8 BOM** 时，按系统 ANSI 代码页（简体中文机器 = GBK）解码脚本。
GBK 是双字节编码，任何 ≥ 0x80 的字节都会**无条件吃掉紧随其后的一个字节**
—— 包括 ASCII 引号。而 UTF-8 中文字符是 **3 字节（奇数）**，所以：

| 字符串 | 字节数 | 结束引号 |
|---|---|---|
| `"中"` | 3（奇） | **被吃掉** |
| `"中文"` | 6（偶） | 保留 |
| `"中文字"` | 9（奇） | **被吃掉** |

引号一被吃掉，字符串就不闭合，继续往下吞，把后面几十行代码全吃进一个
字符串字面量 —— 而且**语法完全合法**（解析器报 0 错误），
所以不报错、不警告，只是静默不执行那几十行。

**这条奇偶性是最恶劣的地方**：同一个文件今天没事，明天在某行加一个字
就会翻转。所以判据不可能是"检查有没有某种危险写法"，只能是"必须有 BOM"。

原脚本能正常跑是因为它是 **UTF-16LE**（`FF FE` 开头）；
我用 UTF-8 无 BOM 重写后就踩进了这个坑。

**新增第四型编码门禁**（`scripts/check_encoding.py`）：
`.ps1` / `.psm1` / `.bat` / `.cmd` 只要含任何非 ASCII 字节，就必须带
UTF-8 BOM。这一型**零豁免、零登记清单** —— 修法是加 3 个字节，
不存在前三型那种"原字符已不可逆丢失"的情况，没有理由给谁开例外。

前三型是"文件内容坏了"，四型是"**文件内容完全正确，是读它的人读坏的**"。
它证明了「文件内容合法」和「文件被正确读取」是两件不同的事，
而前三型只查了前者。

另外记两个核对过程中踩的坑（都写进了测试注释）：

- **不能用 Python 的 `bytes.decode("gbk")` 去验证 PowerShell 的行为。**
  同一串字节，.NET cp936 会吃掉引号，Python 的 gbk codec 会保留它 ——
  因为前者对非法尾字节宽容（消费 2 字节），后者严格（抛错，`replace`
  只消费 1 字节）。拿 Python 验证 .NET 会得到**相反的结论**。
  验证用的解码器必须和被验证的解码器是同一个。
- **变异测试发现「判据写对了但没接进主流程」**：把 `main()` 里那个
  BOM 检查分支整段删掉，39 条测试**全绿** —— 因为当时全仓恰好没有违规文件。
  一个只在没有违规时被执行的检查，无法证明它会拦住违规。
  已补端到端测试，用真实违规文件走真正的入口。

### Fixed — 备份脚本的三个真实缺陷（2026-08-24）

1. **每次失败留一个无人回收的空目录**：旧版在检查容器之前就建了带时间戳的
   目录；容器不在线时 exit 1，目录留在盘上，而清理逻辑只删 `*.zip`，
   永远不碰这些目录。**每天失败一次就多一个空目录。**
   已实测复现并修好：目录延后到容器确认在线之后才建，
   并补了一次历史遗留空目录的回收（只删空的，非空一律不碰）。
2. **压缩失败会静默丢掉备份，日志还说文件留着**：旧版无论压缩成功与否都
   `Remove-Item -Recurse -Force` 删源目录，然后在找不到 zip 时记一句
   "备份文件保留在 …" —— 那个目录上一行刚被删掉。
   结果是 **dump 没了、zip 没了、日志告诉你文件还在**。
   这比"备份失败"更糟：失败会被人发现，一句假的成功不会。
   现在只有确认 zip 存在且非空才删源目录，否则保留原始 dump 并明确告警（exit 4）。
3. 删掉一段死代码：旧版建了个 `$compress` 哈希表，`DestinationPath` 写的是
   `.tar.gz`，但从没被用过（实际产物是 `.zip`）。读的人会以为产物是 tar.gz。

### Fixed — `rate_limit.py` 的注释否认了它自己在做的事（2026-08-24）

`backend/app/rate_limit.py` 的模块 docstring 写着「`RATE_LIMIT_*` 三个配置项
没有任何代码读取、限流从未实现」，**而紧接其下的 100 多行就是在读它们**。

这是那次误判的**起点**：当时用 `Select-String -Path "app\**\*.py"` 搜了一遍，
0 命中就下了结论。那个 glob 在 PowerShell 里只匹配一层目录 ——
实测递归有 117 个 `.py`，它只看到 66 个，漏掉的 51 个恰好是全部顶层模块，
包括 `rate_limit.py` 自己。SECURITY.md 的四个小节都是照这个错结论改的。

一个文件的注释否认自己的实现，是最难被发现的一类错：读代码的人先读注释，
读完就不往下看了。已重写 docstring，并新增门禁
（`TestRateLimitDocstringMatchesItsOwnCode`）从两侧钉住：

- **不许出现否认句**（"从未实现"/"没有任何代码读取"/"计划实现"…）；
  逐行豁免标记 `denial-quote-ok` 只给"引用当年那句错话"的那一行，
  与本仓 `terminology-ok` 同一套做法 —— 豁免的粒度就是漏洞的大小。
- **必须出现肯定句**（"实现本体"、"由 `main.py` 通过 `add_middleware` 装上"），
  且这句话由另一条测试用真实中间件对象验证过。
- 必须保留事故成因（那条具体命令、"只匹配一层"、实测数字 117/66、`rglob` 替代做法），
  以及入站/出站限流的区分（仓里两套同名机制方向相反，不写清会有人改错文件）。

只禁止否认句是不够的：变异测试把肯定句换成「本文件计划实现限流」之后
全部测试仍然绿。**补词表能挡住已知说法，挡不住下一种措辞 —— 所以要两侧都钉。**

### Changed — pre-commit 编码钩子补上 `.psm1` / `.cmd`（2026-08-24）

四型判据管 `.ps1/.psm1/.bat/.cmd`，但 pre-commit 的 `files` 正则只写了
`ps1|bat` —— **判据是对的，触发条件漏了**：新增一个 `.psm1` 脚本时钩子
根本不会被触发。已补齐，并加了一条测试从 pre-commit 配置反向核对判据清单
（`test_precommit_hook_covers_every_bom_required_suffix`），
定位钩子用**整行精确匹配**：变异测试把 id 改成 `check-encoding-DISABLED`
（在 pre-commit 里等于停用）之后，原来的子串匹配照样命中、测试全绿。

### Fixed — 安全文档把「设计意图」写成了「已实现」，其中包括不存在的成本上限（2026-08-23，逐条实测）

`docs/SECURITY.md` 的错法和之前几份文档不同：不是写错了现状，而是
**把 ADR 里的设计决定直接抄成"实现"段落**。于是文档描述了一整套
并不存在的安全控制。对普通文档来说这只是过时；对安全文档来说，
**它让人在评估风险时把不存在的控制算进去**。

实测为**不存在**的（共 8 项，逐条见 `docs/SECURITY.md §11`）：

| 文档声称 | 实测 |
|---|---|
| `LLM_DAILY_BUDGET_USD` 超限自动停用 LLM | 只被读来在接口里回显（3 处），**没有任何累计与拦截** |
| 采集器打不到白名单外的域名 | 域名白名单**从未实现**，改一行 URL 就能访问任意域名 |
| Agent 工具权限校验（`allowed_tools` + `PermissionError`） | 全仓 0 处 |
| LLM 输出扫描密钥关键词 | 全仓 0 处 |
| `/projects/{id}/debug` 端点禁止返回 prompt | **端点不存在** |
| 每周跑模型漂移评估 | 脚本存在，**没有定时任务在跑** |

其中 LLM 预算那条最容易骗过检查：搜一下"有 3 处引用"，看着像实现了 ——
**实际是 3 处回显。没有人在累计花费，因此也无从超限。**

**另外发现那张域名白名单表本身就是错的**：Galxe 的主机名写的是
`api.galxe.com`（真实是 `graphigo.prd.galaxy.eco`）、RootData 整条漏登记、
Alchemy 那条对应的集成根本不存在。**假如当初真按这张表实现了白名单，
Galxe 与 RootData 两个采集器会直接被自己的白名单拦死**，而且报错会指向
"域名不被允许"，让人以为是配置问题。一个没实现的白名单，
它的清单本身也从没被现实检验过。

新增 `backend/tests/test_security_doc_parity.py`（29 条）双向钉住：
文档说存在的必须真存在，§11 点名不存在的必须确实不存在。
反向断言是**待实现清单不是禁止清单** —— 真去实现预算熔断时它会变红，
那正是提醒同步文档的时机。15 个变异全部按预期变红。

### Fixed — 我自己在核对安全文档时判错了三处，根因是搜索模式漏了 22 个文件（2026-08-23）

第一轮核对时我把**限流中间件和全量日志脱敏也判成了"未实现"**，
并照此改了三处文档 —— 三处都是错的。

根因：搜索用了 `backend/app/**/*.py`，在这个 shell 里 `**` 只匹配
**恰好一层子目录**，于是 `backend/app/*.py`（顶层 22 个文件）整个没被搜到。
而 `rate_limit.py` / `main.py` / `config.py` 全在那一层。
实测：递归能扫到 117 个文件，那个模式只扫到 66 个，**漏了 51 个**。

真相是 `app/rate_limit.py` 有一个 155 行、写得相当细的限流中间件
（含 `X-Forwarded-For` 伪造防护、`/run` 昂贵端点分档、配额判定顺序的取舍），
`main.py:288` 也确实装载了；日志脱敏也不是"只覆盖错误信息"，
而是装进了 structlog processor 链、控制台与文件共用同一条链。
文档已改回正确描述，并补上文档与代码不一致的**数字**（真实是 100 req/min
不是 60；`/run` 是 LLM 开启时 1 次、关闭时 10 次，不是一律 1 次）。

**教训写进了测试里**：「搜不到」不等于「不存在」，中间差一步 ——
**先证明搜索本身有效**。新套件里有一条自检用已知存在的符号验证搜索器
工作正常，再去相信它给出的"0 处"结论。

另外，把已实现写成未实现、把未实现写成已实现，**两个方向的代价不对称
但都真实**：前者让人重复实现一遍，后者让人把不存在的保护算进风险评估。
不能因为"往严的方向写更安全"就少查一遍。

### Fixed — `FARM/WATCH/IGNORE` 在前端抄了 5 遍，其中一处漏项会让图表百分比全错（2026-08-23，实测）

标签真值是 `app/agents/scorer.py::LABEL_THRESHOLDS`。前端把它抄了 **5 处**，
彼此之间没有任何同步机制：

| 位置 | 缺一项的后果 |
|---|---|
| `lib/types.ts` `Label` 联合 | TS 会挡住，**这一处是安全的** |
| `lib/format.ts` `LABEL_ZH` | 显示英文原文 |
| `app/portfolio` `LABEL_COLOR` | 退回中性灰 |
| `app/collections` `LABEL_FILTERS` | **那个标签的项目筛不出来，页面上等于不存在** |
| `components/Charts.tsx` `LABEL_COLORS` + `keys` 数组 | 环形图少一瓣，**其余百分比全错** |

`Charts.tsx` 那处最脆：`LABEL_COLORS` 有 `Record<Label, string>` 兜着，
但同一个文件里还硬写了 `const keys: Label[] = ['FARM','WATCH','IGNORE']` ——
**数组漏一项 TS 完全不报错**。环形图会安静地少画一瓣，而占比分母是
`data.reduce(...)`，于是**剩下两瓣的百分比还会各自变大**。
结果不是"少显示一个"，而是**其余数字全错，且看不出错**。

目前 5 处都是齐的（实测比对通过），但没有任何机制防止下次改动漏掉一处。
新增 `TestLabelVocabulary`（6 条）逐处比对，真值从
`LABEL_THRESHOLDS` 解析而不是抄字面量。6 个变异全部按预期变红
（每处各删一个标签 + 单独动 `keys` 数组）。

顺带记一条方法上的坑：写变异脚本时按 CRLF 拼接删除锚点，
结果 3 个 LF 文件的替换**静默不生效**、跑出来是"通过" ——
差点把没生效的变异当成"门禁没覆盖"。改成按文件实际行尾拼接后全部变红。
**一个静默不生效的变异测试，比没有变异测试更危险**：它会让人误判门禁强度。

### Fixed — 风险档位有两套取值范围，前端那张中文表同时多一项又少一项（2026-08-23，实测穷举）

系统里有**两套**风险档位，取值范围不同：

- 评分管道侧（三档）：`team.risk_level`（穷举 `score_to_risk_level()` 全部
  0.00–1.00 输入，输出只有 `high/medium/low`）、
  `risk.sybil_difficulty` / `farming_cost` / `unlock_pressure`
  （`RiskResult` 三个字段的 pattern 都是 `^(low|medium|high)$`）
- Opportunity 侧（**四档**）：`opportunity.models.RiskLevel` 多一个 `critical`

前端 `riskLevelZh` 的 4 个调用点全部来自第一套，但那张表里**多写了一个
`unknown: '未知'`** —— 不可达的死条目：缺值走的是函数开头的
`if (!level) return '—'`，永远到不了这张表。跟之前 `timingZh` 里那个
`growth` 是同一类问题：**死条目会让人以为系统还有额外的判断档位。**

反方向的风险是 `critical`：真值存在，只是前端目前一处都没渲染
`OpportunityAssessment.risks` 的 5 个维度（实测 253 条评估记录里
5 个维度全是 `null`，后端也还没在填）。一旦有人开始展示它，
`critical` 会**直接渲染成英文原文**。

删掉死条目，并新增 `TestRiskLevelVocabulary`（4 条）同时钉两头：
多的不许有、少的要有人管 —— 最后那条不写死"前端不能渲染 risks"
（那会挡住正常开发），而是把两件事绑在一起：**要渲染就得先把词补齐**。
4 个变异全部按预期变红（重新引入死条目 / 删掉 `medium` /
无渲染方就加 `critical` / 开始渲染 `risks` 但没补词）。

### Fixed — 两个测试有 12.5% 概率随机变红，根因是 Windows 时钟只有 15.6ms 精度（2026-08-23，实测复现）

跑完整套件时 `test_cache.py::test_ttl_expiry` 挂了一次，单独重跑却通过。
不是"偶发环境问题"，是一个可量化的真实缺陷 —— 写了个 4000 次采样的复现脚本：

- 测试写法是「TTL 设 50ms，然后 `sleep(60ms)`，断言已过期」。
- 但 Windows 上 `time.monotonic()` 由 `GetTickCount64()` 实现，
  **分辨率是 15.625ms**。墙钟确实睡够了 60ms，缓存读到的 monotonic 差值
  却可能只有 46ms（实测最差值），于是判定"还没过期"。
- 实测**499/4000 = 12.5% 的概率**踩中。

**一个依赖时钟分辨率的断言不是断言** —— 它在不同操作系统、不同负载下
结论不同。修法不是把 sleep 加大（那只是把概率压小、并让套件更慢），
而是**注入假时钟**：直接推进被测模块看到的 `time.monotonic`，
既测到了真正要测的过期判定逻辑，又完全不受时钟精度影响。
顺带修掉了 `test_heat_signals.py::test_cache_expiry` 的同一个写法。

改完连跑 30 次，全部通过，且这两条现在不再消耗任何真实等待时间。

### Fixed — 最后一份编码损坏文档修完；它把 10 个早就在跑的采集器持续标为「计划中」（2026-08-23，全部实测）

`docs/DATA_SOURCE_STRATEGY.md` 是全仓最后一个挂在编码损坏登记表上的文件
（498 处中文字被截断成非法 UTF-8）。修完它意味着**三型编码损坏全部清零，
三份豁免清单同时为空**，一型也升级成零豁免硬门禁。

**但真正值钱的不是修字节，是修内容。** 这份文档的错误方式和前两份不一样，
也更隐蔽：它不是"写了错的现状"，而是**把已经做完的事持续标为「计划中」**。

- **10 个采集器全部已实现，文档却逐个标着「（计划实现位置）」，
  而且 10 个文件路径 10 个都不存在** —— 真实文件没有 `_collector` 后缀
  （文档说 `collectors/defillama_collector.py`，真实是 `collectors/defillama.py`），
  类名也不一样（文档的 `ChainCollector` / `QuestCollector` 真实是
  `EtherscanCollector` / `GalxeCollector` + `Layer3Collector`）。
  **危害不是让人少做事，而是让人重做一遍已经在跑的东西**；
  而且读者一旦发现清单不准，会连里面真正的待办一起不信。
- **`discovery_score` 的"统一公式"是假的**。文档给了
  `0.4×tvl + 0.3×github + 0.2×twitter + 0.1×chain`，
  代码里**没有任何地方实现它** —— 真实是 10 个采集器各算各的，
  权重和入参都不同。更要紧的是：`cryptorank` / `etherscan` / GitHub 关键词搜索
  三条路径的分数上限被**刻意压在 0.28**，低于分析阈值 0.3 ——
  意思是这些源单独永远不触发 LLM 分析，只贡献信号，这是省成本的设计。
  照那条假公式去调权重，会把这个设计误判成"这几个源坏了"。
- **`POST /re-score/{id}` 是幽灵接口**（46 个真实路由里没有它）。
  而 `/api/v1/re-score` 前缀在鉴权表里，所以调用会先撞中间件返回
  **403 而不是 404** —— 你会以为"接口在、只是权限不对"，然后去查 token。
- **说 GitHub / CoinGecko「跟随 DefiLlama 事件触发」** —— 不是，各有独立 cron。
  **说 `raw_projects` 新增就「立即触发分析」** —— 两条链完全解耦
  （`COLLECTION_AUTO_RUN_ENABLED=false`）。信了这条，采集跑完不见新评分时
  会去查分析 Agent 的 bug，而真相是分析要等自己的 cron。
- **4 个 P2 源（Discord / Medium / Mirror / Reddit）写成「自动采集（RSS）」**，
  实际零代码。
- **速率限制器路径写错**（写 `utils/rate_limiter.py`，真实在 `collectors/`）。
- **保留策略缺了最重要的一档**：旧版只按表分档，没区分
  `processed=1` / `processed=0`。而实测 **83% 的采集记录（509/615）永远不会
  被标记 `processed=1`**（它们分数低于 0.3，从不进分析），
  按旧规则不满足任何归档条件，会无限累积。真实代码补了
  `UNPROCESSED_RAW_RETENTION_DAYS=90` 这一档。
- 一批「需后续同步新增」的清单其实**早已完成**：7 个 API Key 字段、
  4 张采集表、`projects` 的 4 个扩展字段、ADR-012。

重写后的文档全部换成**实测数字**：10 个采集器的真实文件/类名、
10 条真实 cron、11 条真实限流配置、5 个真实告警阈值、
15 项来源优先级、各源实测产出（`defillama` 均值 0.796 全部进分析 /
`coingecko` 固定 0.1 一条没进），并新增 §11「还没做的」12 行清单
与 §12「上一版失真记录」9 条。

新增 `backend/tests/test_data_source_strategy_parity.py`（**23 条**）双向钉住：
正向比对采集器文件与类名、来源优先级、cron、限流、告警阈值、采集表 DDL、
正文里被当成可用的接口；反向断言 §12 点名不存在的东西**确实都不存在**
（幽灵接口、8 个旧路径、P2 源无 collector、`evaluation/collection/` 目录、
统一公式、`COLLECTION_AUTO_RUN_ENABLED` 默认值）。
**12 个变异全部按预期变红**，文档字节等同恢复。

同时把一型豁免清单也升级成正面门禁：新增
`test_type1_registry_is_empty` 与 `test_no_unregistered_type1_corruption_in_repo`
（全仓 500 个文本文件扫描）。`docs/ENCODING_REPAIR.md` 同步改写为
"全部修完"，并记下一条结论：**自动修复工具最终一处也没用于交付** ——
它的价值是量出「修复不可行」这个结论（64 个候选、同文档收敛只能定 9.1%），
三份文档全部按实测重写。

### Fixed — `.env.example` 有 47 个键的值和代码不符，还自相矛盾地把 SQLite 悄悄换成了 Postgres（2026-08-23，逐键实测）

配置模板是新人和部署脚本唯一的起点，但**它不被任何代码读取** ——
所以它写错不会报任何错，只会静默误导人。逐键跟 `app/config.py` 比对后发现：

- **一处自相矛盾，也是最危险的一处**：文件写着 `DB_BACKEND=sqlite`，
  同时把 `DATABASE_URL=postgresql://airdrop:airdrop@localhost:5432/airdrop`
  打开了。而 `_resolve_db_backend()` 会**反向**把 `db_backend` 改成 `postgres`。
  照这份模板复制出来的 `.env`，**实际连的是 Postgres，跟它自己声明的那行完全相反**。
  这类错误最难发现：两行单独看都合理，只有读过那个 validator 的人才知道
  后者压过前者。已改成把 `DATABASE_URL` 注释掉，并在注释里写清两条激活路径
  和"它会反向改写 DB_BACKEND"这件事。
- **47 个键的值与代码默认值不一致**：6 个 cron 时间（`cryptorank` `rootdata`
  `twitter_kol` `etherscan` `galxe` `layer3`）、9 个源的重试次数（2 vs 3）、
  `SCHEDULER_MISFIRE_GRACE_SECONDS`（300 vs 3600）、
  `SQLITE_BUSY_TIMEOUT_SECONDS`（模板还把单位写成了毫秒，实际是秒）、
  `AUTH_TOKEN_TTL_HOURS`（24 vs 72）、`MAX_CONCURRENT_PROJECTS`、
  `LLM_SEMAPHORE_SIZE`、4 个 `HEAT_SIGNAL_*`、6 个
  `OPPORTUNITY_ECONOMIC_*`（模板全 true，代码全 false）、`APP_NAME`、
  `SEED_ON_STARTUP` 等。**照模板填出来的系统行为和代码默认值不同，而两边都"看起来对"。**
- **2 个键全仓没有任何代码读取**：`LLM_API_KEYS` / `LLM_BASE_URLS`。
  多接口故障转移真正读的是**编号变量**（`LLM_BASEURL_1` / `LLM_API_KEY_1` /
  `LLM_MODELS_1_1`），且走 `os.environ` 不经 Settings。照那两个逗号分隔变量填，
  **一个 LLM 接口都不会注册**。已删除，并把真正生效的编号变量写法写进注释。
- **`WEIGHT_VERSION=score-v1.4` 混淆了两个版本轴**：`score-v1.4` 是评分
  **模型代号**（前端展示、Opportunity 的 `LEGACY_MODEL_VERSION`），
  而这个键是**权重版本**（代码默认 `v1.2`，随每条评分写入
  `projects.weight_version`）。照模板填会把模型名写进权重列。
- **2 个路径不存在**：`SEED_DATA_PATH=data/seed_projects.json`（真实默认是
  `scripts/seed.py`）、`FETCHER_CACHE_DIR=data/fetcher_cache`（真实是 `cache/`）。
- 另外**顺带标注了 5 组"填了也不生效"的键**：3 个 `RATE_LIMIT_*`（限流至今未实现）、
  `DUNE_API_KEY`（无 collector 读它）、`TWITTER_API_KEY` / `TWITTER_API_SECRET`
  （采集器只认 `TWITTER_BEARER_TOKEN`）、`LLM_DAILY_BUDGET_USD`（不拦截任何调用）。
  这些保留但明确写出"不生效"，因为**一个填了以为生效的开关比一个缺失的开关更坏**。

**新增门禁 `backend/tests/test_env_example_parity.py`（13 个测试）**，
两条主断言：每个键必须真的有人读（是 `Settings` 字段，或标 `env-external`
说明谁读它）；每个值必须等于代码声明的默认值，或标 `env-differs` 写出理由。
另有 4 条 parser self-check，防"解析器读不到东西却一路全绿"。

设计上刻意做对的两件事：

1. **豁免逐键显式，绝不整文件豁免。** 标记写在键的上一行注释里，
   `grep -n env-differs .env.example` 就能审计全部例外；空行会截断注释块，
   所以一个标记不会顺着整节蔓延。整文件豁免正是 `API_SPEC.md` /
   `OPERATIONS.md` 当初烂掉的机制，不能在防它的机制里重犯。
2. **读 `Settings.model_fields[...].default`（声明默认值），不读实例属性。**
   第一版写成 `getattr(Settings(_env_file=None), name)`，在 pytest 里立刻挂了 ——
   `conftest.py` 设了 `APP_ENV=test` / `DB_PATH` / `HOST`，而环境变量优先级
   高于 dotenv。**一个随环境改变结论的断言不是断言**：那样它在 pytest 里、
   裸机上、CI 里判定各不相同。同理，"按模板加载"用的是 init kwargs
   而不是 `_env_file=`，否则测的不是"照这份模板会得到什么"。

数值按数值比（`0.10` 等于 `0.1`），避免为格式差异制造假红。

**自我纠正（CI 抓到的）**：第一版路径断言把 `FETCHER_CACHE_DIR=cache`
也当成"仓库里必须存在的路径"，本地全绿、**CI 直接挂**——
`cache/` 是 `_FileCache.__init__` 里 `mkdir(parents=True, exist_ok=True)`
按需建出来的，本机因为跑过应用才有，全新 checkout 上并不存在。
**这跟上面第 2 条是同一个错的第二次犯：一个只在"已经运行过的机器"上为真的
断言不是断言。** 已拆成两条：`SEED_DATA_PATH` 查"随仓库提交的文件存在"，
运行时目录改查"是相对路径、不用 `..` 逃出仓库"（挡的是缓存写到仓库外，
跟 `DB_PATH` 解析到 `D:\app\data` 同一个坑）。

当前 9 个合法例外各自写了理由：`LOG_FORMAT`（代码默认 json 便于采集，
模板给 console 便于本地看）、4 个 `POSTGRES_*`（代码默认值是 CI 测试实例
`127.0.0.1:5433/airdrop_test`，模板不该把人指向测试库）、
3 个 `OTEL_*` + 2 个 `TELEGRAM_*`（分别由 OTel SDK 和 docker compose 直接读）。

`docs/OPERATIONS.md` 新增 §9.4 记录这份清单与门禁，§9.2 加了
`DATABASE_URL` 反向改写和 6 个 `OPPORTUNITY_ECONOMIC_*` 级联校验两条警示，
§11 补了 5 组"不生效的配置"，§12.12 记下这条失真。

**11 个变异全部按预期变红**：改错 cron、取消注释 `DATABASE_URL`、
加一个无人读取的键、破坏权重和、改回不存在的 seed 路径、
让 `env-differs` 标记过期、把 `env-external` 标在真字段上、
把 `WEIGHT_VERSION` 换成模型代号、把缓存目录改成绝对路径、
用 `..` 逃出仓库、seed 路径指向不存在的文件。
模板字节等同恢复（SHA 核对）。

### Fixed — 三型编码损坏清零：删掉猜不出来的 emoji，而不是补一个（2026-08-23）

`docs/SYSTEM_DIRECTION_CHANGE.md` 有 2 个小节标题的 emoji 被吃成了
U+FFFD 替换符（`## <?> 成功指标（KPI）`、`## <?>️ 实施路线图（v2.0）`）。
git 历史里最早的提交 `a9f2c8b` 就已经是坏的，没有任何底本能证明原来是哪个 emoji。

**处理方式：直接去掉 emoji**，标题变成 `## 成功指标（KPI）` /
`## 实施路线图（v2.0）`，语义零损失。

为什么不随便补一个「看起来差不多」的（比如 📊 / 🗺️）：补上去以后，
这份文档看起来就**从未损坏过** —— 下一个人再也分不清哪个标题的 emoji 是
原作者选的、哪个是后来猜的。**一个看不出来的猜测，比一个看得出来的缺口更糟。**
这跟一型/二型共用同一条原则：只填能证明的，不填猜的。

修完后 `KNOWN_BROKEN_REPLACEMENT` 豁免清单清空，新增
`test_replacement_registry_is_empty` **正面钉住「必须保持为空」**——
和二型同一套设计，理由也一样：豁免清单里的文件没人会去逐行读，
错的**内容**会跟错的字节一起躺着（`API_SPEC.md` 那 13 条幽灵端点就是这么藏住的）。

现在只剩一型的 `docs/DATA_SOURCE_STRATEGY.md`（498 处）挂在豁免清单上，
从原来的 5 个文件降到 1 个。

**顺带纠正一个我自己的误判**：这一轮起初以为
`backend/app/rate_limit.py`、`prometheus.yml`、`docker-compose.prod.yml`
三个文件有 GBK 误解码乱码 —— 依据是终端里看到的乱码输出。
读字节实测：三个文件都是**干净的 UTF-8**，中文完全正确，乱的是 PowerShell
控制台的显示。**终端显示不是文件内容**，判断编码损坏必须读字节。
顺手补扫了「UTF-8 被当成 GBK/latin-1 读」这种设想的第四型，全仓 502 个
文本文件 **0 命中**，结论记进 `docs/ENCODING_REPAIR.md` §6。

### Fixed — 运维手册说「10 个采集源全部就绪」，实测只有 5 个（2026-08-23）

`docs/OPERATIONS.md` §4.3 写的是 10 个源「全部已注册且 `config_ready=true`」。
实测 `GET /api/v1/collections/sources`：只有 5 个为 true
（`defillama` `coingecko` `github` `cryptorank` `etherscan`），
另外 5 个（`rootdata` `twitter_kol` `twitter_keyword` `galxe` `layer3`）
开关关着、Key 也没配；`data_sources` 表里同样只有那 5 个源有记录。

**危害的形态**：未就绪的源在那个端点里**照样列出来**，只是 `config_ready=false`。
排查「为什么没发现新项目」时如果不看这一列，就会去翻一个**从未运行过**的源的
日志 —— 翻不到任何错误，于是误判成"采集是好的，问题在分析侧"，方向整个跑偏。

改法上有一个刻意的选择：门禁比对的是**门控规则**（每个源的开关配置名 +
是否需要 API Key），**不是**本机当前的就绪值。就绪值取决于 `.env`，
把它钉进测试会让这份文档在 CI 和任何别人的机器上必然变红 ——
而那种红传达的是错误的信息（"文档写错了"，其实只是"这台机器没配 Key"）。
本机的实测快照仍然写在文档里，但标注了日期，不参与断言。

顺带记录两件实测出来的事：两个 twitter 源**共用同一个开关** `TWITTER_ENABLED`
（关掉会同时停掉 KOL 轮询和关键词搜索，没法只留一个）；代码默认值下
只有 `defillama` / `coingecko` / `github` 的开关是开的，其余 7 个默认关 ——
一台没配 `.env` 的机器实际能跑的源比那张表短得多。

门禁从 35 个测试加到 39 个，`docs/OPERATIONS.md` §12.11 记下这条失真，
3 个针对性变异全部变红。

### Fixed — 运维手册（OPERATIONS.md）404 处乱码之外，还在**内容上**说谎：18 个假指标、4 个假接口、2 个假脚本、一层不存在的"成本保护"（2026-08-23，全部实测）

这份文件此前挂在编码检查的"已知损坏"豁免清单上。**豁免之后就没人读它了**，
于是它在内容上烂得比编码更彻底。逐条实测后发现：

- **19 个 Prometheus 指标名里 18 个在代码里不存在。** 只有
  `airdrop_http_requests_total` 是真的。为什么这比写错阈值更危险：
  Prometheus 查一个不存在的指标**不报错，返回空结果** —— 面板显示成
  "一切平静"，告警规则永远不触发。看起来被监控着，其实这条监控从来没生效过。
- **4 个 API 路径不存在**，其中 `POST /api/v1/re-score/{id}` 最会骗人：
  它落在管理员前缀里，匿名调用返回 **403 而不是 404**。403 让人确信
  "接口是有的，只是我权限不够"，比 404 更能把人骗住 —— 这正是这个错误
  能活这么久的原因。用管理员凭据打才露出真相：404。
- **两个"已提供的巡检脚本"根本没有这个文件。** 文档整段贴出了
  `scripts/diagnose.sh` 和 `scripts/heal.sh` 的完整 bash 源码，还给了
  安装它们的 crontab。贴出源码让人完全不会怀疑它不存在。值班在压力下
  敲下 `./scripts/heal.sh`，得到"No such file"，白丢一段最宝贵的时间。
- **声称"LLM 超预算会自动停用"——这层保护不存在。** `LLM_DAILY_BUDGET_USD`
  只被两个只读接口读出来展示，代码里没有任何按它拦截调用的逻辑，也没有
  任何 token / 成本指标。真实存在的成本闸门只有 `/api/v1/run` 的频率限制
  （LLM 开启 1 次/小时，关闭 10 次/小时）。**一层写在 runbook 里、值班照着
  信的保护，实际不存在，比压根没写更危险。**
- **端口全篇写 8000，真实是 8002。** 照文档执行 `deploy.sh` 会在健康检查
  环节卡满 30 次重试后失败（脚本本身也硬编码 8000，已在文档里标注）。
- **备份可能备错库。** 文档说库在 `data/airdrop.db`，实测运行时真正连的是
  `D:\app\data\app.db`（288 个项目 / 9.3 MB），而 `data/airdrop.db` 是个
  94 个项目的过期副本。`scripts/backup.sh` 的最后一级回退正是复制那个过期副本
  —— 容器不在时它会**安静地备错文件并报告"备份完成"**。一个报成功却备错的
  备份，比明确失败的备份危险。
- **完全没有日志轮转。** `logs/backend.log` 6 天长到 3.97 MB，代码里没有
  轮转 handler，compose 没有 `max-size`，也没有 logrotate。按当前速率
  一年约 240 MB，无任何上限。
- **归档任务从上线到现在一次都没真正执行过。** 逻辑和调度都在，但本地数据
  最早只到 08-09，还没有记录超过 30 天保留期，所以每次触发都"无事可做"
  （`archive_runs` 0 行）。这条路径在生产上等于未验证。
- 还有：重复的章节号（两个 `## 5`，让所有"见 §5.x"交叉引用二义）、
  `/docs` 在生产环境也无条件开放、Prometheus 的 `environment` 标签写死成
  production。

**处理方式**：整份重写，只写实测为真的命令、端口、路径、指标名、cron；
设计意图和未来规划全部隔离进 §11「未实现」；把上面这些失真单独记进
§12，**并让门禁反向断言它们确实不存在** —— 一份用来纠错的清单自己出错，
比原来的错误更难被发现。文件已移出编码豁免清单（一型损坏从 902 处降到 498 处）。

新增门禁 `backend/tests/test_operations_doc_parity.py`（35 个测试），
解析这份 Markdown 并与代码真相双向比对：指标名对 Prometheus registry、
路径对 OpenAPI schema、脚本对文件系统、cron 对 `settings`、管理员前缀对
`ADMIN_ONLY_PREFIXES`、告警名对 `alert_rules.yml`、阈值对 `check_alerts()`。

**变异测试过程中抓到了门禁自己的一个漏洞，值得单独记**：第一版实现里，
"§12 已登记为幽灵"是**整份文档的全局豁免**。于是把正文的
`GET /api/v1/quarantine` 改成不存在的 `GET /api/v1/audit`，测试依然全绿 ——
一条假命令只要名字进过纠错清单，就能伪装成"已知问题"畅通无阻。
这跟这份文档最初出问题的机制**一模一样**：登记豁免掩盖内容问题。
改成逐行判定后（只有那一行自己写出「不存在 / 没有 / ❌ / 从未」才豁免那一行），
10 个变异全部变红。

### Fixed — `configure_logging()` 每次调用都漏一个文件句柄，且让**无关**测试莫名失败（2026-08-23，CI 实测）

这个缺陷是上面那条 `LOG_LEVEL` 修复的新回归测试**顺带钓出来的**，
本地全绿、CI 变红 —— 因为 CI 把 `ResourceWarning` 当错误，本地不当。

`configure_logging()` 的文档说它幂等可重复调用，但每次调用都会 `open()`
一个新的日志文件，旧句柄既不关闭也不再被引用，只能等垃圾回收 ——
届时 CPython 抛 `ResourceWarning`。

**危害的形态比泄漏本身更值得记**：泄漏发生在 A 处，报错却记在恰好触发
垃圾回收的 B 处身上。CI 里报错的两个测试之一是**归档定时任务的测试**，
和日志配置毫无关系，日志里指向的却是日志级别测试留下的临时文件。
这种错位归因极难排查 —— 看错误信息完全找不到真正的责任方。

修复：重新配置前显式关掉上一个句柄（关闭失败不阻断日志安装 ——
日志系统装不上比一个悬空句柄严重得多）。已用变异测试确认这条被钉住：
去掉关闭逻辑，两个相关测试立刻变红。

生产环境里 `configure_logging()` 只被调一次，所以线上影响有限；
但任何"改完配置重装日志"的场景都会稳定泄漏。

### Fixed — `LOG_LEVEL` 是个"设了但不生效"的开关；可观测性文档里 35 个指标名不存在（2026-08-23，已实测）

**一、`LOG_LEVEL` 对应用自身的日志完全不起作用。**

这个环境变量此前只传给了 uvicorn（Web 服务器自己的访问日志），
应用代码里两百多处日志调用**根本不看它**。实测把 `LOG_LEVEL` 设成
`WARNING`，最啰嗦的 debug 日志（抓取缓存命中、限流等待、逐条抓取失败）
照样一条不少地全量输出。

为什么这比"没有这个开关"更糟：运维把它调成 WARNING 之后，会认为噪音
已经压掉了，于是不再关注日志量 —— 而磁盘和日志后端照旧在被灌满，
真正需要看的 error 淹没在 debug 里。

已修复并实测七种取值：`DEBUG`/`INFO`/`WARNING`/`ERROR` 各自只放出该级别
及以上；`warn` 作为常见别名等同 `WARNING`；**拼错或留空时退回 `INFO`，
而不是降级成 `DEBUG`** —— 后者会让"配置写错"反而把全部调试日志放出来，
方向正好相反。这条行为用变异测试验证过：把非法值的兜底从 INFO 改成 DEBUG，
测试立刻变红。

**二、`docs/OBSERVABILITY.md` 整份重写。**

这份文档同时有两层问题。表层是编码损坏（214 处中文字被截断成非法字节）；
底层更严重：它列出的 **39 个监控指标里有 35 个在代码中根本不存在**，
列出的 **15 个日志事件名一个都不存在**。

为什么这类错误特别危险：Prometheus 查一个不存在的指标**不会报错**，
只返回空结果。面板上呈现为一条空曲线、一个「No data」，看起来像
"系统很安静"，而不是"你查错了名字"。照这份文档写的告警规则同理 ——
永远不会触发，于是给人一种"有人盯着"的错觉。同理，按不存在的事件名
写日志查询，只会得到"最近没有这类事件"的假结论。

重写后的文档全部内容都来自实测：33 个指标的完整目录（名称/类型/标签/
分桶，直接从代码注册表导出）、真实的日志字段与时间戳格式、269 个事件名的
命名空间分布、实际的链路 span 名、已就位的 7 条告警规则与 10 个 Grafana
面板（均已核对引用的指标真实存在）。**所有"计划中但未实现"的项集中放进
一节并明确标注**，共 12 项（日志采样、成本指标、Agent 粒度指标、
`X-Run-Id` 响应头、业务面板等），不再与现状混写。

顺带查明的两处"看着像有、实际是空"：

- **`metrics` 表是死表**：表结构和数据访问层都在，但生产代码里没有任何
  写入点，只有测试调用它，实测 0 行。留着会让人以为指标有历史留存。
- **`/metrics` 端点无鉴权**：无凭据请求实测返回 200。这在容器内网里是
  常规做法，但文档此前没写，部署时容易漏掉网络层限制 —— 现已明确写出。

**三、新增文档与代码的双向一致性门禁。**

新增 17 项检查（`backend/tests/test_observability_doc_parity.py`）：
文档写的每个指标必须在代码里存在，**代码里的每个指标也必须写进文档**
（少写一个和写错一个一样有害 —— 没人会去搜索一个他不知道存在的指标）；
文档里那份"不存在的指标"对照清单必须真的全部不存在；文档引用的日志事件名
与链路 span 名必须真实；已交付的告警规则与 Grafana 面板不得引用幽灵指标。

这套检查用变异测试验证过会真的失败：改一个真指标名、删一行真指标、
把真指标塞进"不存在"清单、编一个事件名、编一个 span 名、改掉小节标题，
六种破坏各自都让 CI 变红。第一版的 span 检查曾把名字写死在测试里，
变异测试照样全绿 —— 一个不读被测文档的断言等于没有断言，已改为从文档解析。

`docs/OBSERVABILITY.md` 已从编码损坏登记表移出，不再享有豁免。
顺带记录一条结论：这类一型编码损坏在本仓库**无法自动恢复** ——
实测每个损坏点有 64 个合法候选，同文档字符集收敛只能唯一确定 9.1%，
结构标尺确定 0 处，且损坏还额外吞掉了相邻字节（该文档从 293 行塌成 268 行），
git 历史里也没有任何可解码的干净版本。所以只能按实测真相重写：
一个猜出来的字比一个明显的乱码更坏，因为读者分不清哪个是原文。

### Fixed — API 文档里有 16 条端点根本不存在，另有 10 处字段/参数是虚构的（2026-08-22，已实测）

`docs/API_SPEC.md` 是前后端对接的唯一书面依据，实测**大面积失真**。
一份声称某接口存在的规范比没有规范更坏：读者照它写调用，拿到 404 后
第一反应是怀疑自己写错了，而不是怀疑文档。

**16 条不存在的端点**（逐条发真实请求量过状态码）：单复数写错的
`/api/v1/project/{id}`、动词与参数顺序颠倒的 `/collections/trigger/{source_id}`、
纯设计稿从未实现的 `/re-score/{id}` 与 `/audit` 与 `/discoveries/stats`、
挂错层级的 `/participation-tasks`、动词写错的 `PUT .../funding`、
以及 `/api/version`（真实路径是 `/version`）等。其中 8 条还被明确标着「已实现」。

`/collections/logs` 这条最阴：实测回 **405** 而不是 404 —— 因为它恰好匹配上
`PATCH /collections/{source_id}` 的路径模式。405 看起来像「端点在、只是动词错了」，
比 404 更容易让人往错的方向排查。

**10 处虚构的字段与参数**，其中三处会造成静默错误：

- `GET /projects` 的 `label`/`sector`/`stage` 文档说「支持逗号多选」——
  实测 `label=FARM,WATCH` 返回 **0 条**：它被当成一个字面值精确匹配，
  既不报错也不匹配。文档还凭空多出 `search`/`limit`/`order` 三个参数。
- `GET /discoveries` 文档列了 7 个参数，**只有 3 个真的存在**。传不存在的参数
  FastAPI 会静默忽略 —— 实测 `?source=defillama&label=FARM` 依然返回全部 693 条，
  调用方以为筛选生效了，拿到的是全量数据。
- `POST /events` 文档说 `event_type` 限定 5 个枚举值 —— 实测**完全不校验**，
  任意字符串都收。一个不校验的枚举、读文档的人却以为它在校验，
  埋点名打错不会有人拦，只会在统计里多出一个孤立事件名。

另外校正：`/auth/anonymous` 是**全仓唯一不走 `{ok, data}` 包络**的端点
（字段是 `access_token`，有效期 3 天不是 30 天）；`§22 数据模型`把
**数据库列名**当成 API 字段名列了出来 —— 这正是详情页四个字段读空的根源，
现已把两者分开写；`/collections/{source_id}/trigger` 是**同步跑完采集并写库**
才返回 200，不是文档说的「202 排队 + 返回任务 id」。

同时修掉该文档**从 §18 起断裂的章节编号**：后半段从「## 21. interactions」
起重新从 21 开始编号，与前半段 §21–§26 整段撞号 —— 同一份文档里「§23」
既指版本管理又指隔离队列，交叉引用有一半概率翻错，而且**两处都真实存在**，
所以看不出自己翻错了。现已平移为 §27–§37，编号 1–37 连续无重复。

新增 `backend/tests/test_api_spec_parity.py`（13 项断言）：把文档每个端点章节
**以及 §3 端点总览表**与 `GET /openapi.json` 的真实路由表双向比对——路径要存在、
动词要对、真实存在的路由不许漏列（漏写比写错更隐蔽：读者不会去找一个他不知道
存在的端点，于是现成功能被重复实现）、标着「未实现」的必须真的不存在
（反向也查：把可用端点标成未实现会让人绕开现成功能）、章节编号不得重复。

单独校验总览表是必要的：**16 条幽灵端点里 13 条只出现在那张表里**，
章节标题一条都没写。总览表恰恰是读者第一眼看的地方 —— 只校验章节标题
等于把门修在后院。

顺带修掉一处「假门」：§3.1 幽灵端点表原本把方法和路径挤在同一个单元格里，
于是解析器里「跳过 §3.1」那段逻辑**永远不会被触发** —— 一条永不执行的规则
等于没有规则，而测试照样全绿。改成与 §3 同布局后该规则才真正生效。
这是靠变异验证（改掉 §3.1 的标题、预期变红却全绿）才暴露的。

另新增 §2.1 写操作鉴权分布表：全仓 21 个写端点**只有 4 个**要求管理员，
其中 `POST /collections/{source_id}/trigger`（会真的打外部 API 并写库）、
`PATCH /collections/{source_id}`、`PATCH /projects/{id}/funding` 三个
匿名 token 就能调 —— 记录现状供上线前评估，未擅自改动鉴权配置。

变异 12 次全部只红对应断言，文档字节级还原。

### 洞察页「高风险团队」：74% 的条目被染成和高危一样的红色

Fixed —— 「高风险团队」列表实测返回 **270 条：high 71 条、medium 199 条**，
也就是 **74% 是「中」**。但徽章一律写死红底红字，还直接显示英文
`high` / `medium`。

**同一种视觉强度代表两种严重程度，等于把分级取消掉了**：要么把 199 个中风险
全按高危处理，要么满屏红色一起无视、连真正的 71 个也漏掉。现在按档位分色
（高=红 / 中=琥珀 / 其他=中性灰），并复用 `riskLevelZh()` 显示中文「高 / 中」。
CSV 导出的风险等级列同样改为中文，与界面一致。

同时删掉一处编造的兜底：原代码在 `flags` 缺失时填入
`['匿名团队', '无公开仓库']` —— 其中 `无公开仓库` **后端根本没有这个 flag**。
真到后端不发这个键那天，界面会替后端凭空断言「这个团队匿名、没有公开仓库」。
现在兜底成空数组，没有标记就不渲染标记行。**编造一个看起来合理的默认值，
比留空危险得多**：读者无法分辨「系统查到了这两条」和「系统什么都没查到」。

`test_frontend_flag_parity.py` 新增 4 项断言把这些钉住：每个档位必须有独立
分支（档位从路由源码的过滤条件解析，路由放宽时测试会红）、不同档位不许共用
配色、flags 兜底不许含硬编码内容。变异 5 次全部只红对应断言。

写这组测试时自己撞上了一个反面案例：断言「`无公开仓库` 不许出现在源码里」
第一次就红了 —— 红的原因不是代码里还留着它，而是**我在代码上方的注释里
解释了「不要写这个」**。字符串匹配分不清「描述规则的文本」和「遵守规则的
代码」，反过来更危险：注释里恰好出现要断言的字样，就能让一条本该报警的
断言假通过。现在这类断言先剥注释再匹配，并为剥注释函数本身加了自检。

### 删掉一个从来没人用的导航外壳组件

Removed —— `frontend-next/components/AppShell.tsx`（137 行，自带一套只有 3 项的
导航、健康指示灯、主题切换、移动端底栏）**全仓没有任何文件 import 它**。
真正在跑的是 `components/Nav.tsx`（10 项导航）。

孤儿组件的危害不是占空间，而是**它会被当成现状读**。我自己审导航时就先翻到
它那份 3 项清单，一度以为侧栏只有 3 个入口。更麻烦的是它带着一份独立演化的
旧逻辑（自己探健康、主题存 `aa-theme-v2`），有人照它改了半天，刷新页面毫无
变化 —— 因为真正生效的是另一个文件。`tsc` 和 `eslint` 都不会报这种文件：
它自身语法和类型都正确。

新增 `backend/tests/test_frontend_structure.py`（6 项）钉住三件事：
组件必须被真的 import；侧栏每个入口必须对应真实页面（点进去 404 的入口，
用户会以为是系统坏了）；每个页面必须有入口能走到（**没有入口的页面等于没做**，
功能在仓库里但用户永远找不到）。变异 3 次全部只红对应断言。

### Changed — 编码损坏「二型」清零，豁免清单从此为空（2026-08-22，已实测）

`docs/API_SPEC.md` 的 70 处二型损坏（整个中文字符变成半角 `?`，文件仍是
合法 UTF-8）已全部修完，`scripts/check_encoding.py` 的 `KNOWN_BROKEN_MOJIBAKE`
清单清空，二型现在是零豁免硬门禁。

修法不是猜原字 —— 那样等于用机器编的文字冒充原文，读者分辨不出。损坏点都落在
描述接口行为的中文散文里，所以是**按实测重写整段**：每句话都对着
`GET /openapi.json` 与真实请求校对。上面那条 Fixed 记的 26 处失真，
就是在这个过程中查出来的。

**这也是为什么豁免清单危险**：只要文件挂在清单上，就没人会去逐行读它，
于是错的内容跟错的字节一起躺着。那 16 条假端点在仓库里存在了很久，
一直没人碰，因为「反正这文件已登记待修」。新增 `test_mojibake_registry_is_empty`
正面钉住清单必须为空 —— 往里加文件是倒退，得在测试里显式讨论，不能悄悄加一行。

一型（3 个文件 1116 处非法 UTF-8）与三型（2 处 emoji）仍在登记中待修。

### Added — 旁路机会引擎面板的三张枚举表纳入一致性回归（2026-08-22，已实测）

`OpportunityWorkflowPanel.tsx` 是全前端最大的一处枚举集中地：工作流状态 7 个、
资格判定 3 个、存活判定 3 个。实测**当前三张表都与后端一致**，没有发现现存缺陷
—— 这条属于补网，不是修 bug。

值得记的是「多一项」的后果比「少一项」严重：少一项界面上冒出英文枚举值，
用户至少看得出不对；多一项则是给了一个**后端会 422 拒绝**的下拉选项，
用户选了、点保存、失败，而界面只会说「保存失败」。

新增 3 项断言（`test_frontend_enum_parity.py` 现共 22 项），双向比对。
变异 4 次全部只红对应断言：删 `BLOCKED`、加一个不存在的 `PAUSED`、
删 `ineligible`、给存活判定加一个 `pending`。

### Fixed — 「阶段」这两个字在界面上代表了两种不同的东西（2026-08-22，已实测）

系统里有两个都叫 stage 的字段，含义完全不同：

- **部署阶段**（`projects.stage`，采集器写入）：`ideation` / `testnet` / `mainnet`
  —— 说的是「代码上到哪张网了」。实测分布：主网 184 / 测试网 97 / 构想期 5 / 空 2。
- **叙事生命周期**（`NarrativeResult.stage`）：`early` / `growth` / `peak` / `mature`
  —— 说的是「这条赛道的热度处在周期哪一段」。实测分布：成长期 208 / 成熟期 64 / 高峰期 9。

而前端只有一个 `stageZh`，**同时收录了两套词汇的 8 个键**。一张表接受两套取值，
等于把口径错配变成了看不出来的错配：传错词汇不会显示英文原文（那样反倒能被
发现），而是显示另一套口径下一个看着很合理的中文。

具体已经在发生的错：详情页「叙事」面板的「阶段」写的是
`narrative.stage ?? project.stage` —— 兜底看起来贴心，实际是拿另一套口径的
答案填这一格。实测有 **7 个项目**没有叙事结果，它们的生命周期那一格显示的是
「主网」/「测试网」，一个属于部署口径的词。

修法：拆成 `stageZh`（只认部署三值）和 `lifecycleStageZh`（只认生命周期四值），
详情页叙事面板只读 `narrative.stage`，缺了就显示「—」。

顺带删掉 `timingZh` 里一个不可达条目 `growth: '上升期'`。穷举
`stage_to_timing()` 的全部 4 个合法输入，输出只可能是 `early` / `peak` / `late`
—— 这一行是永远走不到的死代码，却让人以为系统还有第四种时机判断。

新增 4 项断言（`test_frontend_enum_parity.py` 现共 19 项）：两张表各自不得收录
对方的取值、`timingZh` 不得有不可达条目、详情页不得再兜底跨口径。
部署阶段的真值取自采集器源码里的 `stage="..."` 字面量与
`defillama._infer_stage` 的返回语句；时机的真值靠**真的调用** `stage_to_timing()`
穷举，而不是读 pattern —— pattern 说的是「允许什么」，这里要的是「实际会产出什么」。

变异验证 5 次全部只红对应断言，文件字节级还原。

### Changed — 发现页的来源下拉不再私藏一份采集器清单（2026-08-22，已实测）

发现页「来源」筛选下拉原先在前端写死了 10 个 `source_id`。实测这份清单**当时
确实与后端一致**（`GET /collections/sources` 返回的正是同样 10 个）—— 但这属于
「碰巧对上」：后端加一个采集器，这个下拉就会漏掉它，而漏掉的表现是「筛选不到
那个来源的数据」，不是报错，几乎不可能被发现。

改为在页面加载时请求 `/collections/sources`。请求失败时下拉只保留「全部来源」，
不退回一份可能过时的副本；顶部「今日新增」卡片下的「N 个采集源」在拿不到清单时
显示「采集源 —」而不是「0 个采集源」——「0 个」是一个具体且错误的断言，「—」
才是「还不知道」。

新增 2 项断言防复发（同属 `test_frontend_enum_parity.py`，现共 15 项）。
其中一项在写的时候先踩了自己一脚：最初只断言源码里出现过
`/collections/sources` 字符串，而文件顶部的说明注释里也写着这个路径 ——
把真正的调用改坏，测试照样绿。改成匹配 `apiFetch<...>('/collections/sources')`
的实际调用后，「路径改坏」和「整段删掉请求」两种变异都能被抓到。
这正是本仓反复出现的那个坑：描述规则的文本与遵守规则的代码长得一模一样。

变异验证：把硬编码清单加回来 → 清单断言红；把 apiFetch 路径改坏 → 调用断言红；
整段删掉请求 → 调用断言红；三次都字节级还原。

### Fixed — 项目详情页有四格数据永远读不到，因为键名对不上（2026-08-22，已实测）

详情页「四路分析」摊开显示后端算出的叙事/团队/风险/代币经济。逐个键对着真实
API 响应核对后，发现四处读了**后端从不返回的键名**，而 TypeScript 不会为此报错，
`?? ''` 兜底之后页面只显示「—」或「无」：

| 页面上那一格 | 之前读的键 | 真相 |
|---|---|---|
| 团队 → Flags | `team.flags` | 字段叫 `team_flags`，`flags` 从不出现 → **永远显示「无」** |
| 团队 → 风险 | `team.risk_level` | 后端算了但只打日志，没有字段承载 → **永远空白** |
| 风险 → 交互成本 | `risk.farming_cost` | 同上，`assess_farming_cost()` 结果只进日志 |
| 代币经济 → 解锁压力 | `tokenomics.unlock_pressure` | 该键只在 `risk` 块 → **永远显示「—」** |

实测确认：落库的 281 条记录里，`team_json.risk_level`、`team_json.flags`、
`risk_json.farming_cost` 三个键各出现 **0 次**。

「永远显示『无』」比空白更坏 —— 它看起来像「这个项目没有风险标记」，
而不是「我读错了键名」，所以肉眼几乎不可能发现。

修法分两类，没有一处是「把字面量改对了就算完」：

- **后端本来就算了、只是没落库的**：`TeamResult` 新增 `risk_level`
  computed_field（由 `team_score` 唯一决定，不允许外部传一个与分数矛盾的档位），
  `RiskResult` 新增 `farming_cost` 字段。顺带消掉一处第四份重复
  ——`routers/v1/insights.py` 原先又抄了一遍分档三元表达式，现改为调用同一个
  真值函数；`services/ai_brief.py` 读的也是同两个不存在的键，一并修正。
- **前端读错位置的**：Flags 改读 `team_flags`；「代币经济」里那格重复的
  「解锁压力」直接删掉（压力真值在风险面板已显示一次，tokenomics 块只有
  `unlock_penalty`）。

历史行（本次改动之前打的分）处理方式**刻意不一致**，因为可重放性不同：

- `risk_level` **补算** —— 它由 `team_score` 唯一决定，而 `team_score` 是落库的，
  等于重放同一个映射，不是猜测。实测 4 个历史项目现在都拿到了正确档位。
- `farming_cost` **不补** —— 它的输入 `has_points_program` 不在 `projects` 表里，
  无法忠实重放。历史行因此没有该键，前端显示「—」。宁可显示「不知道」，
  也不端出一个看起来很像真值的默认值。

新增 `backend/tests/test_frontend_field_parity.py`（8 项）：从 TSX 里抓出所有
`team.xxx` / `risk.xxx` 形式的读法，与后端 Pydantic 模型 `model_dump()` 的键
集合比对，不在其中又不在豁免表里就红。另有几项专盯 computed_field 的两个坑：
忘了 `@computed_field` 装饰器时属性仍可访问、但 dump 里没有它（于是落库缺键，
前端回到永远空白）；以及 `extra="forbid"` 与 computed_field 的天然冲突会让任何
「把 dump 喂回构造器」的导入/重算路径硬失败。
`test_projects.py::TestLegacyRowBackfill`（3 项）锁住历史行的补算与**不补算**。

变异验证（每条都实跑并确认只红对应断言，之后字节级还原）：拿掉
`@computed_field` → 2 红；删 `farming_cost` 字段 → 3 红；删回放兼容 validator
→ 1 红；删历史行补算 → 1 红；补算时不检查分数是数字 → 造假断言红；
给历史行硬塞 `farming_cost` 默认值 → 不补算断言红。

同时重写 `docs/API_SPEC.md` §6：原文把路径写成单数 `/api/v1/project/{id}`、
按数据库列名（`team_json` 等）描述响应、还列了一个不存在的 `lineage` 字段。
现按实测响应重写，并加一张字段归属表点明上述四个易错点。

### Fixed — 数据来源中文名漏了两个、多了一个不存在的（2026-08-22，已实测）

`sourceZh()` 负责把后端存的来源标识翻成中文。后端会写进 `projects.source` 的
取值来自三处：10 个采集器的 `source_id`、种子数据 `seed`、Excel/CSV 导入 `import`。

清点后发现：

- **漏了 `rootdata`**（RootData 采集器）→ 该源采到的项目在界面上显示原始标识
- **漏了 `import`** → 通过刚接通的导入功能进来的项目，「数据来源」一栏显示英文
  `import`。上一个 PR 才把导入按钮接上，这条路径立刻就会走到
- **多了 `manual`（手动）** → 后端全仓搜索确认**从不产出**这个值，是个死条目，
  且会让人以为系统支持手动录入

已补齐两个、删掉一个。实测数据库现有 288 个项目的来源分布：
defillama 165 / seed 99 / github 16 / 空 7 / import 1 —— 那个 `import` 就是
上一轮验证导入功能时留下的真实数据，界面上原本显示的正是英文。

一致性回归扩到来源表（`test_frontend_enum_parity.py` 增至 13 项）：
采集器清单从 `app/collectors/*.py` 解析而来，后端加新采集器时测试自动跟上。
保留一个显式例外：裸 `twitter`（采集器只声明 `twitter_kol`/`twitter_keyword`，
但归类逻辑会同时匹配裸值，历史数据里存在），例外写在测试注释里而非默默放过。

变异验证：删掉 `import` 中文名 → 覆盖断言失败；把 `manual` 加回来 →
幽灵条目断言失败；还原后 13 项全过。

### Removed — 通知中心删掉三个永远为空的分类入口（2026-08-22，已实测）

通知中心侧栏列了 7 个分类，但后端 `notifications.py` **只聚合三类**：
新机会（今日新建且 FARM/WATCH）、评分变化（最新两次打分有变动）、
采集器告警（今日采集失败）。

多出来的 **「截止提醒」「资金与融资」「AI 摘要」** 后端从不产出，
于是它们是三个**常驻显示 0 的入口，点进去永远是空列表**。
概览区还有一格「截止时间临近」，副标题写着「有数据时显示」——
实测那个数永远是 0，因为不存在会让它变成非 0 的代码路径。

一个永远为空的入口不是「暂时没数据」，而是在承诺一个不存在的功能。
三个入口已删除；概览那格换成后端真的会产出的「评分变化」，
原来的「新机会 / 评分」合并格也拆开成两个独立指标（此前两类混算一个数）。

实测后端 `GET /notifications` 当前返回 1 条，类型分布 `{'score': 1}`。

一致性回归同步扩到通知类型（`test_frontend_enum_parity.py` 增至 11 项）：
**双向断言**——后端有而前端没有 → 该类通知在侧栏无处可看；
前端有而后端没有 → 承诺了不存在的功能。中文名表与配色表一并双向检查。
后端类型清单从路由源码解析而来，后端加新类型时测试自动跟上。

变异验证：把 `deadline` 加回前端 → `test_frontend_has_no_phantom_types` 失败；
删掉前端的 `collector` → `test_frontend_covers_every_backend_type` 失败；
还原后 11 项全过。

### Fixed — 洞察页漏了一个扣分信号，把它显示成了中性说明（2026-08-22，已实测）

洞察页给团队信号做中文化和正/负分色，真值在后端 `app/agents/team.py` 的
`FLAG_ADJUSTMENTS`——每个信号带一个正负分数，正负号就定义了它是利好还是风险。

前端那张表漏了 **`wash-trading VC`（后端 −0.20，明确的负面信号）**。
漏掉的后果不是「少显示一个」，而是它会走兜底分支：**显示英文原文 + 中性灰**——
一个扣分项被渲染得像无关紧要的补充说明。看这一页的人不会意识到那是风险。

已补上中文名「刷量 VC」并归入负面信号。

同时加了**跨语言一致性回归**（`backend/tests/test_frontend_flag_parity.py`，6 项）：
后端每个信号必须有中文名、前端不能编造后端不产生的信号、
正/负分色必须与后端分数符号一致、每个信号都必须明确归类（不许靠兜底染成灰色）。
这类错误没有任何运行时报错，只能靠一致性断言发现。

经变异验证会真的失败：删掉 `wash-trading VC` 的中文名 →
`test_every_backend_flag_has_chinese_label` 失败；
把 `anonymous team`（−0.25）归到正面表 →
`test_sign_classification_matches_backend` 失败；还原后 6 项全过。
另外还有两项测试专门检查**解析器本身**——一个永远返回空集合的解析器
会让上面所有断言假通过，那比没有测试更糟。

### Added — 同一套思路推广到剩下几张前端硬编码表（2026-08-22，已实测）

洞察页那个漏掉的扣分信号不是孤例，而是一类问题：**前端有若干「必须与后端保持
一致」的常量，却没有任何机制保证同步**。后端改了前端不会报错，只会静默错。

清点并锁住了剩下三处（`backend/tests/test_frontend_enum_parity.py`，8 项）：

- **投资组合页的结果枚举**（7 种）与**状态枚举**（4 种）——漏一项就会在中文界面里
  直接显示英文枚举值如 `not_airdropped`。配色也一并检查：缺色会退回中性灰，
  把「下跌」显示得和「未知」一样。
- **复盘页的批量提交上限**必须等于后端 `max_length=50`。前端偏大 → 整个请求被
  422 拒绝，用户只看到「保存失败」且无从得知原因；偏小 → 白白多发几轮请求。
- **复盘页提供的结果选项**必须都是后端认的值，否则提交必然 422。
  这里刻意**不要求覆盖全集**——复盘页只让用户标三种最明确的结果是产品选择，
  测试只管「提供的每一种都合法」。

实测结论：这三处当前**都是对的**，没有发现新的不一致。但同一类错误刚发生过一次，
锁上的成本远低于下次再靠肉眼发现。

经变异验证会真的失败：删掉投资组合页 `breakeven` 的中文名 → 对应断言失败；
把复盘页上限从 50 改成 100 → 上限断言失败；还原后 8 项全过。
同样附带解析器自检。

### Fixed — 项目详情页的评分权重与标签阈值不再写死，改由后端暴露真值（2026-08-22，已实测）

上一条修完设置页和运维台之后，同类问题在项目详情页还剩最后一处：
8 维子分每一项旁边的权重（`×0.18` 之类）和底部那行「阈值 FARM≥65 / WATCH≥50」
**都是写在前端文件里的字面量**。

这两组数字的性质不一样，但风险相同：

- **8 维权重**的真值在后端环境变量（`WEIGHT_*`，启动时断言 Σ=1.0）。
  实测当前 8 项与前端写死值恰好一致，所以现在看不出问题——
  但 `.env` 一改，页面就会继续显示旧权重。
- **FARM/WATCH 阈值**更值得警惕：这两个数**已经被调过一次**
  （v1.1 把 FARM 从 70 下调到 65）。也就是说这类写死副本变成错的不是假设，
  是已经发生过一次的事，只是当时前端文案跟着改了。

修法上刻意没有走「把前端字面量更新成当前正确的值」这条捷径——那只是把
下一次说谎推迟。改成：

- 后端 `/settings/config` 的 `thresholds` 块新增 `LABEL_FARM_THRESHOLD` /
  `LABEL_WATCH_THRESHOLD`，值从 `scorer.LABEL_THRESHOLDS` **查表**得来，
  不抄第二份常量。
- 前端 8 维权重按 `WEIGHT_*` 键从后端 `weights` 块取；拿不到就显示 `×—`。
- `weight_version` 优先用项目记录自己的值（它记的是「当初用哪版权重打的分」），
  项目上没有才退到后端当前版本，两者都没有显示「—」。此前是硬编码回退到 `v1.2`。

同时加了一道**回归锁**：`tests/api/test_settings.py` 断言本端点返回的两个阈值
必须等于 `scorer.LABEL_THRESHOLDS` 的真值。这条断言经过变异验证——
把端点里的 FARM 阈值改回抄死的 `70.0`，测试确实失败（`1 failed`），
改回来 11 项全过。**以后谁再调阈值而忘了同步，CI 会红，而不是页面静默说谎。**

`docs/API_SPEC.md` 同步记录了这两个新字段及「前端不得自行写死」的约定。

设置页的权重表也做了同样处理：8 行不再各带一个写死的兜底值，
并且**权重合计只有 8 项都从后端拿到时才显示，缺一项显示「—」**。
此前缺值会回落到写死权重，于是无论后端是什么，那一行都稳定显示「1.00 ✓」——
一个永远为真的校验等于没有校验。现在合计不等于 1.0 会显示「✗」。

### Fixed — 前端「看起来能用其实不能用」的控件与写死数据，全部换成后端真值（2026-08-22，已实测）

这一轮专门查一件事：**页面上让人以为能操作、或让人以为在看真实数据的地方，
背后到底有没有东西**。查出四类问题，都已修好。

**一、运维台「数据导入导出」三个按钮完全没接线**

`导出 Excel` / `导出 CSV` / `下载模板` 三个按钮的 `onClick` 数量实测为 **0**——
点了什么都不会发生；上传区域也只是个装饰性的框。而后端**四个接口其实早就写好了**，
一直没人调用。现已全部接通，并逐个用真实 HTTP 请求验证：

| 接口 | 实测结果 |
|---|---|
| `GET /export/projects?format=excel` | 200 · 28842 字节 |
| `GET /export/projects?format=csv` | 200 · 45048 字节 |
| `GET /export/template` | 200 · 5351 字节 |
| 筛选条件无匹配 | 404 · `没有找到符合条件的项目` |
| 不带管理员密钥 | 401 · `Missing API key or token` |

上传现在支持点击、键盘和拖拽三种方式，10MB / 100 条的后端上限会以中文提示返回，
校验失败的行最多列出 20 条具体原因。401/403 不再显示成含糊的「请求失败」，
而是直接说清是「前端服务端没配 `BACKEND_API_KEY`」还是「当前身份不是管理员」。

**二、删掉一行凭空编造的导入历史**

原页面固定显示「最近导入：08-02 · 24 项成功 / 2 项失败」。**后端没有任何导入历史接口**，
这个数字是写死的假数据。已删除，改为显示本次会话真实发生过的导入结果，
没有则显示「本次会话尚未导入（后端无导入历史接口）」。
处理原则和之前一致：**不能验证的猜测比留白更糟**，因为读者分辨不出哪句是真的。

**三、设置页有一整套可以填、可以拨、但存不进去的控件**

设置页顶部写着「只读」，但页面里实际有 **20 多个可编辑输入框**、
一个可拨动的「LLM 增强总开关」、以及「添加接口 / 删除接口 / 添加模型」等按钮。
后端**没有配置写入接口**，这些操作只改前端内存状态，刷新即消失。
其中最容易误导人的是 LLM 总开关和 API Key 输入框：拨过去显示「已启用」、
把密钥填进去看起来像配好了，实际上后端一动不动。

更麻烦的是，那些输入框的默认值大多是**写死的字面量**而非后端真值——
碰巧一致时看不出问题，等 `.env` 一改，页面就会自信地显示旧值。
LLM 接口默认还写着 `api.openai.com` + `gpt-4o-mini/gpt-4o`，与实际配置无关：
后端一个接口都没配时，页面看起来像「已经配好了 OpenAI」。

现在整页 `<input>` 数量为 **0**，6 个开关全部 `disabled` 且标注「（只读）」，
所有数值一律来自 `GET /settings/config` 与 `GET /llm/status`，
后端没给值就显示「—」而不是回落到某个默认值——**「不知道」必须和「是这个值」能区分开**。
密钥字段只显示「已设置 / 未设置」（后端本来就只返回布尔值，从不返回密钥内容），
带小眼睛的密钥输入框整个删除：既写不进去也没有值可显示，留着只会诱导人往里填密钥。

顺带补上了 **9 项真实生效但页面从没展示过**的配置：4 个归档相关保留期与归档 cron、
misfire 补跑窗口、日志格式、链路追踪、数据库后端、运行环境。
归档子系统 08-22 才落地，此前看设置页的人不会知道有它。

**四、运维台采集源排期表 11 项里 5 项是错的**

各采集源卡片上显示的 cron 来自一张前端写死的对照表。逐项和后端比对后发现：

| 采集源 | 前端显示 | 后端实际 |
|---|---|---|
| cryptorank | `0 9 * * *` | `15 9 * * *` |
| rootdata | `0 10 * * *` | `45 9 * * *` |
| twitter_kol | 每天 06:00 | **每小时一次** |
| etherscan | 每天 07:00 | **每 6 小时一次** |
| layer3 | 每天 11:00 | `30 10 * * *` |

运维台的用途正是判断「这个源什么时候会自己跑」，显示错的排期比不显示更糟。
写死的表已删除，改为从 `/settings/config` 的 `sources` 块实时读取。
同一页「分析调度」原本也写死成 `0 8 * * *`，现同样改为真值。

### Added — 前端第一套单元测试（20 个，零新增依赖）

前端此前**没有任何测试框架**。这次给新抽出的解析逻辑
（`frontend-next/lib/download.ts`：文件名解析、错误信息映射、上传校验）
补了 20 个测试，全部通过。

三个刻意的选择：

- **零新增依赖**：用 Node 24 自带的 TypeScript 直接执行 + 内置 `node:test`，
  不引入 vitest/jest。`npm audit --audit-level=high` 必须保持 0，
  每加一个依赖都是以后的审计面。
- **测的是真文件**：测试直接 import 页面在用的那个源文件，不是复制一份。
  测一份副本等于什么都没测——这也是把解析逻辑从页面里抽出来的原因。
- **证明它会失败**：故意把源码里的兜底逻辑改坏，套件确实报错退出 1，
  改回来再确认 20/20。没证明过会失败的测试不值得信任。

CI 的 Node 版本从 22 提到 24：这些测试依赖原生 TS 执行，
该能力在 22.x 需要开关、在 24 是默认。本地绿 / CI 红的版本偏差是最难查的一类故障，
所以两边钉在同一大版本。CI 里同时新增了 `npm test` 步骤——
**测试写了但没在 CI 里跑，等于没写**。

### Fixed — Trivy job 里叠着第二个独立故障：SARIF 上传无权限（2026-08-22，已验证）

**验证结果**（PR #5 合并后 master `05741b3` 上那次 push 运行的实测）：
`Upload Trivy results` **success**，整个 job **success**，
且 `code-scanning/analyses` 里**第一次出现 `ref=refs/heads/master` 的记录**
（`results_count=0`，Trivy 0.74.0）—— 此前所有记录都只有 `refs/pull/*`，
这正是「上传只在 PR 事件下成功」的直接证据。
master 上三个 workflow（`CI` / `Security Scan` / `Docs Link Check`）**全部 success**。

合并 PR #4 之后，`Docker Image Trivy Scan` 在 master 上**仍然红着**，
但原因和之前完全不同：

```
Run Trivy scan          success   ← Trivy 本身 0 漏洞，修复是有效的
Upload Trivy results    failure   ← Resource not accessible by integration
```

**实测原因**：仓库默认 workflow 令牌权限是 `read`
（`actions/permissions/workflow` → `default_workflow_permissions: read`），
而 `upload-sarif` 需要 `security-events: write`。
`pull_request` 事件下这一步能成功（PR #4 的记录确实在 `code-scanning/analyses`
里，`ref` 为 `refs/pull/4/merge`），但 `push` 事件下同一步骤被拒。

修法是**只在 `container-scan` 这一个 job 上声明所需权限**，
而不是把仓库默认改成 write —— 默认 `read` 是对的，
一个 job 需要更多权限不该成为放宽所有 workflow 令牌的理由。

**这同时更正了前一条记录的说法**（原文保留在下方）。我此前写「这个 job 红 13 天
是因为 36 个高危」「SARIF 上传成功但告警数为 0」，两句都只对一半：
**一个 job 里叠了两个独立故障**，漏洞那个把权限那个挡住了 ——
Trivy 先 exit 1，上传成不成功根本轮不到显现。
逐步骤走了 08-09 以来 master 上这个 workflow 的每一次运行：

| 日期 | commit | 事件 | Run Trivy scan | Upload |
|---|---|---|---|---|
| 08-22 | d1b710b | push | **success** | failure |
| 08-17 | 3d6d7ef | schedule | failure | failure |
| 08-13 | d77d827 | push | failure | failure |
| 08-13 | 237b23c | push | failure | failure |
| 08-10 | fd0cb60 | schedule | failure | failure |
| 08-09 | fd0cb60 | push | failure | failure |

**上传从第一天起就在所有非 PR 运行里失败**，只是没人看见，因为扫描先失败了。
修好一道闸门，露出了它背后的下一道。此前观察到的「告警数为 0」正是这件事的可见症状。

顺带修两个让上一个修复无法验证的问题：

- **push 路径过滤不包含 `security.yml` 自己** —— 改这个 workflow 不会触发它。
  想验证权限修复，得顺手改个 requirements 或 Dockerfile，或者等周一的定时任务。
  对一个红了 13 天的 job 来说这是很糟的处境。
- **加了 `workflow_dispatch`**（手动触发）。这个 job 之所以一直没人诊断，
  部分原因就是复现一次运行得先凑出一个符合路径过滤的提交。

两处都不改变扫描做什么、拦什么。

### Merged — PR #4 与 #5 已合入 master，远程全绿（2026-08-22）

| PR | 合并 commit | 内容 |
|---|---|---|
| #4 | `d1b710b` | 46 个 commit、283 个文件：归档子系统、编码修复、门禁修复、上线修复 |
| #5 | `05741b3` | Trivy job 的 `security-events: write` 权限 + 触发器修复 |

合并 #4 前 12 项检查全绿：

```
Full Backend Test Suite   2648 passed, 4 skipped
Coverage Gate             88.21%（10493/11896 行），门槛 80%
Docker Build Check        镜像构建成功 + 容器响应 /health
Docker Image Trivy Scan   0 HIGH（此前 36）
Frontend Lint & Build     npm audit 0 vulnerabilities（此前 9 high）
Check Markdown Links      0 死链（此前 6）
另有 Lint & Format Check / Type Check (mypy) / pip-audit /
Detect Secrets / Dependency Review / Trivy
```

**master 最终状态（`05741b3` 实测）**：`CI` / `Security Scan` / `Docs Link Check`
三个 workflow 全部 success。这是 2026-08-09 以来 master 第一次全绿。

dependabot PR #3 已关闭 —— 其 nanoid 修复已 cherry-pick 进 #4（保留原作者）。
关闭时说明了它卡 5 天的真实原因（假检查名，非代码问题）。

### Fixed — CI 三处长期红灯（2026-08-22）

推送 37 个积压 commit 时开了 PR #4，借 CI 把三项长期失败的检查查清并修掉。
它们**都先于本次改动存在**，只是此前没人看得出失败原因。

- **Docker 镜像扫描：36 个高危 → 0**（`Docker Image Trivy Scan` 自 08-09 起每次都红）

  这个 job 从未给出可读的失败原因：workflow 只让 Trivy 输出 SARIF 到文件，
  失败时日志里只剩一句 `exit code 1`；SARIF 上传成功但 code scanning 告警数为 0，
  run 里也没有可下载的构件 —— 从任何角度都看不到漏洞明细。先加了一步 table 格式
  输出（`exit-code: 0`，只负责让人看见，判定仍归 SARIF 那步），漏洞才第一次露面：

  - **34 个来自基础镜像的 util-linux 家族**：9 个包 × 4 个 CVE
    （`bsdutils` / `libblkid1` / `liblastlog2-2` / `libmount1` / `libsmartcols1` /
    `libuuid1` / `login` / `mount` / `util-linux`），
    CVE-2026-53612/53613/53614（mount 的 TOCTOU 与 nosuid/noexec 绕过）、
    CVE-2026-53615（libblkid 整数溢出）。Debian 已发布修复 `2.41.5-0+deb13u1`，
    但 `python:3.12-slim` 这个 tag 不会随安全更新重新指向新层，所以构建时
    必须显式 `apt-get upgrade`。加上之后 36 → 2。

  - **剩下 2 个的真正来源是 pip 的内嵌依赖清单，升级 pip 修不掉**。
    `setuptools 70.3.0`（CVE-2025-47273）与 `msgpack 1.1.2`
    （GHSA-6v7p-g79w-8964）这两个版本号，与 pip 自带的
    `pip/_vendor/vendor.txt` 里钉着的版本**逐字一致**。pip 把依赖以源码形式
    内嵌在 `pip/_vendor/` 下、不产生 dist-info，Trivy 把 vendor.txt 当成包清单来读 ——
    这也解释了它每次扫描都警告两遍
    「Third-party SBOM may lead to inaccurate vulnerability detection」，
    以及诊断输出里 `PkgPath = None`、target 名叫 `Python` 而非文件路径。
    本机 pip 已是最新的 26.2.1，vendor.txt 里**依然**钉着这两个旧版，
    所以任何 pip 升级都不可能清掉它们。
    改为**从镜像里删除 pip 与 setuptools**，builder 与 production 两个阶段都删 ——
    只删一处没用，因为 production 会 `COPY --from=builder /venv /venv`
    把同一份 pip 搬过去（Trivy 报告里确实列着
    `venv/lib/python3.12/site-packages/pip-26.2.1.dist-info/METADATA`）。
    删除安全性已实测：用 `sys.meta_path` 拦截器让 pip / setuptools /
    pkg_resources / `_distutils_hack` 全部无法导入，应用仍完整启动（28 条路由）、
    `/health` 返回 200 healthy，pandas / numpy / psycopg / alembic / apscheduler
    均正常；numpy 里唯一的 `from setuptools import ...` 位于 `numpy/distutils`
    （构建期代码），应用完整导入后它从未被加载。
    **代价**：镜像内不能再 `pip install` 排障，已在 Dockerfile 注明；
    可选的 OTel 依赖若要装，必须加在删除步骤之前。

- **前端依赖 9 个高危 → 0**（`npm audit` 自 08-13 起让 `Frontend Lint & Build` 红着）

  9 个全部源自 `nanoid < 3.3.18`（GHSA-2v37-7h3g-55p8，CWE-835 无限循环），
  经 `postcss` 传染到 `next` / `tailwindcss` 与各 postcss 插件。
  修法是 **cherry-pick dependabot PR #3 的原始 commit**（`16e4763`），
  而不是自己写版本号 —— 本机 npm registry 不可达（`EPERM`），
  编不出可信的 `integrity` 哈希，只能用它从 registry 取到的那份。
  只改 `frontend-next/package-lock.json` 3 行。实测 `npm audit --audit-level=high`
  从 9 high / exit 1 变为 **found 0 vulnerabilities / exit 0**，
  `tsc --noEmit` 与 `eslint` 仍 exit 0。同时解掉了 dependabot PR #3 卡了 5 天的问题。

- **文档死链 6 条 → 0**（`Docs Link Check`）

  `docs/00_index.md` 与 `ENGINEERING_ROADMAP.md` 仍链向 `0966179`
  （移除遗留 HTML 原型）里删掉的 6 个文件，而索引表还给每一个都标着 ✅：
  `PROJECT_BOOTSTRAP_OVERVIEW.md`、`IMPLEMENTATION_STATUS.md`、
  `PROJECT_BOOTSTRAP_CHECKLIST_V2.md`、`PROJECT_BOOTSTRAP_AUDIT_REPORT_V2.md`、
  `COLLECTION_ANALYSIS_HANDOFF.md`、`DESIGN_REVIEW_CHANGELOG.md`。
  实现状态改指向 `docs/PHASES.md`，并在索引里留了一条说明记录删了什么、为什么，
  而不是悄悄改表。本地复现 CI 判据（仅相对链接、folder-path 与 workflow 一致）
  扫 117 个 md 确认清零。ADR / superpowers 计划 / DELIVERY_CHECKLIST 里的**散文提及**
  保持原样 —— 那是历史记录而非导航，CI 也不检查它们。

### Changed — Trivy 失败时先打印明细（2026-08-22）

`security.yml` 的 `container-scan` 增加一步 table 格式扫描（非阻断），
排在原 SARIF 步骤之前。判定逻辑完全没变，SARIF 那步仍以 HIGH/CRITICAL 阻断。
动机是这个 job 红了 13 天却无人能说出原因 —— 一个只报「失败」不报「为什么」的
门禁，实际效果等于没有门禁。

### Fixed — master 分支保护有 3 个检查名对不上（2026-08-22 已修）

**原判断（保留原文）**：`master` 的 `required_status_checks` 要求 5 项，
其中 **3 项在仓库里没有任何 job 会产出**：
要求 `Lint (ruff)` 而实际 job 名为 `Lint & Format Check`；要求 `Test (pytest)`
而实际为 `Full Backend Test Suite`；要求 `Coverage Gate` 而覆盖率门禁在 pytest
步骤内部、不是独立 job。这 3 项会永远 pending，因此任何 PR 的 `mergeStateStatus`
恒为 `BLOCKED`（dependabot PR #3 卡 5 天正是此因）。
门禁看着有 5 道、实际只有 2 道生效。修法二选一：改保护规则的名字对齐实际 job 名，
或改 job 名对齐保护规则。**未擅自改动** —— 修改分支保护属于放宽门禁，需所有者决定。

**所有者选了第一种（改保护规则名对齐实际 job 名）。已按此执行：**

- **`Coverage Gate` 不是改名能解决的 —— 它需要一个真实存在的 job**。
  覆盖率门槛此前只以 `--cov-fail-under=80` 参数的形式藏在测试步骤内部：
  闸门是真的，名字是假的，而分支保护匹配的正是名字。
  新增 `coverage-gate` job（`name: Coverage Gate`）：不重跑测试（那要 7 分半），
  只下载测试阶段已上传的 `coverage.xml` 并独立断言行覆盖率。
  这**不是**与 `--cov-fail-under` 重复劳动 —— 写在命令行里的阈值被谁调低或删掉
  都不会有任何提示，而这是一道名字可见、能独立失败的闸门。
  选「让名字真实存在」而不是「从必过列表删掉它」：后者是放宽门禁，前者不是。

- 另两项是纯改名：`Lint (ruff)` → `Lint & Format Check`、
  `Test (pytest)` → `Full Backend Test Suite`。

- **闸门本身先被验证过能失败**，因为一个不会失败的覆盖率闸门比没有闸门更糟。
  用人造边界样本逐个实测：恰好 80.00% 放行（边界不得误拒）、79.99% 拦、
  79.00% 拦、缺 `line-rate` 属性拦（不得静默当成通过）、0% 拦、100% 放行 ——
  六个全部符合预期。浮点比较用 `pct + 1e-9 < THRESHOLD`，
  避免二进制表示误差把真正的 80.0% 判成不及格。
  CI 实跑结果：**88.21%（10493/11896 行）通过**。

- **改分支保护时逐项比对了改前改后**（服务器回读，不是复述请求）：
  仅 `contexts` 5 个名字变化，`strict` / `enforce_admins` / `allow_force_pushes` /
  `allow_deletions` / `required_reviews` / `restrictions` /
  `required_linear_history` / `required_conversation_resolution` /
  `block_creations` / `lock_branch` 全部保持原值。
  必过检查数量 **5 → 5，未减少**，且改后 5 个名字**每一个都对应真实 job**。
  改前的原始配置已存为回滚点。

结果：PR #4 的 `mergeStateStatus` 从 `BLOCKED` 变为 **`CLEAN`**。
门禁从「看着 5 道实际 2 道」变成「5 道全部真实生效」。

### Fixed — 归档从未真正运行过（2026-08-22）

从「归档页上那句『暂无运行历史接口』」查起，结果发现它掩盖的不是缺个接口，
而是**归档功能从来没有生效过**，以及两处会导致数据无限增长或被提前删除的缺陷。

- **归档从未被调度**：`app/archive.py` 的 `RawDataArchiver` 逻辑是真实的，但
  全仓只有手动脚本 `scripts/archive_raw_data.py` 会调用它 —— `scheduler.py`、
  CI、compose、Dockerfile 里对 `archive` 零引用，而 `DATABASE_DDL.md` §6.1 却
  写着「每日 cron 执行」。现在归档由 `UnifiedScheduler` 按 `ARCHIVE_CRON`
  （默认 `0 3 * * *`，在采集 job 08:00–10:30 之前跑完）执行
  - 归档失败**不会**让调度器崩掉，失败作为一行 `status=failed` 记入历史
  - 采集与分析都关掉、只开归档时调度器仍会启动（此前 `start()` 的「全都关了」
    判断只看两个开关，加第三档时必须一并修正，否则归档会静默失效）

- **低分采集记录永远不会被归档，会无限累积**：`processed` 只在采集记录被提升为
  正式项目时置 1，而提升要求 `discovery_score >= 0.3`。低分记录永远不会被提升，
  所以**永远不满足 `processed = 1 AND 超期` 这个归档条件**。实测本地库 693 行
  `raw_projects` 中有 509 行（**73%**）是 `processed = 0`，且它们的分数全部
  < 0.3；`processed = 1` 的 184 行全部 ≥ 0.3。这 509 行里也没有任何一行存在
  同 `dedup_key` 的高分兄弟记录，佐证逻辑不会把它们救回来
  - 按最近一次采集的 460 条低分记录估算（`raw_data` 平均 474 B）：1 个月约
    13,800 行 / 6.2 MB，1 年约 167,900 行 / 75.9 MB，3 年约 503,700 行 / 227.7 MB
  - 新增 `UNPROCESSED_RAW_RETENTION_DAYS`（默认 90 天）单独一档。**归档而非删除**
    —— 它们是复盘「当时为什么没立项」、以及日后调阈值做回溯验证的唯一依据

- **归档表自身的保留期零实现**：`DATABASE_DDL.md` 写了归档表 180/365 天保留期，
  但全仓搜不到任何按 `archived_at` 删除的语句 —— 归档表只进不出，等于把无界增长
  从主表搬到了归档表。现在按 `RAW_ARCHIVE_RETENTION_DAYS`（180）/
  `SIGNALS_ARCHIVE_RETENTION_DAYS`（365）真实清理

- **时间戳格式不一致会提前一天删数据**：归档表的 `archived_at` 走 SQLite
  `DEFAULT CURRENT_TIMESTAMP`，写出来是 `'2026-08-22 02:08:51'`（**空格**分隔）；
  而其它时间列由应用层 `datetime.isoformat()` 写入，形如
  `'2026-08-15T14:51:16.959145+00:00'`（**T** 分隔）。SQLite 的 TIMESTAMP 实际是
  TEXT，`<` 是字符串比较，空格 `0x20` < `T` 0x54，所以拿 ISO cutoff 去比
  `archived_at`，**当天写入的行会被判成「早于今天零点」**。实测（保留期设 0 天、
  行刚写入）：ISO cutoff 命中 1 行（刚归档的数据当场被删），空格 cutoff 命中 0 行。
  现在两种 cutoff 分开（`_cutoff` / `_cutoff_db_default`），并有回归测试锁住
  「同一次运行里刚归档的行不得被删」

- **保留期传 0 天曾被静默改成默认值**：构造函数原写 `days or settings.xxx`，
  于是显式传入的 `0`（合法取值，意为「立刻清理」）被当成「没传」。这个 bug 一度
  让上面那条时间戳测试**假通过**，改用 `is None` 判断后才复现出真实缺陷

### Added — 归档运行历史（2026-08-22）

- **新增 `archive_runs` 表 + `GET /api/v1/archive/runs`**：每次归档运行记一行
  （开始时间、触发方式 scheduler/manual/api、耗时、六个分项行数、成功或失败），
  失败也记 —— 只显示成功会让「归档连续几天没跑成」在界面上看不出来
  - **管理员专属**（`/api/v1/archive` 加入 `ADMIN_ONLY_PREFIXES`）：响应含各表真实
    行数与运维配置，与 `/settings` 同一口径
  - 端点**只读**，查看历史不会触发一次清理（有测试锁住）
  - 写历史失败不会掩盖归档结果（记账失败不该丢掉真实成果）
- **`/archive` 页改为真实数据**：六档保留策略各自的当前行数与「待清理」预估、
  归档调度状态与 cron、最近 20 次运行明细。此前只有保留期数字 + 一句占位说明
- **新增 Alembic 迁移 `0003`**：`archive_runs` 表 + 三个 `archived_at` 索引，
  可单独回滚到 `0002` 而不影响其它表（有测试验证回滚后再升回来仍成功）

### Docs — 修正与实现不符的归档文档（2026-08-22）

- **`DATABASE_DDL.md` §6.1 的示意 SQL 缺了 `processed` 条件**：原文写
  `WHERE discovered_at < datetime('now','-30 days')`，看起来「什么都归档」，
  而实现一直带 `processed = 1`。已改为与实现一致，并新增 §6.2（为什么未处理记录
  需要单独一档，含实测数据与增长估算）、§6.3（时间戳格式陷阱）
- `API_SPEC.md` 新增 §31 archive；`.env.example` 补齐 5 个新配置项并说明
  为什么需要它们

### Added — 打通「行动 → 复盘 → 校准」闭环（2026-08-21）

评分决策引擎此前已能算出 162 个重点参与项目，但 `watchlist` / `interactions` /
`feedback` 三张表全为 0 条 —— 结论一条都没被跟进。本次补齐从「看到」到「行动」
再到「反哺校准」的链路。

- **新增今日行动清单**（`GET /api/v1/action-queue` + 工作台卡片）：把 FARM/WATCH
  项目的参与清单跨项目聚合，按「任务优先级 × 项目分数 × 是否必做 × 是否已收藏」
  排序，默认排除已有交互记录的项目。此前参与清单只存在于单个项目详情页，
  上百个项目必须逐个点进去才看得到任务，没有任何视图回答「今天该做什么」
  - 采用**轮转取样**：5 个名额覆盖 5 个不同项目，而非被最高分项目占满
    （实测纯按分数排时 5 个名额只覆盖 3 个项目）
  - 只提供推进资格的任务类别（official/testnet/mainnet/research/risk/dev）；
    track/social 类每个项目都有，会把清单刷满
  - 结果确定性排序，同分不随字典顺序抖动
  - 标记「已做」**复用 `interactions` 表**，不引入第二套状态，记录在参与复盘页可见
- **新增结果复盘页**（`/review`）+ `POST /api/v1/feedback/batch` +
  `GET /api/v1/feedback/pending-review`：把「标十几个项目的结果」压缩到一次请求。
  权重校准门禁需要 200 条样本，而逐条进入项目详情页提交的成本让这个数字实际
  不可能达到（实测 feedback 表为 0 条，校准能力永久空转）
  - 每行三个按钮（空投了 / 没空投 / 归零），选完一次性批量提交
  - 整批在**单事务**内写入，不会出现「标了 10 个成功 3 个」的中间状态
  - 有交互记录的项目排最前（你真投入过，结果对校准最有价值）
  - 页面显示校准进度条（当前样本 / 门禁 200）。**门禁阈值未被改动**，
    降低的是录入成本而不是安全标准

### Security — 批量反馈端点加固（2026-08-21）

- **`POST /feedback/batch` 增加项目存在性校验**：该端点与既有 `POST /feedback`
  一样只需匿名 token（`/api/v1/feedback` 不在 `ADMIN_ONLY_PREFIXES` 里）。缺少
  存在性校验时，任意 `project_id` 都会入库 —— 实测**一次请求**注入 200 条伪造
  ID（`ghost-0..199`）即可让 `calibration_ready` 变为 `True`，等于用凭空数据
  决定真实的评分权重。现在未知 `project_id` 整批拒绝（404），一条伪造 ID 会让
  同批次的真实条目也不写入
- **批量条数上限由 200 收紧到 50**：与校准门禁同为 200 时，单次调用即可填满门禁。
  压到 50 使填满至少需要 4 次请求，叠加限流后提高投毒成本；真实使用一屏也标不到
  50 个。前端改为自动分批发送，用户勾再多也不会吃 422
- 补回归测试：伪造 ID 被拒、混合批次整体回滚、404 不被兜底 `except` 改写成 500

### Fixed — 术语闸门此前是坏的（2026-08-21）
**发现的问题**：`scripts/check_terminology.py` 是防止「评分决策引擎」术语回退的
唯一机械闸门（CLAUDE.md §1），但实跑 `--all` **直接失败** —— 3 个文件 5 处命中。
更关键的是它**自己一行测试都没有**，所以这个坏状态没人知道。

5 处命中里有 3 处是**不该改的**：`CLAUDE.md` 那行正在定义禁用词清单本身，
`SESSION_MEMORY_2026-07-26.md` 那行引用的是历史 git commit message（改它就是
篡改记录）。这说明问题不在文档，在闸门缺少表达"这里必须写出禁用词"的手段。

**修复**：
- 新增**行级豁免**机制（行尾加 `terminology-ok`）。刻意做成逐行显式、可
  `grep -rn terminology-ok` 审计，**不做整文件豁免** —— 否则真正的术语回退
  会藏在被豁免的文件里。当前全仓真豁免只有 **2 处**，都有明确理由
- 修掉 1 处真实的术语回退：`docs/DEPLOYMENT_REPORT_FINAL.md:115`
- `iter_tracked_files()` 改用 `shutil.which("git")` 解析全路径（ruff S607），
  并补 `check=False`
- 新增 **27 个测试**（`backend/tests/test_check_terminology.py`），其中三条是
  防滥用的关键断言：豁免只作用于本行、豁免标记数量受限、
  **本测试文件自己必须过闸门**（禁用术语样本全部拼接生成而非字面量 ——
  写字面量会让测试文件自己被拦下，实测踩过）
- 把 `--all` 的结果固化成测试，等于让 CI 也守住这道闸门
  （此前只有 pre-commit 守，`git commit --no-verify` 就绕过了）

**现在**：`check_terminology.py --all` 退出码 0。

### Fixed — 发现并部分修复文档编码损坏（2026-08-21）

**发现的问题**：文档存在**三种**编码损坏，成因同源（写回文件时没用 UTF-8，
或用 `errors='replace'` 解码后又写回），但可修复性差别很大。

**一型（非法 UTF-8，1116 处）**：每个 3 字节中文字符的第 3 字节被替换成 `?`。
`docs/DATA_SOURCE_STRATEGY.md` 498 处、`docs/OPERATIONS.md` 404 处、
`docs/OBSERVABILITY.md` 214 处。

**二型（整字变 `?`，70 处）**：`docs/API_SPEC.md`。整个中文字符被替换成一个
半角 `?`，结果**仍是合法 UTF-8** —— 一型的检查完全看不见它。
它在 git 历史里潜伏了 **6 个提交**。

**三型（字面 U+FFFD，2 处）**：`docs/SYSTEM_DIRECTION_CHANGE.md` 两个小节标题
的 emoji 被吃掉了。同样是合法 UTF-8，前两型的检查都看不见。
**这一型损失可忽略**（装饰性图标，不影响语义），但它证明了一件事 ——
"查完了"这个判断必须有依据。

这类损坏**静默**：文件照样能打开、git 照常提交，只是内容里多了一堆 `?`。
`DATA_SOURCE_STRATEGY.md` 的**所有历史版本都已损坏**，无法从 git 恢复。

**一型已定 629 处（56.4%）**，全部是可证明的恢复，不含猜测：
- 432 处由 `6823d18` 的干净历史底本序列对齐**精确恢复**
- 27 处由"该前缀在全仓语料里只对应唯一字符"推断
- 170 处由上下文规则推断，并通过**交叉验证**：规则与底本对齐独立推断同一批
  位置，105 处可核对、**冲突 0**

**箭头规则已按实测收紧，进度因此从 646 退到 629 —— 这是刻意的。**
原规则"前缀 e286 一律填 `→`"在 5 个文档上是 72/72 = 100%，扩到 140 个文档后
只有 **92.34%**（916/992），平均每 13 处写错 1 个字。
e286 前缀的真实分布：`→` 916、`←` 26、`↓` 22、`↔` 15、`↑` 13。

关键观察：**全仓统计不可靠，但同一份文档内部用法高度一致** ——
62 个含箭头的文档里 56 个（90.3%）只用 `→`。所以判据从"全仓统计"换成
"**本文档证据**（含其 git 底本）全为 `→`" + "整行只有箭头则弃权（那是架构图
纵向连接符）"。留一法实测（逐个隐藏一个箭头，只用同文档其余箭头判断）：
**582/582 = 100%**，代价是弃权 410 处。

顺带一个有价值的负面结果：两种"看起来更聪明"的收紧 —— 限缩到非缩进行
（93.14%）、限缩到左右均非空白（94.92%）—— 几乎没用。
**换判据的维度**（从"位置像不像"改成"这份文档怎么用箭头"）才有质变。

实际影响：`DATA_SOURCE_STRATEGY.md` 无底本且存活箭头 0 个，17 处箭头全部退回
人工，待定数 470 → **487**。宁可多留 17 个显眼的占位符，也不写 1 个看似通顺
的错字。

**刻意未修的一型 487 处**：这些位置的前缀对应多个候选字符，无法证明是哪一个。
实测 `efbc` 前缀的 4 个全角标点各占 26/25/20/19% —— 按频率猜的错误率接近 3/4。
把猜测写进文档比留占位符更糟：读者无法分辨哪句是原文、哪句是机器编的。
已导出为受约束选择题清单（每条给候选集 + 前后 40 字上下文）。

**二型 70 处只检测不修复**，理由是实测发现它**连"1 字符换 1 字符"都不成立**：
逐行对齐底本后，24 处可核对位置里恰好 1 个字符的是 **0 处**，多于 1 个的是
**24 处**（例如一个 `?` 吃掉了 `求/` 两个字符）。既没有候选集约束、也没有
长度约束，任何自动修复都无法用逐字节校验证明没越界。

**三型 2 处只登记不修复**：没有干净底本（最早版本就已损坏），从上下文只能
猜是哪个 emoji。补一个合适的图标属于**内容编辑**而非数据恢复，不该由修复
脚本代劳。

**新增三个工具 + 56 个测试**：
- `scripts/check_encoding.py` — 检测**三型**损坏，**已挂 pre-commit 钩子**防复发
- `scripts/repair_utf8_docs.py` — 一型四轮修复 + 选择题导出/合并
- `scripts/verify_utf8_repair.py` — 机械校验：未损坏正文必须逐字节一致，
  每处修复必须落在候选集内。这条约束结构上排除了"顺手改写句子"的修复方式。
  实测四种输入（正确修复 / 改一个字 / 插改写标记 / 删 300 字）：
  只有第一种通过，其余全部拦下
- `backend/tests/test_utf8_repair_tools.py`（32 个）— 含上述交叉验证，
  以及箭头收紧后的 8 条（默认弃权、混用多向的文档不填、独行箭头不填、
  底本也算证据）
- `backend/tests/test_encoding_mojibake.py`（24 个）— 二型 + 三型检测，
  重点防误报；含"全仓不得出现未登记损坏"这两条把钩子效果固化下来的断言，
  以及一条断言三型判据与二型互不重叠

**三型是怎么被发现的**：写完二型检测后我主动追问"还有没有第三种形态"，
换判据（字面 U+FFFD、中文后连续 `??`、全仓非 md 文本）重扫才找到。
教训写进了 `docs/ENCODING_REPAIR.md` —— **检测判据的盲区就是损坏的藏身处**。
同一份文档 §6 也如实写了"没有再找第四种形态，但没找到不等于不存在"。

**过程中更正了自己的一个结论**：上下文规则最初只在 5 个文档上量，四条都是
100%，看起来很稳。扩到全仓 140 个既有文档复测后发现箭头规则只有 **92.2%**、
括号 99.6%、句号 99.4% —— **小样本给了虚假的安全感**。
箭头那条本轮已换判据收紧到留一法 100%（见上）；括号与句号留着（错误率 0.5%
量级、被交叉验证兜住），但已在 `docs/ENCODING_REPAIR.md` §6 明确登记它们
**不算"已证明"**，若要机械处理剩下的 487 处必须先用同样方式重测。

详见 `docs/ENCODING_REPAIR.md`。

### Security — 锁定依赖版本，构建可复现（2026-08-21）

- **`backend/requirements.txt` 全部改为精确 `==`**（此前 25 行全是浮动 `>=`）。
  浮动约束意味着每次 `docker build` 拉到的可能是不同版本，本地测过的组合与线上
  跑的不是一回事，出问题无法复现。锁定值逐包与**本地跑通 2500 个测试的环境**
  核对一致（Python 3.11.9）
- **拆出 `requirements-dev.txt`**：pytest / pytest-asyncio / pytest-cov / respx /
  Faker / ruff / mypy 各锁精确版本，且**不进生产镜像**
- **CI 三处散装安装改为装锁定文件**：`pip install ruff`、`pip install pytest
  pytest-cov`、`pip install mypy` 全部替换。散装等于不锁版本 —— ruff/mypy 小版本
  间检查结果会变，这正是"本地绿 CI 红"的常见来源
- **`pip-audit` 现在能给出确定结论**：审计对象从浮动约束变为精确版本，
  同时覆盖 dev 依赖
- Dockerfile 的 `COPY backend/requirements*.txt` 收窄为只复制运行时依赖，
  并注明启用链路追踪时需要额外装什么

**发现的既有问题（顺带查清）**：那 7 个 `opentelemetry-*` 包被声明为必需依赖，
但**本地从未安装**，也**没有任何针对性测试**（`pytest -k "otel or tracing"` →
2504 deselected，0 个用例）。实测 `OTEL_ENABLED=true` 且缺包时应用正常启动、
`/health` 200，仅打一条 `tracing.unavailable` 警告——说明它们对主流程并非必需。
已移入可选的 `requirements-otel.txt`。

**后续补救**：既然它成了可选依赖，"缺包时能降级"就从隐含假设变成了**契约**，
必须有测试守。新增 `backend/tests/test_tracing_degraded.py`（18 个），
覆盖 no-op span 的调用契约、以及最关键的一条 ——
**运维在生产打开了 `OTEL_ENABLED` 但镜像没装 OTel 包**时，
必须记 warning 继续跑、不能让应用启动失败。
`app/tracing.py` 覆盖率从 **44% 升到 58%**（实测；剩下 42% 需真装 OTel 才能走到）。
同时把两处 `# pragma: no cover - deps always installed` 注释改掉 ——
那句话是错的（依赖并非总是安装），现在指向 `requirements-otel.txt`。

**刻意未锁的一项**：`requirements-otel.txt` 保留 `>=` 区间。本次作业时本机
**无法访问 PyPI**（实测连接被关闭），无法确认这些包的可用版本；凭记忆写死一个
版本号会让人以为"已锁定已验证"，实际是未经证实的猜测，比不锁更危险。首次真正
启用追踪时应实测通过后回填。

**验证方式**：新建干净 venv → 仅装 `requirements.txt` → 实测 41 个包安装成功、
应用启动、`/health` 200、`/metrics` 200；追加 `requirements-dev.txt` 后跑
22 个测试通过；临时环境已清理，本机 venv 未受污染。

### Changed — 统一用户归属过滤口径（2026-08-21）

- **新增 `app/services/user_scope.py`，收敛「查某用户的记录」这一判断**。
  根因是两张表写入 `user_id` 的方式**不一致**（实测确认，非推测）：
  `POST /interactions` 直接落 `body.user_id`，不传即 **NULL**；
  `POST /watchlist/{id}` 走 `body.user_id or "default"`，不传落 **'default'**。
  因此没有单一正确写法——只按 `user_id = 'default'` 查会漏掉 interactions 里
  那批 NULL（表现为用户刚标记「已做」的项目仍反复出现在今日行动里），
  而无条件加 `OR user_id IS NULL` 又会在多用户启用后把归属未标注的数据算进
  每个用户名下。现在：查默认用户时纳入 NULL，查具名用户时严格匹配、不读 NULL
- **`GET /feedback/pending-review` 补上用户过滤**：此前完全没有用户条件，
  与 `/action-queue` 口径不一致。多用户启用后会把别人标过的项目从你的待标
  清单里剔掉、也会把别人的交互记录当成你的
- 新增 `tests/test_user_scope.py`（8 个）锁死语义，含多用户隔离与表名白名单

### Performance — 核实 action-queue 无需缓存（2026-08-21）

上一轮我把「`action-queue` 无缓存」列为风险，**实测后确认该判断有误**：
候选池固定 60，耗时与库内项目总数**无关**。288 项目库下端到端中位数 **26ms**
（`/dashboard/overview` 为 46ms），纯聚合计算约 0.04ms/项目、线性增长。
因此刻意**不加缓存**——缓存会引入失效时机问题（标记「已做」后必须立即从清单
消失），收益却只有几毫秒。已补两个测试锁死：考察项目数 ≤ 候选池上限、
标记后下次请求立即排除该项目。

### Fixed — 独立评审发现的问题（2026-08-21）

- **修复 `--brand` CSS 变量从未定义，导致交互色静默失效**：`globals.css` 里有 10 处
  `rgb(var(--brand))`（其中 5 处为既有代码），但该变量在 `:root` / `.dark` 中
  **从未定义** —— Tailwind 的 `brand` token 只作用于工具类（`bg-brand`），不产生
  CSS 自定义属性。解析为 `rgb(undefined)` 后颜色被静默丢弃：结果按钮的选中态、
  校准进度条填充、行 hover 边框全都不显示。现按 `--accent` 同值补上定义，
  顺带修好 5 处既有失效样式
- **修复结果复盘页跨批次部分失败会重复提交**：勾选超过 50 条时分批发送，若第 2 批
  失败，第 1 批已成功却仍留在待提交集合里，用户重试会把成功过的项目重复写一遍。
  现在失败时把已成功的批次从选择集中移除，并刷新列表反映真实已写入的部分。
  界面文案也改为如实说明「批次内原子、跨批次不保证」，不再笼统宣称「不会部分成功」
- **`ActionQueue` 不再静默吞掉标记失败**：错误此前只经可选的 `onDone` 回调上报，
  调用方不传时点击失败毫无反馈。现增加组件内 `role="alert"` 错误提示兜底
- 结果按钮补 `aria-pressed` 与描述性 `aria-label`（此前选中态只靠 `data-active`
  传达，读屏软件无法感知）

### Fixed — 参与清单信号读取（2026-08-21）

- **修复参与清单的信号全部读不到、任务退化为通用套话**：扩展信号（`has_testnet` /
  `has_task_portal` / `explicit_airdrop_mention` 等）存储在 `projects.meta.signals`
  里，而 `projects` 表**没有**这些列；`generate_participation_tasks()` 直接读顶层
  键，于是 281 个项目的信号判断**恒为 False**，全部落到「无信号」兜底分支。
  实测最高分项目（83 分）也只拿到 5 条与项目无关的通用任务。
  现新增 `signals_view()` 把 `meta.signals` 展平后再传入，实测同样 6 个项目
  产出 11 种不同任务组合、单项目任务数从恒定 5 条变为 5~15 条
  - 顶层值优先于 meta（已迁移到真实列的字段不被旧快照覆盖）
  - meta 里显式的 `False` / `0` 视为有效观测，不当作缺失
  - 原有单测传的是**扁平** dict，因此这个 bug 长期潜伏且测试全绿；
    已补按真实存储形态断言的回归用例
- **修复 `/feedback/pending-review` 被动态路由吞掉**：FastAPI 按声明顺序匹配，
  `/feedback/{project_id}` 在前会把 `pending-review` 当成一个 project_id，返回
  `{"project_id":"pending-review","count":0,"items":[]}` —— HTTP 200 但内容全空。
  已调整声明顺序并补回归测试

### Security — 上线复核 P0 修复（2026-08-20）

- **修复 `/api/v1/settings/config` 明文泄露 LLM API Key**（严重）：该端点直接返回 `settings.llm_providers`，其中含 `api_key` 原文。配合公开的 `POST /api/v1/auth/anonymous`（任何人可领匿名 token），构成**零凭证窃取 OpenAI/DeepSeek 密钥**的完整链路（已实测复现）。现改为只返回 `has_api_key` 布尔值，与 `/llm/status` 的脱敏口径一致
- **`/api/v1/settings` 收入管理员权限**（`ADMIN_ONLY_PREFIXES`）：运行时配置快照含 CORS 白名单、DB 后端、全部阈值与 cron，属运维信息，不应对匿名角色开放。修复后匿名 token 访问返回 403，管理员 200
- **移除 `NEXT_PUBLIC_API_KEY` 客户端兜底**（`frontend-next/lib/api.ts`）：`NEXT_PUBLIC_*` 会被内联进浏览器 bundle，任何访客都能在 DevTools 读到管理员密钥。鉴权统一由服务端 `proxy.ts` 注入，密钥不出服务端
- **生产环境 CORS 增加 localhost 校验**：`CORS_ORIGINS` 含 `localhost`/`127.0.0.1` 时拒绝启动，避免生产忘配导致真实前端域名被全部挡掉（表现为"上线后所有接口跨域失败"）

### Fixed — 上线复核 P0/P1 修复（2026-08-20）

- **修复容器按官方文档启动必然 CrashLoop**（阻断）：`docker-compose.yml` 的 `environment:` 白名单未透传 `AUTH_TOKEN_SECRET`，而镜像内无 `.env`（被 `.dockerignore` 排除）、也没有 `env_file:`；`APP_ENV` 默认为 `production` 时生产自检强制要求该值 → `docker compose up -d --build` 100% 起不来。现补 `env_file: [.env]`，与 `docker-compose.prod.yml` 对齐。已用真实容器验证：修复前拒绝启动，修复后 `Up (healthy)` 且 `/health` 返回 healthy
- **修复两层缓存 TTL 边界判定**（`app/utils/fetcher.py`）：内存与磁盘层都用 `time.time() - ts > ttl`，`ttl=0`（语义为"不缓存"）时因 `0 > 0` 为 False 而返回本该过期的数据。Windows 时钟分辨率约 15.6ms，实测 20 次里 14 次命中脏数据。改为 `>=`
- **移除 `/collections` 整页虚构数据**：原页面零 API 调用，展示 9 个不存在的项目（Nova Protocol / Poly Oracle 等）配虚构评分与「官方确认 Q3 空投」类假情报，对空投决策系统属误导性金融信息。现接入既有但从未被前端使用的 `GET/DELETE /api/v1/watchlist`，取消收藏为真实写入
- **移除 `/archive` 整页虚构数据**：原页面展示虚构归档记录、「命中率 99.2%」、「已归档 38.6 GB」，且所有开关为 `onChange={() => {}}`。现只展示 `/settings/config` 里真实的保留期配置，并明确标注「暂无运行历史接口」
- **移除 `/settings` 假保存按钮**：`handleSave` 只弹「配置已保存」toast 而不写任何东西，旁边却写着「修改将在保存后写入 .env 并热加载」，自相矛盾。整页改为明确的只读快照（标题标注「只读」，输入框改文本展示，开关 `disabled`），删除保存按钮
- **移除 `/ops` 假调度块与假配额**：`SCHEDULER_JOBS` 写死 4 个后端根本不存在的 job 及其「成功 · 182 条」等执行结果，开关空操作、「立即执行」无 onClick；`SOURCE_QUOTAS` 写死各源配额用量。现改为展示 `/settings/config` 的真实调度配置，配额改用后端真实的 `api_calls_today`
- **移除项目详情页恒为「排名第 1」**：`const rank = 1` 写死，任何项目都显示第 1 名
- **采集源缺凭证时启动告警**：`GITHUB_ENABLED=true` 但 `GITHUB_TOKEN` 为空时，GitHub 源静默不跑（`is_enabled()` 返回 False），而 execution 维度占 13% 权重会永久缺失。现在启动日志显式 warning
- **`dashboard.py` 影子块异常不再静默 `pass`**：改记 debug 日志，避免真实 SQL/schema 故障被吞掉、面板恒显 0 而无从排查

### Changed — 工程门禁与配置一致性（2026-08-20）

- **CI 三道门修复至全绿**：`ruff check` 由 99 errors → 0（62 项自动修复，其余逐条判断）；`ruff format` 由 31 文件待重排 → 全部合规；`mypy app` 由 7 errors → 0
- **`sqlite3.Row` 的 SIM118 加豁免**：`"col" in row.keys()` 是列存在性检查的唯一正确写法（`in row` 检查的是**值**），照 ruff 建议改会让所有可选列静默变 None——已在 `pyproject.toml` 注明原因
- **统一两份 `pyproject.toml` 的口径**：`backend/pyproject.toml` 的 version 1.0.0→0.1.0、requires-python >=3.10→>=3.11、mypy python_version 3.13→3.12（对齐 CI 与 Dockerfile 的 3.12），并补上此前缺失的 `--cov-fail-under=80`（pytest 从 `backend/` 运行时用的正是这份配置，等于本地跑测试完全不校验覆盖率）
- **`.env.example` 补关键提示**：`AUTH_TOKEN_SECRET` 标注生产必填（为空则容器 CrashLoop）+ 生成命令；`CORS_ORIGINS` 标注生产必须改真实域名

### Added — Portfolio/Settings 真实化 + middleware 迁移（2026-07-26）

- **Portfolio 页接入真实 API**：去掉全部 mock 数据，改为读取 `GET /interactions/summary` + `GET /interactions`；KPI、校准矩阵、分布、记录表全部真实数据
- **Settings 页接入真实配置**：新增 `GET /api/v1/settings/config` 只读端点，返回运行时配置快照（密钥只返回布尔值）；前端 Settings 页从硬编码默认值改为回填真实运行时值
- **Next.js middleware → proxy 迁移**：`middleware.ts` 重命名为 `proxy.ts`（Next.js 16 约定），消除 deprecation warning

### Added — 通知中心增强：评分变化 + 已读持久化（2026-07-26）

- **评分变化通知**：从 `project_history` 对比同项目最新两条快照，生成 `score` 类型通知（升/降/标签变化）
- **已读状态持久化**：新增表 `notification_reads` + `POST /api/v1/notifications/read`（支持 ids / all）；刷新后已读状态保留
- **前端通知中心**：点击单条 / 「全部已读」会调用后端持久化；并修正 `apiFetch` 解包 `data` 后的字段读取

### Added — 通知中心真实化（2026-07-26）

- **新增 `GET /api/v1/notifications` 聚合端点**：返回今日新 FARM/WATCH 机会 + 采集器失败告警
- **通知中心页接入真实数据**：去掉写死 mock，改为读取上述端点；空库时显示空状态而不是假项目

### Added — Dashboard 今日流水线真实化（2026-07-26）

- **新增 `GET /api/v1/dashboard/overview` 聚合端点**：一次返回今日采集运行数、今日新增项目数、发现队列待处理数、影子引擎今日评估数
- **Dashboard「今日流水线」卡片接入真实数据**：原先写死的 `sampled 3 / saved 3 / 待处理 12` 等假数据，改为读取真实采集运行、发现队列与影子评估计数

### Added — 上线审核 P1 项（GO_LIVE_AUDIT_REPORT，2026-07-26）

- **所有 API 响应携带 `X-Disclaimer` 响应头**（`Not investment advice...`，SECURITY.md §7.5 合规要求）
- **新增 HTTP 请求计数指标** `airdrop_http_requests_total`（按方法 + 状态码分档）
- **告警规则补充**：`HighAPIErrorRate`（5xx > 0.1/s，critical）+ `PipelineConsecutiveFailures`（15 分钟 ≥ 2 次失败，critical）；`airdrop_pipeline_runs_total` 新增 `status` 标签（started/completed/failed）
- **生产自检新增 AUTH_TOKEN_SECRET 校验**：生产环境该值为空会拒绝启动（否则匿名 token 每次重启后失效）

### Fixed — 上线审核阻断项修复（GO_LIVE_AUDIT_REPORT，2026-07-26）

- **删除冲突的 `backend/Dockerfile`**：它与正确的 `docker/Dockerfile` 并存且被 `docker-compose.yml` 默认引用，但其 Python 3.14 + 不完整的 COPY 会让 `docker compose up --build` 构建失败；统一引用 `docker/Dockerfile`
- **统一 Python 版本到 3.12**：此前 CI 用 3.13、Dockerfile 用 3.11、本地为 3.14，三处不一致可能引入「CI 通过但生产失败」的幽灵问题；现 CI（ci.yml / security.yml）和 docker/Dockerfile 统一为 3.12

### Fixed — 系统审查（采集链路 / 流水线 / 安全 / 前端，2026-07-26）

详见 `SYSTEM_AUDIT_REPORT.md`。真实库（702 项目 / 1040 原始记录）实测：7 项可验证信号里 6 项命中率为 0%。

**采集链路**
- **DefiLlama 补齐字段映射**：`description`（文本判断的唯一来源，缺失导致 `has_docs`/`has_roadmap`/`explicit_airdrop_mention` 全语料恒为 False）、`tvl_usd`、`has_twitter`/`has_github`
- **跨源合并首次可发生**：galxe/layer3/etherscan/twitter/coingecko 不再臆造赛道（写死 `Quest`/`On-chain`/`Unknown`/`DeFi` 会让 dedup_key 与真实赛道永不相撞，真实库 `source_count>=2` 恒为 0%）；新增"赛道未知分组并入同名已知分组"（仅唯一匹配时）
- **信号补充源不再被阈值挡在门外**：coingecko(0.1)/etherscan/cryptorank(≤0.28) 低于分析阈值 0.3，此前从不进入合并；现在同 dedup_key 已有记录过线时，低分佐证一并载入，且 `limit` 改为约束项目数而非原始行数
- **Twitter 正文参与解析**：推文载荷在 `raw_data["text"]`，此前不在取值范围内，两个 twitter 源贡献恒为零
- **修正阶段与代币推断**：TVL 分档判 testnet 造成 31.8% 项目误标；`_is_unlisted` 改为"真实 ticker > gecko_id"，并把 DefiLlama 的 `"-"` 哨兵值（真实库 658/1040 条）排除在 ticker 之外
- **GitHub 赛道整词匹配**：`"ai" in desc` 会命中 blockchain/chain/mainnet；改存 `pushed_at` 而非会被 star 顶新的 `updated_at`
- **CoinGecko 不再拿币种图标当官网**
- **合并容忍 naive/aware 时间戳混用**：`min()` 抛 TypeError 会中断整批采集

**流水线与调度**
- **持久化失败不再报成功**：状态改到落库之后再定；出队判据从"内存评分成功"改为"确实写进 projects"，此前整批丢失且队列已清空、DB 与 metrics 均无痕迹
- **每次运行落持久记录**（`LogRepository.log_run` 此前定义了却从无调用方）
- **cron 传 timezone**：预构造的 `CronTrigger` 不继承 `scheduler.timezone`，`TIMEZONE` 配置被静默忽略；`misfire_grace_time` 由默认 1 秒改为 1 小时 + `coalesce`

**安全（对照 `docs/SECURITY.md`）**
- **500 响应不再回显异常原文**（psycopg 异常带 DSN 含库密码，httpx 异常带 `?apikey=`）
- **安装 structlog 脱敏 processor**（§3.3 要求但全仓库无 `structlog.configure()`）：按字段名脱敏、递归容器，且排在 traceback 渲染**之后**
- **`APP_ENV` 归一化**：`Production`/`PRODUCTION`/`prod`/`"production "` 此前全部绕过生产安全校验
- **API_KEY 长度下限 32**（§4.2；原实现只校验非空）
- **接入限流**（§4.2/§10.4，三个配置项此前无人读取）：按 IP 滑动窗口 + 429/Retry-After，昂贵端点分档；默认不采信可伪造的 `X-Forwarded-For`，新增 `TRUSTED_PROXY_COUNT`
- **输入长度与取值域上限**：feedback 的 note 实测 20MB 直接落库；funding 的 NaN 会写进 meta 再报 500
- **移除根目录 nginx.conf 的 CORS 通配**：`Access-Control-Allow-Origin: *` + 自动放行预检 + `always`（连 401 都带），且与后端同名头重复导致白名单失效
- `/health` 降级时返回 503（探针按状态码判活）

**前端**
- **采集按钮失效**：后端返回嵌套 `status.enabled`，前端读顶层 `enabled` → 启用列表恒为空；同一错位让 Ops 页全部显示"已禁用"
- **Insights 页 `热度 NaN`**：读错字段名（`heat_score` vs `avg_heat_score`）
- **失败请求不再渲染成空数据成功**；Nav 的接口状态改为三态（检测中/在线/异常），并改探 `/health`
- **项目详情页加代次守卫**：重评后的刷新可能被慢的旧响应覆盖，把分数写回重评之前

### Fixed — 评分决策引擎回归规范（ADR-014，2026-07-26）
- **跨源合并不再丢信号**（`utils/normalize.py`）：原按来源优先级整条择一，落选来源的 23 个信号字段被清空、`source_count` 恒为 1，导致「多发现一个来源分数反而下降」。改为按字段类合并（存在性布尔 OR / 数值 max·min / 列表并集 / 标量取最高可信已知值），并给 `manual`/`api` 显式取值以否决权（唯二能主张否定的来源）；合并结果与输入顺序、与 `PYTHONHASHSEED` 均无关。见 `DATA_SCORING_DICT.md §5.8`
- **Risk Agent 改用 `tokenomics.risk`**（`agents/risk.py`）：原误取 `unlock_penalty`，与 `DATA_SCORING_DICT.md §5.7.2` 不符，方向与模型意图相反（高解锁压力少扣 31.5 分、VC 集中反加 12 分）
- **`airdrop_signal` 子分统一到 `agents/airdrop_signal.py`**：原在 scorer 与 risk 各有一份实现，2304 种信号组合中 666 种结果不一致
- **confidence 去掉 0.55/0.45 人为下限**（`agents/scorer.py`）：四个 Agent 全部成功的正常路径下 confidence 恒 ≥0.55，`confidence < 0.5 强制降档` 只在 Agent 崩溃时生效，而它本意是防「可验证信号不足」
- **`weight_version` 改为从配置读取**；新增 `projects.sub_scores` 列承载子分快照（不复用 `raw_signals`——那一列存的是采集**输入**信号），UPSERT 以 `COALESCE` 写入，评分失败时不覆盖上一次的好快照
- **`TokenomicsResult` 可往返**：`computed_field` + `extra="forbid"` 曾使 `model_dump()` 无法回放，任何从 `tokenomics_json` 重建的路径都会硬失败
- **`tier-1 vc backed` 误判修正**（`agents/team.py`）：`funding_quality <= 0` 时不再打 tier-1 标记
- **融资文本匹配收紧**（`agents/collector.py`）："hourly funding rate"、"raised the block gas limit" 等不再误判为融资事件
- **信号缺失判定修正**（`services/project_signals.py`）：布尔 `False` 与数值 `0` 是有效观测，不再当作缺失，消除单向棘轮

### Fixed — 旁路机会引擎（ADR-014，`opportunity-v2.0`）
- **联合概率区间算法**（`opportunity/probability.py`）：端点由逐分位连乘（`low×low×low`，与 `base` 的独立性假设自相矛盾）改为相对不确定度平方和合成，并以逐分位连乘为地板/天花板，保证新区间恒为旧区间的子集（0.1 网格穷举 2334 万组验证）。原算法使「官方分发 + 积分制资格」档的 `joint.low` 恒为 0.1650，永远跨不过 FARM 门槛 0.20。`base=0` 时端点不再一并归零，避免经 `DUST_REWARD` 误判 IGNORE
- **`TOO_EXPENSIVE` 恢复可达**（`opportunity/decision.py`）：已确知超预算的判定前移到「证据不足」短路之前（仍后于三个 BLOCK 判定），并要求来源等级 ≥ B 且为 observed/derived，避免一条 U 档道听途说把项目钉成 30 天 IGNORE。此前 270 项语料中旧引擎产出 `NOT_FIT` 的数量为 0，用户被告知「去补证据」而真实原因是「太贵了」
- **理由码不再塌缩**（`opportunity/decision.py`）：补齐 `service.evaluate_row` 使用的 8 个 `_usd`/`_hours` 后缀命名，此前一律映射为通用码 `WAIT_MORE_EVIDENCE`
- **证据新鲜度延长衰减尾部**（`opportunity/service.py`）：原 >90 天一律 0.2 且永不再降；现 ≤180 天 0.2、≤365 天 0.1、此后 0.05（只收紧，任何年龄都 ≤ 原值）

### Added
- `backend/app/agents/airdrop_signal.py` — `airdrop_signal` 子分唯一实现
- `backend/scripts/dual_run_compare.py` — 新旧引擎双跑对比（`dump`/`diff` 主引擎，`dump-opp`/`diff-opp` 旁路引擎）
- `backend/tests/test_review_regressions.py` — 74 条回归测试（每条对应一处已确认并修复的缺陷）
- `backend/app/rate_limit.py` — 按 IP 限流中间件
- `backend/scripts/backfill_meta_signals.py` — 从 raw_projects / project_signals 回填历史行的 meta.signals
- `SYSTEM_AUDIT_REPORT.md`
- `docs/adr/ADR-014-engine-spec-conformance.md`
- 工程基础设施完整搭建（P0/P1 全部完成）
- `pyproject.toml` — 项目元数据 + ruff/mypy/pytest 配置
- `.env.example` — 全量环境变量模板
- `.gitignore` — 完整的忽略规则
- `.editorconfig` — 跨编辑器格式统一
- `Makefile` — 开发常用命令
- `backend/app/` — FastAPI 应用骨架（config/db/main/models）
- `agents/` — 15 个详细 Agent 定义文件（Planner/Architect/Backend/Researcher/Frontend/Database/DevOps/Prompt/Reviewer/Security/Performance/Tester/Release/Documentation/Knowledge）
- `skills/` — 21 个实际 Skill 模板（backend/frontend/database/security/performance/deployment/documentation/api/llm/prompt/evaluation/debug/refactor/review/architecture）
- `prompts/` — 5 个 Prompt 模板文件（Narrative/Team/Risk/Tokenomics/Orchestrator）
- `knowledge/` — 业务和技术知识文件（business/technical/api/external/decisions）
- `configs/` — 分环境配置文件（dev/staging/prod）+ Feature Flags
- `tests/` — 可运行测试骨架（unit/contracts/golden/api，22 passed）
- `docs/00_index.md` — 00–15 编号体系文档索引
- `.github/workflows/docs.yml` — 文档链接校验 CI

---

## [0.1.0] - 2026-07-08

### Added
- 完整设计文档体系（20+ 份文档）
- 11 份 ADR（ADR-001 ~ ADR-011）
- 编码规范（`CONVENTIONS.md`，17 节）
- API 规范（`docs/API_SPEC.md`）
- 评分数据字典（`docs/DATA_SCORING_DICT.md`）
- 数据库 DDL（`docs/DATABASE_DDL.md`）
- 前端规范（`docs/FRONTEND_SPEC.md`）
- 用户故事（`docs/USER_STORIES.md`）
- 任务分解（`docs/TASK_BREAKDOWN.md`）
- 部署文档（`docs/DEPLOYMENT.md`）
- 可观测性设计（`docs/OBSERVABILITY.md`）
- 安全规范（`docs/SECURITY.md`）
- 数据质量框架（`docs/DATA_QUALITY.md`）
- 运维手册（`docs/OPERATIONS.md`）
- 性能基准（`docs/PERFORMANCE_BENCHMARK.md`）
- Golden 测试用例（`docs/GOLDEN_TEST_CASES.md`）
- 设计令牌（`docs/DESIGN_TOKENS.md`）
- 术语表（`docs/GLOSSARY.md`）
- Agent 系统（`agents/README.md`）
- Skills 系统（`skills/README.md`）
- Prompt 管理（`prompts/README.md`）
- 知识库（`knowledge/README.md`）
- CI/CD 流水线（ci.yml / security.yml / release.yml）
- PR 模板 + Issue 模板
- 测试骨架（`tests/` + `conftest.py`）
- Docker 配置（Dockerfile + nginx）
- AI 开发工作流（`docs/AI_DEV_WORKFLOW.md`）
- 项目启动检查清单（`docs/PROJECT_BOOTSTRAP_CHECKLIST.md`）

---

## [0.0.1] - 2026-07-07

### Added
- 项目初始化
- README.md 基础结构
- 基础目录结构

---

[Unreleased]: https://github.com/web3-airdrop-alpha/web3-airdrop-alpha-agent/compare/v0.1.0...HEAD
[0.1.0]: https://github.com/web3-airdrop-alpha/web3-airdrop-alpha-agent/compare/v0.0.1...v0.1.0
[0.0.1]: https://github.com/web3-airdrop-alpha/web3-airdrop-alpha-agent/releases/tag/v0.0.1
