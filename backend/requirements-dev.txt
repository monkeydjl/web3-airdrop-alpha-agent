# ──────────────────────────────────────────────────────────────────────────
# 开发/测试依赖 —— 版本已锁定
#
# 与 requirements.txt 分开的原因：这些包不该进生产镜像（docker/Dockerfile
# 只装 requirements.txt）。CI 里的 pytest/mypy 步骤请装这个文件，
# 而不是散装 `pip install pytest pytest-cov` —— 散装等于不锁版本，
# 本地绿、CI 红这类问题就是这么来的。
#
# 用法：
#   pip install -r requirements.txt -r requirements-dev.txt
#
# 以下版本是本地实测通过 2500 个测试的确切组合（Python 3.11.9，2026-08-21）。
# ──────────────────────────────────────────────────────────────────────────

# ── 测试框架 ──
pytest==9.1.1
pytest-asyncio==1.4.0
pytest-cov==7.1.0

# ── 测试替身 / 造数据 ──
respx==0.23.1
Faker==40.36.0

# ── 静态检查（CI 三门用到；本地也建议装同版本，避免"本地绿 CI 红"）──
ruff==0.16.1
mypy==2.3.0
