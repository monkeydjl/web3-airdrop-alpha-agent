# 性能优化报告 · Web3 Airdrop Alpha Agent System

日期：2026-07-26 ｜ 范围：事件循环、数据库热路径、采集器网络层、前端数据获取

本轮在上一次代码审查（见 `CODE_REVIEW_REPORT.md`）之后进行，聚焦性能而非缺陷修复。所有结论均由可复现的基准脚本量化，脚本已随代码提交（`backend/scripts/bench_event_loop.py`、`backend/scripts/bench_db.py`），可自行复跑。

## 实测结果

事件循环阻塞（导出 2000 个项目为 Excel 期间，并发探测 `/health` 延迟）：

| 指标 | 优化前 | 优化后 | 变化 |
|---|---|---|---|
| `/health` 最大延迟 | 1100.9 ms | 92.2 ms | **12× 改善** |
| 同窗口内完成的 `/health` 请求数 | 7 | 39 | **5.5× 吞吐** |

优化前 `/health` 的最大延迟 1100.9 ms 与重请求耗时 1099.3 ms 几乎相等——这说明健康检查在整个导出期间被完全挂起，而非只是变慢。优化后 p50 从 2.8 ms 升到 13.4 ms 属预期且正确：轻请求现在与导出真实竞争 CPU，而不再是被彻底饿死后偶尔挤进一两个。

数据库热路径（1000 条规模）：

| 路径 | 优化前 | 优化后 | 变化 |
|---|---|---|---|
| `save_batch(1000)` | 6239.9 ms | 1583.8 ms | **3.9×** |
| `persist_collection_result(1000)` | 244.9 ms | 139.5 ms | **1.8×** |
| `/insights` 聚合 | 9.7 ms | 3.1 ms | **3.1×** |

## 做了什么

**事件循环。** 项目里 24 个路由处理器声明为 `async def`，却只做同步阻塞的数据库与 CPU 工作（`sqlite3`/`psycopg` 都是同步驱动）。在 asyncio 下这意味着单个慢请求会独占事件循环线程，让整个进程对所有其他请求无响应——包括健康检查，因此负载均衡器会在真正的高负载时把健康实例摘掉。改为普通 `def` 后 FastAPI 自动将其调度到线程池。判定哪些可以转换用 AST 扫描完成（检查函数体内是否存在 `await`/`async with`/`async for`），真正需要异步的处理器（`/run`、`/collections/{id}/trigger`、`/ai-brief`、文件上传）保持不变。`import_projects` 里的 pandas/openpyxl 解析改用 `asyncio.to_thread` 卸载。

由于线程池让 SQLite 写并发从"被事件循环串行化"变成真实并发，同步增加了显式 `busy_timeout`（新配置项 `SQLITE_BUSY_TIMEOUT_SECONDS`，默认 10 秒），让写锁竞争进入等待而不是直接抛 `database is locked`。

**数据库。** `save_batch_with_rows` 此前对每个项目单独建连接、跑 WAL pragma、再关闭，500 个项目就是 500 轮建连开销；改为整批复用一条连接，逐条提交的失败隔离语义保持不变。`persist_collection_result` 此前对每条发现执行一次 `SELECT` 查重（一轮 DefiLlama 采集 = 100+ 次往返），改为按 source 分组、对 dedup_key 分块的批量查询加内存记账，批内同 key 的重复计数行为与原实现逐位一致（已用回归测试锁定）。`/insights` 此前用 `SELECT *` 拉最多 1 万行——把 `raw_signals`、`meta`、四个 agent 的 JSON 全搬进 Python，只为读其中两个字段；改为分组计数下推到 SQL，行数据只取必需的窄投影。

**采集器网络层。** 最重要的是一处正确性问题：API 路由此前每次请求都新建一整套采集器实例，而令牌桶限流器是**实例状态**——等于每次调用手动触发端点都拿到一个满桶，出站限流可被反复调用完全绕过（进而可能让上游把你的 IP 拉黑）。新增 `app/collectors/factory.py` 提供进程级共享注册表，调度器与 API 路由共用同一批实例，限流这才真正生效。另外修复 CryptoRank 的限流器位置错误（此前整轮分页最多 4 次请求只消耗 1 个令牌），并让 Twitter 的 KOL 分批与关键词查询共用一个 HTTP 客户端复用连接；顺带修正"单批失败即丢弃全部已取回推文"的行为——现在单批失败只丢该批，但全部失败仍如实上报 error。

**前端。** 新增 `lib/useAsyncData.ts`：每次请求带 `AbortController`，并用代次守卫丢弃过期响应。此前所有页面手写的 `load()` 都没有取消机制，慢的旧响应后到会覆盖新响应——洞察页的刷新按钮甚至没有 `disabled` 守卫，连点三次就能复现图表闪烁并最终停在最慢那次的结果上。同时修复分页：此前硬编码 `?page_size=500` 且从不读取响应里的 `total`，项目数一旦超过 500，列表、统计卡、赛道分布与筛选下拉全部基于被截断的切片计算且界面无任何提示；新增 `lib/projects.ts` 按 `total` 分页取全，触及上限时在界面显式提示。另外修复 toast 定时器泄漏（前一条的定时器会提前清掉后一条，且卸载后仍触发 setState）。

顺带修复了一处审查发现但上轮未处理的缺陷：`GET /export/project/{id}` 因把中文"详情"直接内插进 `Content-Disposition` 而必然抛 `UnicodeEncodeError`（Starlette 以 latin-1 编码响应头），该端点从未成功过；改为 RFC 6266/5987 合规的 ASCII 回退名加百分号编码。同时为上传补上文档中声称却从未实现的 10MB 限制（此前任意大小文件会被整个读进内存）。

## 验证

后端 **1832 项测试全部通过**（本轮新增 8 个优化回归测试），`ruff` 零告警。新增的回归守卫包括：AST 扫描确保不再出现"async def 但无 await"的路由、`trigger_collection` 必须保持异步（共享限流器持有 `asyncio.Lock`，若改同步再 `asyncio.run` 会跨事件循环报错）、注册表实例共享性、批内去重计数一致性、批量保存只建一次连接。

四项改动均经独立对抗式复核，重点核验了：转换后的处理器无残留异步构造且未被 `Depends` 引用；批量去重在"同批同 key"与"跨批同 key"下计数与旧实现完全一致，且 dedup_key 含 SQL 元字符时全程参数化；共享连接在异常路径上仍恰好关闭一次，且注入连接时不会误关调用方的连接；`BEGIN IMMEDIATE` 在同一连接上反复执行不会触发嵌套事务错误（已实测四轮含失败回滚的序列）。

前端改动全部通过 TypeScript 严格模式类型检查（`lib` 层唯一报错是 `process` 需要 `@types/node`，你的项目已具备）。**建议本地跑一次 `npm run build` 复核**——云端沙箱无法访问 npm registry，我无法在这里完成完整构建。

## 已知取舍

`get_default_registry()` 缓存的采集器实例在构造时读取部分配置（如 API key），因此运行时修改这些配置需调用 `reset_default_registry()` 才生效。当前代码从不在运行时修改 `settings`，测试也都直接构造采集器而非走注册表，故无实际影响；该重置函数作为逃生舱保留并已被测试覆盖。

Postgres 下批量去重把"查找到插入"的窗口从每条微秒级放宽到整批——仅当手动触发与定时任务同时采集同一数据源时才会触发，且由于 `raw_projects` 没有唯一索引，旧实现在同样情形下也会产生重复行（不会报错）。彻底解决需要加 `(source_id, dedup_key)` 唯一索引，但对已存在重复数据的库这会让 `init_db` 启动失败，故本轮未做，留作需要时的独立迁移。
