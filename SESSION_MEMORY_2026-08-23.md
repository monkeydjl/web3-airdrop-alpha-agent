# 2026-08-23

## 本次做了什么

一句话：**修掉一个"设了但不生效"的日志开关，并把一份 90% 内容失真的
可观测性文档按实测重写，同时给它加上双向门禁。**

这一轮是上一轮"编码损坏修复"的延续。`docs/OBSERVABILITY.md` 挂在编码损坏
登记表上（214 处中文字被截断成非法字节），我本来只打算修字节；结果一读内容，
发现字节问题只是表层。

---

## 一、`LOG_LEVEL` 从来没有对应用日志起作用

### 现象

`LOG_LEVEL=WARNING` 设了没用。我把它设成 WARNING，然后在子进程里真的打了
四条不同级别的日志 —— 四条**全都输出了**。

### 原因

这个变量只被传给了 uvicorn（Web 服务器自己的访问日志）。应用代码里
287 处日志调用走的是 structlog，而 structlog 的配置里**没有装级别过滤器**，
所以它一条都不过滤。

### 为什么这比"没有这个开关"更糟

有开关但不生效，运维会以为噪音已经压掉了，于是不再关注日志量。
而磁盘和日志后端照旧在被灌满，真正要看的 error 淹在 debug 里 ——
问题被"我已经处理过了"的错觉掩盖住。

一个不生效的开关，本质上和一个假的仪表盘是同一类问题：
它不是缺功能，它是在**主动误导**。

### 修法与验证

装上级别过滤器。然后实测七种取值（每种都开独立子进程，避免
structlog 全局状态串味）：

| 设成 | 实际放出的级别 |
|---|---|
| `DEBUG` | debug / info / warning / error |
| `INFO` | info / warning / error |
| `WARNING` | warning / error |
| `ERROR` | error |
| `warn`（常见别名） | warning / error |
| `BOGUS`（拼错） | info / warning / error |
| 留空 | info / warning / error |

**最后两行是刻意的设计选择**：配置写错时退回 `INFO`，
**不能**退回 `DEBUG`。后者会让"手滑写错一个字"反而把全部调试日志放出来 ——
故障方向正好相反，而且没人会怀疑是自己拼错了变量值。

用变异测试确认这条真的被钉住了：把兜底从 INFO 改成 DEBUG，测试立刻变红；
去掉级别过滤器（回到修复前），变红；去掉 `warn` 别名，变红。三种破坏都被抓住。

---

## 二、可观测性文档：编码损坏只是表层，内容 90% 是虚构的

### 实测出来的数字

| 项 | 文档声称 | 代码实际 |
|---|---|---|
| 监控指标名 | 39 个 | 其中 **35 个根本不存在** |
| 日志事件名 | 15 个 | **一个都不存在** |
| 链路 span 名 | 一整棵树 | 名字**全不相同** |

### 为什么这类假名字特别危险

**Prometheus 查一个不存在的指标不会报错，只返回空结果。**

面板上呈现出来的是一条空曲线、一个「No data」。看起来像"系统很安静"，
而不是"你查的东西不存在"。基于假指标写的告警规则同理 ——
它永远不会触发，于是给人一种"有人盯着"的错觉。

日志查询也一样：按不存在的事件名去搜，得到的是"最近没有这类事件"，
而不是"你搜错了名字"。

**一个永远不报警的告警，比没有告警更危险**，因为它占掉了"这块已经有人管"
的位置。这和上面那个不生效的日志开关是同一种病。

### 重写内容（全部实测）

- 33 个指标的完整目录：名称 / 类型 / 标签 / 直方图分桶，直接从代码的
  Prometheus 注册表导出，不是手抄。
- 真实的日志字段与时间戳格式（是 `2026-08-23 12:21:10` 这种本地时间格式，
  **不是** ISO-8601 带 `Z` 的形式 —— 写日志解析规则会踩这个）。
- 269 个真实事件名的命名空间分布，附最高频的 8 个命名空间与示例。
- 实际的 4 个链路 span 名。
- 已就位的 7 条 Prometheus 告警规则、10 个 Grafana 面板 ——
  逐条核对过引用的指标真实存在（结论：这两份配置文件是干净的，
  失真只在文档里）。
- 采集侧那套写在代码里、走日志不走 Prometheus 的五阈值告警（此前完全没写）。

### 12 项"未实现"集中标注

以前这些设计意图和现状混在一起写，读者分不清哪句描述的是现在、
哪句是计划。现在全部拎出来单独一节，逐项标注：
日志采样、note 截断、`X-Run-Id` 响应头、LLM 成本/token 指标、
Agent 粒度指标、数据质量指标、采集配额指标、HTTP 耗时直方图、
业务面板、`metrics` 表写入、告警抑制策略。

### 顺带查明的两处"看着像有、实际是空"

**`metrics` 表是死表。** 表结构在、数据访问层在、测试也在调用它 ——
但**生产代码里没有任何写入点**，实测 0 行。留着它会让人以为指标有历史留存，
排查问题时白跑一趟。要么接上，要么删掉，不该维持现状。

**`/metrics` 端点无鉴权。** 无凭据请求实测返回 200。在容器内网里这是常规做法，
但文档此前一个字都没写 —— 部署时容易漏掉网络层限制。现已明确写出，
并指明抓取目标是 compose 内网名。

---

## 三、给文档加了双向门禁（17 项检查）

新增 `backend/tests/test_observability_doc_parity.py`。它做六件事：

1. 文档写的每个指标必须在代码注册表里存在。
2. **反向**：代码里的每个指标也必须写进文档。
3. 文档里那份"代码中不存在的指标"对照清单，必须真的全部不存在。
4. 文档引用的日志事件名必须真实。
5. 文档引用的链路 span 名必须真实（且反向也要覆盖）。
6. 已交付的告警规则与 Grafana 面板不得引用幽灵指标。

**第 2 条为什么必须有**：少写一个指标和写错一个指标一样有害 ——
读者不会去搜索一个他不知道存在的东西。只查"文档里的是否为真"是单向的，
等于只修前门。

**第 3 条为什么必须有**：那份对照清单是为了让照旧文档写过查询的人对上号。
如果清单里混进一个真指标，等于劝人放弃一个可用的指标 ——
清单本身就成了新的谎言。

### 变异测试：六种破坏，六次变红

不做变异测试就不能相信一套新测试。这次跑了六种破坏：

| 破坏 | 结果 |
|---|---|
| 把一个真指标名改成不存在的名字 | 红（2 项失败） |
| 删掉一整行真实指标 | 红（反向断言抓住） |
| 把真指标塞进"不存在"清单 | 红 |
| 编一个日志事件名 | 红 |
| 编一个 span 名 | 红 |
| 改掉小节标题（让拆分锚点失效） | 红（4 项失败） |

**第一版的 span 检查是绿的 —— 这是本轮最值得记的一条教训。**

我第一版把 span 名**写死在测试代码里**（`for span in ("airdrop.pipeline.run", ...)`），
然后断言"这些名字在代码里存在"。变异测试把文档里的 span 名改成假的之后，
测试**照样全绿** —— 因为它检查的是测试自己的常量，跟被测文档毫无关系。

> **一个不读被测对象的断言，等于没有断言。**

改成从文档解析 span 名之后，同一个变异立刻变红。这已经是本项目第四次
遇到"看起来在检查、实际什么都没检查"：
前三次分别是注释让字符串断言假通过、注释让"字符串不存在"断言假失败、
以及一段永不执行的跳过逻辑。共同点都是**断言的对象错了**。

---

## 四、关于一型编码损坏的结论（已写进检查脚本注释）

上一轮说"前 2 字节把候选限制在 64 个以内，修复是做选择题"。
本轮把这道选择题真的做了一遍，结论是：**做不了。**

| 手段 | 能唯一确定多少处 |
|---|---|
| 同文档字符集收敛 | 101 / 1115（9.1%） |
| 结构重复标尺（2/3/4/5 字窗口） | **0 / 1115** |
| git 历史里的干净底本 | **不存在**（旧版本是改写前的草稿，行级唯一匹配 0 条） |

而且损坏不只是"丢第 3 字节"，它还**额外吞掉了相邻一个字节** ——
`OBSERVABILITY.md` 从 293 行塌成 268 行，表格行和列表项被粘连在一起，
连上下文结构本身都被破坏了。

所以修复方式只能是**按实测真相重写**，绝不猜原字。
理由和上一轮一样，只是现在有数字支撑：**一个猜出来的字比一个明显的乱码更坏，
因为读者分不清哪个是原文。**

`docs/OBSERVABILITY.md` 已从登记表移出，不再享有豁免。

顺带记一条：**登记豁免会掩盖内容问题，不只是字节问题。**
那 35 个假指标名能活这么久，正是因为文件挂在"已知损坏"清单上 ——
既然已经登记为坏的，就没人会去逐行读它。上一轮 API_SPEC 的 16 条假端点
是同一个成因。剩下两个还在登记表上的文件（`OPERATIONS.md` 404 处、
`DATA_SOURCE_STRATEGY.md` 498 处）**大概率藏着同类内容问题**，
修的时候要按"重写 + 加门禁"来做，不能只修字节。

---

## 验证结果（全部实跑）

| 检查 | 结果 |
|---|---|
| `pytest`（含覆盖率门禁 ≥80%） | **2730 passed / 5 skipped**，覆盖率 **88.39%**，36m43s |
| `ruff format --check app tests` | 233 files already formatted |
| `ruff check app tests` | All checks passed |
| `mypy app` | no issues in 117 source files |
| `scripts/check_encoding.py` | exit 0（剩 3 个登记文件） |
| `scripts/check_terminology.py --all` | exit 0 |
| 前端 `tsc --noEmit` | exit 0 |
| 前端 `eslint` | exit 0 |
| 前端 `node test.mjs` | 20 pass / 0 fail |
| 新增 parity 套件单跑 | 17 passed |
| 变异测试（文档 6 种 + 日志级别 3 种） | 9/9 按预期变红，文档字节等同恢复 |

---

## 决定与取舍

1. **非法 `LOG_LEVEL` 退回 INFO 而不是 DEBUG** —— 配置写错不该反而放开输出。
2. **文档里保留那份"35 个不存在的指标"清单**，而不是删掉了事 ——
   照旧文档写过查询的人需要一个地方对上号；同时用测试保证这份清单里
   没有真指标混进去。
3. **`metrics` 表暂不动**（不接也不删），只在文档里如实标注它是死表。
   动它属于功能变更，需要所有者决定。
4. **span 检查改为从文档解析**，不写死名字 —— 见上面那条教训。

---

## 下一步

1. 重写 `docs/OPERATIONS.md`（404 处一型损坏 + 预计有同类内容失真）。
2. 重写 `docs/DATA_SOURCE_STRATEGY.md`（498 处）。
3. `docs/SYSTEM_DIRECTION_CHANGE.md` 剩 2 处三型（emoji 变 U+FFFD），量小。

## 遗留问题（需要所有者拍板的）

1. **Python 版本口径不一致**：Docker 镜像与 CI 用 3.12，本地环境是 3.11.9，
   项目声明 `>=3.11`。两个版本都能跑通全套测试，但"以哪个为准"需要定。
2. **`/api/v1/collections` 是否要加进管理员白名单**：目前采集触发接口
   任何拿到匿名 token 的人都能调（会真的跑采集并写库）。上线前建议收紧。
3. **生产环境建议把 `SEED_FALLBACK_ENABLED` 设为 false**（当前 true）——
   否则采集失败时会静默用种子数据兜底，看起来像采集成功了。
4. **`metrics` 表要接上还是删掉**（见上）。
5. **归档功能的真实运行仍未观测到** —— 本地数据只覆盖到 08-15，
   还没等到一次真实的 03:00 归档窗口。
