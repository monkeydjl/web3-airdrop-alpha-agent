# 2026-08-23

## 本次做了什么

一句话：**修掉一个"设了但不生效"的日志开关，并把一份 90% 内容失真的
可观测性文档按实测重写，同时给它加上双向门禁。**

这一轮是上一轮"编码损坏修复"的延续。`docs/OBSERVABILITY.md` 挂在编码损坏
登记表上（214 处中文字被截断成非法字节），我本来只打算修字节；结果一读内容，
发现字节问题只是表层。

---

## 一、`LOG_LEVEL` 从来没有对应用日志起作用

### 现象

`LOG_LEVEL=WARNING` 设了没用。我把它设成 WARNING，然后在子进程里真的打了
四条不同级别的日志 —— 四条**全都输出了**。

### 原因

这个变量只被传给了 uvicorn（Web 服务器自己的访问日志）。应用代码里
287 处日志调用走的是 structlog，而 structlog 的配置里**没有装级别过滤器**，
所以它一条都不过滤。

### 为什么这比"没有这个开关"更糟

有开关但不生效，运维会以为噪音已经压掉了，于是不再关注日志量。
而磁盘和日志后端照旧在被灌满，真正要看的 error 淹在 debug 里 ——
问题被"我已经处理过了"的错觉掩盖住。

一个不生效的开关，本质上和一个假的仪表盘是同一类问题：
它不是缺功能，它是在**主动误导**。

### 修法与验证

装上级别过滤器。然后实测七种取值（每种都开独立子进程，避免
structlog 全局状态串味）：

| 设成 | 实际放出的级别 |
|---|---|
| `DEBUG` | debug / info / warning / error |
| `INFO` | info / warning / error |
| `WARNING` | warning / error |
| `ERROR` | error |
| `warn`（常见别名） | warning / error |
| `BOGUS`（拼错） | info / warning / error |
| 留空 | info / warning / error |

**最后两行是刻意的设计选择**：配置写错时退回 `INFO`，
**不能**退回 `DEBUG`。后者会让"手滑写错一个字"反而把全部调试日志放出来 ——
故障方向正好相反，而且没人会怀疑是自己拼错了变量值。

用变异测试确认这条真的被钉住了：把兜底从 INFO 改成 DEBUG，测试立刻变红；
去掉级别过滤器（回到修复前），变红；去掉 `warn` 别名，变红。三种破坏都被抓住。

---

## 二、可观测性文档：编码损坏只是表层，内容 90% 是虚构的

### 实测出来的数字

| 项 | 文档声称 | 代码实际 |
|---|---|---|
| 监控指标名 | 39 个 | 其中 **35 个根本不存在** |
| 日志事件名 | 15 个 | **一个都不存在** |
| 链路 span 名 | 一整棵树 | 名字**全不相同** |

### 为什么这类假名字特别危险

**Prometheus 查一个不存在的指标不会报错，只返回空结果。**

面板上呈现出来的是一条空曲线、一个「No data」。看起来像"系统很安静"，
而不是"你查的东西不存在"。基于假指标写的告警规则同理 ——
它永远不会触发，于是给人一种"有人盯着"的错觉。

日志查询也一样：按不存在的事件名去搜，得到的是"最近没有这类事件"，
而不是"你搜错了名字"。

**一个永远不报警的告警，比没有告警更危险**，因为它占掉了"这块已经有人管"
的位置。这和上面那个不生效的日志开关是同一种病。

### 重写内容（全部实测）

- 33 个指标的完整目录：名称 / 类型 / 标签 / 直方图分桶，直接从代码的
  Prometheus 注册表导出，不是手抄。
- 真实的日志字段与时间戳格式（是 `2026-08-23 12:21:10` 这种本地时间格式，
  **不是** ISO-8601 带 `Z` 的形式 —— 写日志解析规则会踩这个）。
- 269 个真实事件名的命名空间分布，附最高频的 8 个命名空间与示例。
- 实际的 4 个链路 span 名。
- 已就位的 7 条 Prometheus 告警规则、10 个 Grafana 面板 ——
  逐条核对过引用的指标真实存在（结论：这两份配置文件是干净的，
  失真只在文档里）。
- 采集侧那套写在代码里、走日志不走 Prometheus 的五阈值告警（此前完全没写）。

### 12 项"未实现"集中标注

以前这些设计意图和现状混在一起写，读者分不清哪句描述的是现在、
哪句是计划。现在全部拎出来单独一节，逐项标注：
日志采样、note 截断、`X-Run-Id` 响应头、LLM 成本/token 指标、
Agent 粒度指标、数据质量指标、采集配额指标、HTTP 耗时直方图、
业务面板、`metrics` 表写入、告警抑制策略。

### 顺带查明的两处"看着像有、实际是空"

**`metrics` 表是死表。** 表结构在、数据访问层在、测试也在调用它 ——
但**生产代码里没有任何写入点**，实测 0 行。留着它会让人以为指标有历史留存，
排查问题时白跑一趟。要么接上，要么删掉，不该维持现状。

**`/metrics` 端点无鉴权。** 无凭据请求实测返回 200。在容器内网里这是常规做法，
但文档此前一个字都没写 —— 部署时容易漏掉网络层限制。现已明确写出，
并指明抓取目标是 compose 内网名。

---

## 三、给文档加了双向门禁（17 项检查）

新增 `backend/tests/test_observability_doc_parity.py`。它做六件事：

1. 文档写的每个指标必须在代码注册表里存在。
2. **反向**：代码里的每个指标也必须写进文档。
3. 文档里那份"代码中不存在的指标"对照清单，必须真的全部不存在。
4. 文档引用的日志事件名必须真实。
5. 文档引用的链路 span 名必须真实（且反向也要覆盖）。
6. 已交付的告警规则与 Grafana 面板不得引用幽灵指标。

**第 2 条为什么必须有**：少写一个指标和写错一个指标一样有害 ——
读者不会去搜索一个他不知道存在的东西。只查"文档里的是否为真"是单向的，
等于只修前门。

**第 3 条为什么必须有**：那份对照清单是为了让照旧文档写过查询的人对上号。
如果清单里混进一个真指标，等于劝人放弃一个可用的指标 ——
清单本身就成了新的谎言。

### 变异测试：六种破坏，六次变红

不做变异测试就不能相信一套新测试。这次跑了六种破坏：

| 破坏 | 结果 |
|---|---|
| 把一个真指标名改成不存在的名字 | 红（2 项失败） |
| 删掉一整行真实指标 | 红（反向断言抓住） |
| 把真指标塞进"不存在"清单 | 红 |
| 编一个日志事件名 | 红 |
| 编一个 span 名 | 红 |
| 改掉小节标题（让拆分锚点失效） | 红（4 项失败） |

**第一版的 span 检查是绿的 —— 这是本轮最值得记的一条教训。**

我第一版把 span 名**写死在测试代码里**（`for span in ("airdrop.pipeline.run", ...)`），
然后断言"这些名字在代码里存在"。变异测试把文档里的 span 名改成假的之后，
测试**照样全绿** —— 因为它检查的是测试自己的常量，跟被测文档毫无关系。

> **一个不读被测对象的断言，等于没有断言。**

改成从文档解析 span 名之后，同一个变异立刻变红。这已经是本项目第四次
遇到"看起来在检查、实际什么都没检查"：
前三次分别是注释让字符串断言假通过、注释让"字符串不存在"断言假失败、
以及一段永不执行的跳过逻辑。共同点都是**断言的对象错了**。

---

## 四、关于一型编码损坏的结论（已写进检查脚本注释）

上一轮说"前 2 字节把候选限制在 64 个以内，修复是做选择题"。
本轮把这道选择题真的做了一遍，结论是：**做不了。**

| 手段 | 能唯一确定多少处 |
|---|---|
| 同文档字符集收敛 | 101 / 1115（9.1%） |
| 结构重复标尺（2/3/4/5 字窗口） | **0 / 1115** |
| git 历史里的干净底本 | **不存在**（旧版本是改写前的草稿，行级唯一匹配 0 条） |

而且损坏不只是"丢第 3 字节"，它还**额外吞掉了相邻一个字节** ——
`OBSERVABILITY.md` 从 293 行塌成 268 行，表格行和列表项被粘连在一起，
连上下文结构本身都被破坏了。

所以修复方式只能是**按实测真相重写**，绝不猜原字。
理由和上一轮一样，只是现在有数字支撑：**一个猜出来的字比一个明显的乱码更坏，
因为读者分不清哪个是原文。**

`docs/OBSERVABILITY.md` 已从登记表移出，不再享有豁免。

顺带记一条：**登记豁免会掩盖内容问题，不只是字节问题。**
那 35 个假指标名能活这么久，正是因为文件挂在"已知损坏"清单上 ——
既然已经登记为坏的，就没人会去逐行读它。上一轮 API_SPEC 的 16 条假端点
是同一个成因。剩下两个还在登记表上的文件（`OPERATIONS.md` 404 处、
`DATA_SOURCE_STRATEGY.md` 498 处）**大概率藏着同类内容问题**，
修的时候要按"重写 + 加门禁"来做，不能只修字节。

---

## 五、CI 钓出一个本地看不见的句柄泄漏（本轮最有价值的意外收获）

上面那套新回归测试在本地全绿，**推上 CI 后变红**。

### 差别在哪

CI 跑 pytest 时加了 `-W error::ResourceWarning`（把资源警告当错误），
本地默认不加。所以同一个缺陷，本地只是一条被忽略的警告，CI 直接失败。

### 缺陷本身

`configure_logging()` 的文档说它"幂等，可重复调用"。但它每次调用都
`open()` 一个新的日志文件，**旧句柄既不关闭也不再被引用** ——
只能等垃圾回收，届时 CPython 抛 `ResourceWarning`。

### 危害的形态比泄漏本身更值得记

CI 里失败的两个测试，其中一个是**归档定时任务的测试** ——
和日志配置毫无关系。而报错信息里指向的临时文件，是日志级别测试留下的。

> **泄漏发生在 A 处，报错记在恰好触发垃圾回收的 B 处身上。**

这种错位归因极难排查：看错误信息完全找不到真正的责任方，
只会觉得"归档测试莫名其妙地偶发失败"。所以必须在源头钉住，
而不是在报错的那个测试里加个 try/except 糊过去。

### 修法

重新配置前显式关掉上一个句柄。关闭失败不阻断日志安装 ——
日志系统装不上比一个悬空句柄严重得多，不能为了修小问题引入大问题。

变异测试确认：去掉关闭逻辑，两个相关测试立刻变红。

生产里 `configure_logging()` 只被调一次，线上影响有限；
但任何"改完配置重装日志"的场景都会稳定泄漏。

### 教训

**本地绿不等于 CI 绿，差别往往在警告策略上。** 以后写涉及文件句柄、
socket、子进程的测试，本地就该主动加 `-W error::ResourceWarning` 跑一遍，
而不是等 CI 告诉我。

---

## 六、运维手册（OPERATIONS.md）：同一个成因，这次代价更高

上一节末尾我写下预判：「剩下两个还在登记表上的文件大概率藏着同类内容问题」。
接着修 `OPERATIONS.md`，预判成立，而且**这份文件的失真直接指向故障处置**。

### 查出来的东西

| 类别 | 实测结果 |
|---|---|
| 引用的指标名 | 19 个，**18 个不存在** |
| 引用的 API 路径 | **4 个不存在** |
| "已提供的巡检脚本" | **2 个文件根本没有**（却整段贴出了 bash 源码 + 安装 crontab） |
| 后端端口 | 全篇写 8000，真实 **8002** |
| 数据库文件 | 说在 `data/airdrop.db`，运行时真正连的是 `D:\app\data\app.db` |
| 章节编号 | 有**两个 `## 5`**，所有「见 §5.x」都是二义的 |

### 三条最值得记的

**1. 声称"LLM 超预算会自动停用"—— 这层保护不存在。**
`LLM_DAILY_BUDGET_USD` 只被两个只读接口读出来展示，代码里没有任何拦截逻辑，
也没有任何 token / 成本指标。真实的成本闸门只有 `/api/v1/run` 的频率限制。
**一层写在 runbook 里、值班照着信的保护，实际不存在，比压根没写更危险** ——
没写的话人会自己去确认，写了的话人会跳过确认。

**2. 403 比 404 更能骗人。**
`POST /api/v1/re-score/{id}` 这个接口根本不存在，但它落在管理员前缀里，
所以匿名调用返回 **403**。403 传达的意思是"接口有，你权限不够"，
于是没人怀疑过它。用管理员凭据打才露出真相：404。
上一轮我记过「405 比 404 更有说服力」，这次是同一类陷阱的升级版。

**3. 备份可能安静地备错文件。**
`scripts/backup.sh` 容器不在时会回退到复制 `data/airdrop.db` ——
那是个 94 个项目的过期副本，真库在 `D:\app\data\app.db`（288 个项目）。
它会**报告"备份完成"**。一个报成功却备错的备份，比明确失败的备份危险，
因为它把"我有备份"这个错觉一直维持到需要恢复的那一天。

顺带查实：归档任务从上线到现在**一次都没真正跑过**（`archive_runs` 0 行），
因为本地数据最早只到 08-09，没有记录超过 30 天保留期，每次触发都无事可做。
这条路径在生产上等于未验证。日志文件也**完全没有轮转**（6 天 3.97 MB，
无 handler、无 `max-size`、无 logrotate）。

### 门禁自己出了同一个毛病（本轮最有价值的发现）

新写的 `test_operations_doc_parity.py` 有 35 个测试，跑变异测试验证它能失败时，
**抓到了门禁自己的漏洞**：

第一版实现里，"§12 已登记为幽灵"是**整份文档的全局豁免**。
于是我把正文一句真话（`GET /api/v1/quarantine` 的 count）改成不存在的
`GET /api/v1/audit`，**测试依然全绿** —— 一条假命令只要名字曾进过纠错清单，
就能伪装成"已知问题"畅通无阻。

这跟这份文档最初出问题的机制**一模一样**：登记豁免掩盖内容问题。
我写门禁的时候，把自己刚总结的教训又犯了一遍。

改成**逐行判定**（只有那一行自己写出「不存在 / 没有 / ❌ / 从未」才豁免那一行），
10 个变异全部变红。同时加了"过滤条件写反"的自检 ——
如果过滤把绝大多数行都剔掉，测试会因为"几乎不检查任何东西"而失败。

> 教训：**一个豁免机制的粒度就是它的漏洞大小。** 整文件豁免 = 整文件失控。
> 项目里 `terminology-ok` 是逐行标记的，这个设计是对的，我该早点照抄。

`docs/OPERATIONS.md` 已移出编码豁免清单（一型损坏 902 → 498 处，只剩
`DATA_SOURCE_STRATEGY.md` 一个文件）。

---

## 七、三型编码损坏清零 + 采集源就绪状态的失真

### 7.1 三型：2 处 emoji 变 U+FFFD，修法是「删掉」不是「补一个」

`docs/SYSTEM_DIRECTION_CHANGE.md` 第 125、190 行两个小节标题的 emoji
被吃成了 U+FFFD。git 历史里最早的提交 `a9f2c8b` 就已经是坏的 ——
没有任何底本能证明原来是哪个 emoji。

**决定：直接去掉 emoji**，标题变成 `## 成功指标（KPI）` /
`## 实施路线图（v2.0）`。语义零损失。

为什么不补一个「看起来差不多」的（📊 / 🗺️ 是能猜到的）：补上去以后，
这份文档看起来就**从未损坏过**，下一个人再也分不清哪个标题的 emoji 是原作者
选的、哪个是后来猜的。**一个看不出来的猜测，比一个看得出来的缺口更糟。**

修完后豁免清单 `KNOWN_BROKEN_REPLACEMENT` 清空，新增
`test_replacement_registry_is_empty` 正面钉住「必须保持为空」——
跟二型清零那次一套设计。现在只剩 `DATA_SOURCE_STRATEGY.md`（一型 498 处）
挂在豁免清单上，从 5 个文件降到 1 个。

### 7.2 我自己误判了一次：终端乱码 ≠ 文件乱码

上一轮记忆里写着「`rate_limit.py` / `prometheus.yml` / `docker-compose.prod.yml`
有 GBK 型乱码，现有判据抓不到，可能需要第 4 个检测器」。

**这条是错的。** 这一轮读字节实测：三个文件都是干净的 UTF-8，中文完全正确。
乱的是 PowerShell 控制台的显示。

**教训：判断编码损坏必须读字节，不能凭终端里看到的输出下结论。**
顺手把设想的第四型（UTF-8 被当成 GBK / latin-1 读）也扫了一遍全仓
502 个文本文件 —— **0 命中**。结论写进 `docs/ENCODING_REPAIR.md` §6，
连这次误判本身也一起记进去了。

### 7.3 「10 个采集源全部就绪」是假的，实测只有 5 个

`OPERATIONS.md` §4.3 上一版写 10 个源「全部已注册且 `config_ready=true`」。
实测 `GET /api/v1/collections/sources`：只有 5 个 true
（`defillama` `coingecko` `github` `cryptorank` `etherscan`）；
另外 5 个开关关着、Key 也没配。`data_sources` 表里同样只有那 5 个源有记录。

**危害的形态**：未就绪的源在端点里照样列出来，只是 `config_ready=false`。
排查「为什么没发现新项目」时不看这一列，就会去翻一个**从未运行过**的源的日志 ——
翻不到任何错误，于是误判成"采集是好的、问题在分析侧"，方向整个跑偏。

**门禁设计上的一个刻意选择**：比对的是**门控规则**（每个源的开关配置名 +
是否需要 Key），**不是**本机当前的就绪值。就绪值取决于 `.env` ——
把它钉进测试，这份文档在 CI 和任何别人的机器上都会必然变红，
而那种红传达的是错误信息（"文档写错了"，其实是"这台机器没配 Key"）。
本机实测快照仍写在文档里，但标了日期、不参与断言。

顺带实测到两件事：两个 twitter 源**共用同一个开关** `TWITTER_ENABLED`
（没法只开一个）；代码默认值下只有 `defillama` / `coingecko` / `github`
的开关是开的，其余 7 个默认关。

变异测试：改错开关名、把「需要 Key」改成「不需要」、删掉一行源 —— 3/3 变红。

---

## 八、`.env.example` 有 47 个键跟代码不符，还自相矛盾

配置模板是新人和部署脚本唯一的起点，但**它不被任何代码读取** ——
写错不会报任何错，只会静默误导人。逐键跟 `app/config.py` 比对后发现：

### 8.1 最危险的一处：模板自己说 SQLite，实际连 Postgres

文件写着 `DB_BACKEND=sqlite`，同时把
`DATABASE_URL=postgresql://airdrop:airdrop@localhost:5432/airdrop` 打开了。
而 `_resolve_db_backend()` 会**反向**把 `db_backend` 改成 `postgres`。

**照这份模板复制出来的 `.env`，实际连的是 Postgres，跟它自己声明的那行完全相反。**

这类错误最难发现：两行单独看都合理，只有读过那个 validator 的人才知道
后者压过前者。改法是把 `DATABASE_URL` 注释掉，并在注释里写清两条 PG 激活路径
和"它会反向改写 DB_BACKEND"这件事。

### 8.2 其余失真

- **47 个键的值不一致**：6 个 cron、9 个源的重试次数（2 vs 3）、
  misfire grace（300 vs 3600）、`SQLITE_BUSY_TIMEOUT_SECONDS`（模板还把
  单位写成毫秒，实际是秒）、JWT 有效期（24 vs 72）、6 个
  `OPPORTUNITY_ECONOMIC_*`（模板全 true，代码全 false）等。
  照模板填出来的系统行为和代码默认值不同，**而两边都"看起来对"**。
- **2 个键全仓无人读取**：`LLM_API_KEYS` / `LLM_BASE_URLS`。多接口故障转移
  真正读的是编号变量（`LLM_BASEURL_1` / `LLM_API_KEY_1` / `LLM_MODELS_1_1`），
  走 `os.environ` 不经 Settings。照那两个填，**一个 LLM 接口都不会注册**。已删。
- **`WEIGHT_VERSION=score-v1.4` 混了两个版本轴**：`score-v1.4` 是评分
  **模型代号**，这个键是**权重版本**（代码默认 `v1.2`）。照模板填会把模型名
  写进 `projects.weight_version` 列。
- **2 个路径不存在**：`SEED_DATA_PATH=data/seed_projects.json`（真实是
  `scripts/seed.py`）、`FETCHER_CACHE_DIR=data/fetcher_cache`（真实是 `cache/`）。
- 顺带标注了 5 组「填了也不生效」的键：3 个 `RATE_LIMIT_*`（限流至今未实现）、
  `DUNE_API_KEY`、`TWITTER_API_KEY` / `TWITTER_API_SECRET`（采集器只认
  `TWITTER_BEARER_TOKEN`）、`LLM_DAILY_BUDGET_USD`。保留但写明不生效 ——
  **一个填了以为生效的开关比一个缺失的开关更坏。**

### 8.3 新门禁，以及一个自己踩到的设计坑

`backend/tests/test_env_example_parity.py`（13 个测试）两条主断言：
每个键必须真的有人读（是 `Settings` 字段或标 `env-external` 说明谁读它）；
每个值必须等于代码声明的默认值，或标 `env-differs` 写出理由。

**踩到的坑（同一条原则，犯了两次）**：

第一次 —— 比对写成 `getattr(Settings(_env_file=None), name)`，在 pytest 里
立刻挂了：`conftest.py` 设了 `APP_ENV=test` / `DB_PATH` / `HOST`，
而环境变量优先级高于 dotenv。改成读 `Settings.model_fields[...].default`。

第二次 —— 路径断言把 `FETCHER_CACHE_DIR=cache` 当成"仓库里必须存在的路径"，
**本地全绿、CI 直接挂**：`cache/` 是 `_FileCache.__init__` 里
`mkdir(parents=True, exist_ok=True)` 按需建出来的，本机因为跑过应用才有，
全新 checkout 上并不存在。已拆成两条：`SEED_DATA_PATH` 查"随仓库提交的文件
存在"，运行时目录改查"是相对路径、不用 `..` 逃出仓库"。

**教训：一个随环境改变结论的断言不是断言。** 同一条原则我在这一轮里犯了两次，
第二次还是靠 CI 才发现的 —— 说明「本地绿」根本不足以证明门禁写对了；
凡是断言涉及"文件是否存在""某个值是多少"，都要先问一句
**「在一台没跑过这个项目的新机器上，这条还成立吗？」**

同理，"按模板加载"用的是 init kwargs 而不是 `_env_file=`，
否则测的不是"照这份模板会得到什么"。

豁免**逐键显式**：标记写在键的上一行注释里，`grep -n env-differs .env.example`
可审计全部例外；空行截断注释块，所以标记不会顺着整节蔓延。整文件豁免
正是 `API_SPEC.md` / `OPERATIONS.md` 当初烂掉的机制。

变异测试 **11/11 变红**（改错 cron、取消注释 `DATABASE_URL`、加无人读取的键、
破坏权重和、让标记过期、把 `env-external` 标在真字段上、把 `WEIGHT_VERSION`
换成模型代号、缓存目录改绝对路径、用 `..` 逃出仓库、seed 路径指向不存在的文件），
模板字节等同恢复。

---

## 验证结果（全部实跑）

| 检查 | 结果 |
|---|---|
| `pytest`（含覆盖率门禁 ≥80%） | 日志轮：**2730 passed / 5 skipped**，88.39%；运维文档轮：**2765 passed / 6 skipped**，88.38%；三型 + 采集源轮：**2770 passed / 6 skipped**，88.38%，36m21s；`.env.example` 轮：**2783 passed / 6 skipped**，88.38%，36m34s（四轮均按 CI 的 `-W error::` 参数跑） |
| `ruff format --check app tests` | 235 files already formatted |
| `ruff check app tests` | All checks passed |
| `mypy app` | no issues in 117 source files |
| `scripts/check_encoding.py` | exit 0（**剩 1 个登记文件**，从 5 降到 1） |
| `scripts/check_terminology.py --all` | exit 0 |
| 前端 `tsc --noEmit` | exit 0 |
| 前端 `eslint` | exit 0 |
| 前端 `node test.mjs` | 20 pass / 0 fail |
| 新增 parity 套件单跑 | OBSERVABILITY 17 passed；OPERATIONS **39 passed**；编码 **26 passed**；`.env.example` **14 passed** |
| 变异测试（文档 6 + 日志级别 3 + 句柄泄漏 1 + 运维文档 10 + 采集源 3 + env 模板 11） | **34/34 按预期变红**，被改文件均字节等同恢复（SHA 核对） |

---

## 决定与取舍

1. **非法 `LOG_LEVEL` 退回 INFO 而不是 DEBUG** —— 配置写错不该反而放开输出。
2. **文档里保留那份"35 个不存在的指标"清单**，而不是删掉了事 ——
   照旧文档写过查询的人需要一个地方对上号；同时用测试保证这份清单里
   没有真指标混进去。
3. **`metrics` 表暂不动**（不接也不删），只在文档里如实标注它是死表。
   动它属于功能变更，需要所有者决定。
4. **span 检查改为从文档解析**，不写死名字 —— 见上面那条教训。
5. **句柄泄漏在源头修**（configure_logging 里关旧句柄），
   不在报错的那个无关测试里加抑制 —— 抑制只会让下一个人再踩一遍。
6. **文档门禁的豁免改成逐行判定，不做整节/整文件豁免** ——
   整文件豁免正是这些文档最初烂掉的原因，不能在防它的机制里重犯。
7. **`scripts/deploy.sh` / `health-check.sh` 里的 8000 端口本轮不改**，
   只在文档里明确标注"这两个脚本现在跑不通"。改脚本要单独验证
   （本机没有可用的 bash + Docker 环境跑通它们），混进文档 PR 里等于
   交付一份没验证过的代码。
8. **三型 emoji 直接删掉，不补猜的**（见 §7.1）——
   看不出来的猜测比看得出来的缺口更糟。
9. **采集源门禁比对「门控规则」而不是「本机就绪值」**（见 §7.3）——
   钉住依赖 `.env` 的值会让文档在别人机器上必然变红，而那种红是假信号。
10. **`.env.example` 门禁读「声明默认值」而不是实例属性**（见 §8.3）——
    同一条原则：断言不能随环境改变结论。
11. **「填了不生效」的键保留但写明不生效，不删掉**（`RATE_LIMIT_*`、
    `DUNE_API_KEY`、`TWITTER_API_KEY` 等）—— 删掉会让人以为这个能力不存在，
    留着不说明则更坏。写明「配了没用、以及为什么」是唯一诚实的选项。

---

## 下一步

1. 重写 `docs/DATA_SOURCE_STRATEGY.md`（498 处一型损坏，预计同样有内容失真）。
   已用 `errors="replace"` 生成过可读副本通读了一遍，确认失真点至少包括：
   全篇的 collector 类名和文件名写成「计划实现位置」，但 10 个 collector
   **早已全部实现**（`backend/app/collectors/` 下 18 个 `.py` 文件）；
   §采集质量评估流程说每月输出报告到 `evaluation/collection/`，
   实测该目录**不存在**（只有 `evaluation/llm/`）；
   `POST /re-score/{id}` 又出现了一次（不存在的接口）；
   `OPERATIONS.md §5 采集故障处理` 这个交叉引用已失效（现在是 §4.3）。
2. 修 `scripts/deploy.sh` / `health-check.sh` 的 8000 端口 + 容器名 + DB 路径
   （需要能实跑验证的环境）。

## 遗留问题（需要所有者拍板的）

1. **Python 版本口径不一致**：Docker 镜像与 CI 用 3.12，本地环境是 3.11.9，
   项目声明 `>=3.11`。两个版本都能跑通全套测试，但"以哪个为准"需要定。
2. **`/api/v1/collections` 是否要加进管理员白名单**：目前采集触发接口
   任何拿到匿名 token 的人都能调（会真的跑采集并写库）。上线前建议收紧。
3. **生产环境建议把 `SEED_FALLBACK_ENABLED` 设为 false**（当前 true）——
   否则采集失败时会静默用种子数据兜底，看起来像采集成功了。
4. **`metrics` 表要接上还是删掉**（见上）。
5. **归档功能的真实运行仍未观测到** —— 本地数据只覆盖到 08-15，
   还没等到一次真实的 03:00 归档窗口。
