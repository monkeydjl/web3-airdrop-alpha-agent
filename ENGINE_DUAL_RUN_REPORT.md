# 引擎改动新旧双跑对比报告

> 生成日期：2026-07-26 · 对应 ADR-014 · 工具：`backend/scripts/dual_run_compare.py`
> 用途：在决定是否对存量库全量重算之前，先量化改动的影响面。

---

## 一、先看结论

改动分两条独立链路，互不影响：

**主评分决策引擎（`score-v1.4`，用户可见分数）** — 264 项语料中 224 项分数变化，80 项跨越标签边界。FARM 数量由 95 增至 172（+81%）。变化的方向是**单向的**：几乎全部是"分数上升"，因为修复的三处缺陷都在系统性地惩罚证据更充分的项目。

**旁路机会引擎（`opportunity-v2.0`，不影响主分）** — 270 项语料中 94 项决策变化。其中 90 项由"证据不足，请补充证据"更正为"成本超预算，判定不合适"；4 项由 WATCH 升为 FARM。

两条链路都不需要迁移数据即可运行。是否重算存量库，是一个**产品口径**决定而非技术必需——见第五节。

---

## 二、主评分决策引擎

语料 264 项 = 12 个 Golden 用例 + 192 项信号空间网格 + 60 项走真实跨源合并路径的项目。全部确定性，无随机。

```
语料规模              : 264 个可比项目
分数发生变化          : 224 (84.8%)
分数变化区间          : -2 .. +19
平均变化              : +6.20
中位变化              : +2
```

### 标签迁移

| 迁移 | 数量 |
| --- | --- |
| WATCH → FARM | 77 |
| IGNORE → WATCH | 2 |
| WATCH → IGNORE | 1 |
| **合计** | **80（30.3%）** |

### 标签分布

| 标签 | 旧 | 新 | 变化 |
| --- | --- | --- | --- |
| FARM | 95 | 172 | +77 |
| WATCH | 161 | 85 | −76 |
| IGNORE | 8 | 7 | −1 |

### 置信度分布

| 统计量 | 旧 | 新 |
| --- | --- | --- |
| 最小值 | 0.5500 | 0.4429 |
| 中位数 | 0.7429 | 0.8143 |
| 最大值 | 0.9143 | 1.0000 |
| < 0.50 的项目数 | 0 | 3 |

最后一行是关键：规范 `DATA_SCORING_DICT.md §6.1` 写着"`confidence < 0.5` 时 label 强制降一档"。
旧实现在 Agent 覆盖率满时给 confidence 加了 0.55 的下限——精确地说，Agent 崩溃时旧公式仍能低于
0.5（缺 1–3 个 Agent 的下限依次为 0.45 / 0.20 / 0.10），但那是异常路径；**四个 Agent 全部成功的
正常路径**下 confidence 恒 ≥ 0.55（穷举 256 种信号配置最低值恰为 0.5500）。于是这条规则只在
Agent 崩溃时生效，而它本意是防"可验证信号不足"——信号再稀疏也降不了级。去掉下限后，
信号稀疏的项目首次可触发降档（语料中 3 个）。

### 变化幅度的分层

分数变化不是均匀分布的，而是明显分成两层：

| 层 | 项目 | 变化幅度 | 原因 |
| --- | --- | --- | --- |
| 走跨源合并路径 | 60 项（`MergeProject*`） | **+19**，全部 WATCH→FARM | 合并不再丢信号，`source_count` 恢复真实值 |
| 未走合并路径 | 204 项（Golden + 网格） | −2 .. +3，中位 +2 | `tokenomics.risk` 修正 + `airdrop_signal` 统一 + confidence 口径 |

第一层最能说明问题。语料构造的是生产中最常见的形态：一条来自任务门户（galxe，信号丰富）
的记录 + 一条来自行情站（defillama，信号稀疏）的记录，同一个项目。旧引擎合并后：

```
galxe 记录单独评分   : 77  FARM
两条记录合并后评分   : 64  WATCH     ← 多发现一个来源，分数反而掉了 13 分
新引擎合并后评分     : 80  FARM
```

这不是精度问题，是方向问题：旧引擎在**惩罚证据**。

---

## 三、旁路机会引擎

语料 270 项 = 「资格机制(3) × 事件强度(3) × 多钱包政策(2) × 成本档位(3) × 证据年龄(5)」全组合。

```
机会语料规模                : 270 个可比项目
决策状态发生变化            : 94 (34.8%)
reward_probability.low      : 均值 +0.0555   区间 +0.0396 .. +0.0724
reward_probability 区间宽度 : 均值 -0.1728   区间 -0.1859 .. -0.1561
跨过 low>=0.20 FARM 门槛    : 新获得 80 / 新失去 0
confidence.overall          : 均值 -0.0200   区间 -0.0375 .. +0.0000
```

### 决策状态分布

| 状态 | 旧 | 新 | 变化 |
| --- | --- | --- | --- |
| ACTIONABLE（FARM） | 7 | 11 | +4 |
| MONITOR（WATCH） | 173 | 169 | −4 |
| INSUFFICIENT_EVIDENCE（WATCH） | 90 | 0 | −90 |
| NOT_FIT（IGNORE） | **0** | 90 | +90 |

`NOT_FIT` 旧值为 0，跑遍 270 种组合一次都没产出过——这是"`TOO_EXPENSIVE` 在真实链路上
不可达"最直接的证据。成本超预算会让资格概率无法派生，进而被"证据不足"分支抢先短路。
用户收到的建议是"去补证据"，而真实结论是"这个项目对你的画像来说太贵了，补再多证据也没用"。

### 理由码修正

| 迁移 | 数量 |
| --- | --- |
| `WAIT_MORE_EVIDENCE` → `TOO_EXPENSIVE` | 90 |
| 去掉 `REWARD_TOO_UNCERTAIN` | 80（新增该码：0） |

第二行是区间算法修正的结果：`REWARD_TOO_UNCERTAIN` 此前有很大一部分不是真的"回报太不确定"，
而是逐分位连乘把区间下界压到了 0.1650 这个数学假象上。

### 区间算法为何这样改

`joint_probability` 的 `base` 按独立性假设取三因子相乘，端点却按 `low×low×low` 连乘。
逐分位连乘只在三因子完全同向时成立，与 `base` 的独立性假设**不可能同时为真**。

以最佳规则栈（0.65/0.78/0.90 × 0.65/0.80/0.90 × 0.75/0.88/0.95）验证：

| 方法 | p10–p90 区间 | 宽度 |
| --- | --- | --- |
| 40 万次独立三角分布蒙特卡洛（真值） | 0.4528 – 0.5953 | 0.1425 |
| 逐分位连乘（旧） | 0.3169 – 0.7695 | 0.4526 |
| 相对不确定度平方和合成（新） | 0.3893 – 0.6664 | 0.2772 |

新算法仍偏保守（覆盖率 99.2%，宽于真值），这是刻意的——该区间下界直接驱动资金决策门槛，
宁可保守。但它不再与 `base` 的假设自相矛盾，中档规则栈的 `joint.low` 由 0.1650 回到 0.2154，
门槛重新可达。

平方和合成还额外加了两道兜底：下界不得低于逐分位连乘、上界不得高于逐分位连乘。没有这层兜底，
当某因子 `low = 0` 使相对不确定度之和超过 100% 时，`base × (1 − rel)` 会变成负数并被夹到 0——
比旧算法还悲观，与"只收紧不放宽"自相矛盾。0.1 网格上穷举 2334 万组三元组验证：0 违例，
且无一例出现"旧过 FARM 门槛、新过不了"。

### 两点需要如实说明

**一、区间收窄是双向的。** 新 `high` 恒 ≤ 旧 `high`（可证：`Π(1+rᵢ) ≥ 1+Σrᵢ ≥ 1+√Σrᵢ²`），
所以 `gross_reward.high < 30 → DUST_REWARD`（30 天 IGNORE）比以前更容易触发。这不是缺陷——
旧 `high` 落在真实分布约 0% 的尾部，靠一个虚高的乐观值去躲开 DUST 判定本就不成立——但
"只会解锁 FARM"的说法是不完整的，它同样会产出新的 IGNORE。相应地，`base = 0` 时端点不再
一并归零（保留连乘值），否则一个"最可能不发生但并非不可能"的存活率估计会把 `gross_reward.high`
强行归零，经 DUST 门槛误判成 IGNORE。

**二、新鲜度衰减没有改变任何决策。** confidence 因尾部衰减最多下降 0.0375，语料中**没有任何项目**
因此丢失 FARM（`跨过门槛：新失去 0`，无 ACTIONABLE → MONITOR 迁移）。前四档（≤7/30/90/180 天）
与原值完全一致，只延长了 180 天之后的尾部，任何年龄的 freshness 都 ≤ 原值。

---

## 四、验证方式

| 项 | 结果 |
| --- | --- |
| 后端全量测试 | **1870 passed**（含 36 条回归测试 + 12 个修订后的 Golden 用例） |
| `ruff check` | 全部通过 |
| `ruff format` | 无新增漂移（基线本身有 11 个文件漂移，未动） |
| 区间算法 | 40 万次蒙特卡洛验证覆盖率 + 0.1 网格穷举 2334 万组验证"新区间恒为旧区间子集"，均已固化为测试 |
| 合并顺序无关性 | 4000 组随机记录集 × 全排列，0 例结果随顺序变化 |
| `TOO_EXPENSIVE` 可达性 | 打桩回退该修复后复现旧行为（`INSUFFICIENT_EVIDENCE` / `REWARD_TOO_UNCERTAIN`），确认回归测试有效 |
| 双跑可复现 | `dual_run_compare.py` 语料全确定性，`now` 固定，无随机 |
| 对抗式复核 | 独立子代理逐条尝试证伪，确认 9 处缺陷（含本轮改动引入的 3 处），已全部修复并补回归测试 |

对抗式复核抓到的三处**本轮引入**的问题值得单独记一笔，它们都不是"不够优雅"而是会出事的：

1. `_determinate_misfit` 原本不校验证据来源等级，而 `resolve_factor` 没有等级下限——一条
   `U` 档（权重 0）的 `assumed` 成本记录就足以把项目判成 30 天 `IGNORE`。已加
   `hard_cost_confirmed_minimum`（observed/derived + 等级 ≥ B），与同一函数里工时分支的门槛对齐。
2. 子分快照原本写进 `raw_signals` 列，而该列存的是采集到的**输入**信号（`scripts/seed.py` 与
   `raw_signals_hash` 都按此语义写入），两种形状撞在一个列名下；且评分失败时会用 `{}` 覆盖掉
   上一次的好快照。已改为独立的 `sub_scores` 列 + `COALESCE` 写入。
3. 合并策略最初写成"无差别 OR/max"，会让一条 twitter 噪声推翻 `manual` 的显式 `False`。
   但改成"严格按可信度裁决"又矫枉过正：抵达合并的是**已归一化整行**，爬取源缺失的布尔被填成
   `False`，那是"没看到"而非"核实了不存在"——实测该写法把合并路径的收益从 +19 削到 +8，
   等于把本轮最主要的修复还了回去。最终方案是分开处理：爬取源全源 OR/max，
   `manual`/`api`（唯二能主张否定的来源）的显式取值直接采信。

一处方法学修正值得说明：双跑对比按 `name` 而非 `id` 对齐。项目 `id` 由内容派生（uuid5），
跨源合并结果一变 `id` 就变——而"合并结果变了"恰恰是本轮影响最大的改动。按 `id` 对齐会把
那 60 个项目静默判为"新旧各自独有"从而排除在对比之外，正好漏掉最该看的部分。

---

## 五、存量数据：三个选项

改动**不要求**迁移。新增的 `projects.sub_scores` 列由 `init_db` 在启动时自动补齐；
`weight_version` 与 `sub_scores` 在旧行上为空，新写入才有值，旧行照常读取和展示。以下三选一：

### 选项 A — 不重算（默认，符合 `WEIGHT_CALIBRATION.md §5`）

历史行保留旧 `weight_version`，新采集/新评分的项目走新引擎。

- 优点：零风险，无跳变；符合协议里"不强制全库重算"的既定做法
- 缺点：库里长期并存两套口径的分数，排行榜上新旧项目不可直接比较；已入库的多源项目仍带着被压低的分数

### 选项 B — 全量重算（一次性）

跑一次批量 re-score。

- 优点：口径统一，多源项目的分数回到应有水平
- 缺点：**全库标签一次性跳变**。按语料比例外推，约 30% 的项目会换标签，FARM 数量可能接近翻倍。若下游有按 FARM 数量设定的容量假设（每日推送条数、人工复核工作量、前端首屏），需要先评估
- 建议：先在只读副本上跑一遍，用 `dual_run_compare.py` 的口径统计真实库的迁移矩阵，再决定

### 选项 C — 分批重算（推荐）

先只重算 `source_count >= 2` 的项目（受合并缺陷影响最大的那批），观察一周，再决定是否扩到全库。

- 优点：把最大的一处修复的收益先拿到，同时把标签跳变控制在可观察的规模
- 缺点：过渡期内库里有三种口径（旧、部分新、全新），运维需要记账

### 无论选哪个，建议先做

- [ ] 在只读副本上跑一次全库双跑，得到**真实数据**（而非语料）的迁移矩阵
- [ ] 确认下游是否存在按 FARM 数量的容量假设
- [ ] 若已有基于旁路引擎标签的告警，同步调整（90 项 WATCH→IGNORE 的迁移会触发）

---

## 六、复现命令

```bash
cd backend

# 主评分决策引擎
git stash && python scripts/dual_run_compare.py dump /tmp/before.json && git stash pop
python scripts/dual_run_compare.py dump /tmp/after.json
python scripts/dual_run_compare.py diff /tmp/before.json /tmp/after.json

# 旁路机会引擎
git stash && python scripts/dual_run_compare.py dump-opp /tmp/opp_before.json && git stash pop
python scripts/dual_run_compare.py dump-opp /tmp/opp_after.json
python scripts/dual_run_compare.py diff-opp /tmp/opp_before.json /tmp/opp_after.json
```

> `git stash` 会把新代码暂存以在旧代码上取基线。若工作区有未跟踪文件，用 `git stash -u`，
> 并注意 `scripts/dual_run_compare.py` 本身也会被暂存——先复制到 `/tmp` 再放回。
