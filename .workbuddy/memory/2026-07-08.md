# 2026-07-08

- 阅读《Web3 Airdrop Alpha Agent System.md》设计文档（项目定位 / 架构 / 模块 / API / 前端 / 演进路线）。
- 编写 `ENGINEERING_ROADMAP.md`：基于设计文档的详细工程路线图（技术栈、架构、数据模型、Agent 设计、评分算法、API、前端、数据接入、调度、MVP→V2→V3 演进、里程碑、测试、部署、风险、验收标准）。
- 用户明确要求：**先写文档，不要急于写代码**。曾误判"继续完成未完成的任务"为开始编码并写了部分代码骨架，已删除恢复纯文档状态。
- 应要求补充 4 份专项文档（docs/）：`API_SPEC.md`、`DEPLOYMENT.md`、`FRONTEND_SPEC.md`、`DATA_SCORING_DICT.md`。
- 当前状态：文档阶段完成（roadmap + 4 专项规范）。等待用户发出"开始写代码 / 开始写 production repo"后再进入实现。
- 用户二次确认仍处设计阶段，要求"整体完善 + 细化模块 + 新内容能写入就写入"，不写实现代码。
- 完成 Roadmap v1.1 扩充：
  - 决议 ADR-001~005（§18），更新 §2.1/§16 状态为已决议，新增 ADR-004（SQLite→PG）、ADR-005（APScheduler）。
  - 细化 §6.1（PipelineState/AgentContext/reducer 语义）、§6.2（归一化去重/幂等/UUID v5）、§7.6-7.9（缺失降级/平滑/tie-break/权重校准）、§10.1-10.4（fetcher 容错矩阵+TTL+回填）、§14（测试金字塔+契约+属性+golden）、§15（CI/CD+配置管理+迁移回滚+备份）。
  - 新增 §19 LLM 集成、§20 可观测性、§21 安全合规、§22 数据治理、§23 容量性能、§24 反馈回流与 Memory（V3 前瞻）。
  - 更新 §0 阅读指引、§4 目录结构（fetcher.py/backtest.py/prompts//tests 子目录）、§17 DoD 衔接新设计、文档版本→v1.1。
- 仍处规划阶段，未写实现代码。
- 用户第三次推进"继续推进可推进设计"，完成 Roadmap v1.2：
  - 拆分 ADR 到 `docs/adr/`（README + ADR-001~005 独立成文，四段式格式 + 模板），Roadmap §18 改为索引表。
  - §5.4 新增 V2 表 DDL：feedback / events / quarantine / project_history / weight_changelog / narratives（6 张表，含索引与外键）。
  - §13 排期表重排为 12 周里程碑，每行附验收门；W4 含可观测骨架、W5 LLM、W6 安全、W9 可观测完整、W10 PG 迁移、W11 反馈校准。
  - 新增 `docs/OBSERVABILITY.md`（8 节：原则/日志规范/Prometheus 完整指标目录/追踪/告警/Grafana/本地调试/容量预估）。
  - 新增 `docs/SECURITY.md`（9 节：原则/STRIDE/密钥/鉴权/输入校验/依赖镜像/隐私合规/安全测试/事件响应）。
  - Roadmap §0 配套规范清单更新；版本号 → v1.2。
- 仍处规划阶段，未写实现代码。
- 用户第四次推进"继续推进"，完成 Roadmap v1.3：
  - 新增 `docs/DATA_QUALITY.md`（12 节）：6 维质量 + P0/P1/P2 字段分级 + 检测规则 + quarantine 处置 + TTL 新鲜度 + 血缘 + SLA + 治理流程。
  - 新增 `docs/OPERATIONS.md`（11 节）：角色值班 + 日/周/月检查 + 部署发布回滚 + 8 类故障手册 + 配置变更 + 备份恢复演练 + 监控 + 安全事件决策树 + 容量管理 + 紧急联系人。
  - 新增 `docs/GLOSSARY.md`：业务/技术/角色/文档缩写/阶段 5 类术语统一。
  - Roadmap §15 引用 OPERATIONS、§22 引用 DATA_QUALITY、§0 配套规范扩到 10 项；版本号 → v1.3。
- 文档体系成型，仍处规划阶段，未写实现代码。
- 用户要求“按顺序修复”，完成 P0/P1/P2 级缺口修复：
  - 统一 Schema：`ENGINEERING_ROADMAP.md` 的 `events`/`quarantine`/`project_history` 表与 `DATABASE_DDL.md` 对齐；新增 `audit_logs`/`llm_eval_changelog`/`metrics` 索引说明；修正 §16.1 ADR 索引至 ADR-010；修复 §8.6 重复章节编号。
  - 统一 reason 规则：`DATA_SCORING_DICT.md` §8 新增完整 reason 候选字符串表与选择算法；`GOLDEN_TEST_CASES.md` 全部 16 条用例 `reason_contains` 按新规则修正；GT-002 修正 `team.risk_level` 为 `high`；GT-016 补齐 reason。
  - 完善 API：`API_SPEC.md` 合并重复版本管理章节；新增 `POST /api/v1/events`、`POST /api/v1/auth/anonymous`、`GET /api/version` 端点；补充 `/health` 的 `config_version`；补充 `/projects` 的 `stage`/`search` 参数；`/project/{id}` 响应增加 `lineage` 字段；修正 `/feedback` 的 `outcome` 注释；移除 409 错误码。
  - 完善可观测性：`OBSERVABILITY.md` 注册 `airdrop_narrative_heat_score`/`airdrop_feedback_total`/`airdrop_project_score` 指标并修正业务面板 PromQL。
  - 补充实现细节：`DATA_SCORING_DICT.md` 新增 §5.7 子项内部映射（Tokenomics unlock_penalty、Risk token_risk 启发式、Team 多 flag 叠加、Narrative SECTOR_PROFILE 初值）。
  - 其他：`TASK_BREAKDOWN.md` W11-00 依赖修正为 W11-05；`USER_STORIES.md` 明确 IGNORE 颜色按版本区分；`DATA_QUALITY.md` 修正完整性扫描 SQL。
  - 补充设计阶段入口文档：`README.md`，包含项目定位、边界、文档地图、当前阶段、快速开始、技术栈、ADR 索引。仍为规划阶段，未写实现代码。
- 用户要求更新 `docs/adr/README.md` 索引：新增 ADR-001~010 的“一句话摘要”与“影响面”列，新增按主题快速导航表（评分算法、数据层、调度、前端、API 与兼容性等），便于按角色/主题查找 ADR。
- 用户要求做一次章节引用交叉核对：编写 Python 脚本扫描 32 份 Markdown 文档，提取标题编号与 SQL 注释伪章节，核对 `§X.Y` 引用与 Markdown 文件链接。初始发现 227 处引用问题，经逐轮修正（处理默认上下文、文件名边界、ROADMAP 别名、SQL 注释伪章节、backtick 包裹文件名等）后，最终修复至 0 问题。期间修复了 `ADR-008`/`CONVENTIONS`/`DATA_QUALITY`/`DATA_SCORING_DICT`/`DEPLOYMENT`/`DESIGN_GAP_ANALYSIS`/`DESIGN_REVIEW_CHANGELOG`/`ENGINEERING_ROADMAP`/`GLOSSARY`/`OBSERVABILITY`/`OPERATIONS`/`SECURITY`/`TASK_BREAKDOWN` 中约 30 处引用格式或目标章节错误。产出 `docs/CROSS_REF_CHECK.md` 报告，确认所有章节引用与 Markdown 链接一致。
- 用户要求以 15 年经验架构师身份建立/完善 Project Bootstrap 工程基础（18 个部分）。经检查，项目已完成 P0/P1 全部 40 项，P2 剩余 2 项未开始。本次补充：
  - 代码生成器模板：`templates/codegen/`（README + 6 个 Jinja 模板） + `scripts/codegen.py`。
  - 日志集中采集：Loki 配置 `docker/loki/`（含 `loki-config.yml`、`promtail-config.yml`、README），并更新 `docker-compose.prod.yml` 加入可选 Loki/Promtail 服务。
  - OpenTelemetry：`configs/observability/otel/`（Collector 配置、Instrumentation 元数据、README），并更新 `docker-compose.prod.yml` 加入可选 OTel Collector / Jaeger 服务。
  - 项目管理模板：`.github/ISSUE_TEMPLATE/task.md`、`docs/risk_register.md`、`docs/decision_log.md`、`docs/sprint_template.md`、`docs/backlog_template.md`、`docs/meeting_notes_template.md`。
  - 测试框架扩展：`tests/load/locustfile.py`（负载测试）、`tests/e2e/test_e2e_pipeline.py`（端到端测试）、`tests/README.md`（测试体系说明）。
  - 索引更新：`docs/PROJECT_BOOTSTRAP_CHECKLIST.md` 升级至 v1.3，P2 全部完成（42/42，100%）；新增 `docs/PROJECT_BOOTSTRAP_OVERVIEW.md`（18 部分落地索引 + 快速启动 + 状态统计）；`docs/00_index.md` 增加 Project 与项目管理模板引用。
- 未编写业务代码，所有产出为工程基础设施模板、配置与文档。

- 用户要求检查文档内容一致性并修复。本次检查覆盖新增 Bootstrap 文件、新增 P2 配置与现有 API_SPEC / Makefile / .env.example / docker-compose.prod.yml 的引用一致性。发现并修复：
  - `docker-compose.prod.yml` 中 Loki / Promtail / OTel / Jaeger 服务原被注释，导致 `PROJECT_BOOTSTRAP_OVERVIEW.md` 的快速启动命令会失败；改为取消注释并添加 `profiles: ["observability"]`，默认不启动监控栈，需要时通过 `--profile observability` 启用。
  - `PROJECT_BOOTSTRAP_OVERVIEW.md` 快速启动命令由 `make install` 修正为 `make setup`（Makefile 中实际目标名）；`docker compose` 命令增加 `--profile observability`。
  - `configs/observability/otel/README.md` 与 `docker/loki/README.md` 的启用说明由"取消注释"改为"使用 observability profile"。
  - `tests/e2e/test_e2e_pipeline.py` 原使用 `/api/v1/run/{run_id}/status`，但 `API_SPEC.md` 未定义该端点；改为直接验证 `/api/v1/run` 响应并等待数据落库。
  - `tests/README.md` 同步更新 E2E 测试范围描述。
  - `.env.example` 缺少 `docker-compose.prod.yml` 使用的 `POSTGRES_PASSWORD`，已补充；同时新增可选 OpenTelemetry 环境变量（`OTEL_EXPORTER_OTLP_ENDPOINT` 等）。
  - `configs/README.md` 目录结构增加 `observability/otel/`。
  - `templates/codegen/README.md` 示例命令补充 `--category backend`；新增 `templates/codegen/config_yaml.jinja` 以匹配目录结构。
  - `PROJECT_BOOTSTRAP_CHECKLIST.md` 中 Skill 数量由 21 修正为 22，文档版本号统一为 v1.3。
  - 创建 `docker/nginx/logs` 目录供 Promtail 挂载 Nginx 日志。
  - 运行一致性检查脚本验证：所有关键文件存在、YAML 格式有效、docker-compose 服务配置正确、.env.example 包含关键变量，全部通过。


