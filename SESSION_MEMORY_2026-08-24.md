# 2026-08-24

## 本次做了什么

一句话：**修备份脚本时踩进一个"改完跑成功、实际什么都没做"的坑，
根因是文件编码而不是代码逻辑；由此发现并封堵了第四型编码损坏，
顺带修掉一段"注释否认自己实现"的 docstring。**

这一轮的三件事有一条共同的线：**「跑通了」和「做对了」是两件事。**

- 备份脚本 exit 0 说"备份成功"，实际一个文件都没生成。
- 编码门禁的判据写得完全正确，但没接进主流程，删掉它 39 条测试全绿。
- `rate_limit.py` 的注释说限流从未实现，而它自己就是实现本体。

三件都不是"功能缺失"，而是**"看起来没问题"**。这类问题不会被人发现，
因为它们不产生任何报错信号。

---

## 一、备份脚本"跑成功了"，实际什么都没备份

### 现象

修完 `scripts/auto_backup.ps1` 的空目录问题后跑验证：脚本输出正常、
返回 exit 0、日志写着"备份成功"。但盘上没有任何产物，
Docker 甚至根本连不上。

打开 `Set-PSDebug -Trace 1` 看执行轨迹，发现**执行从第 20 行直接跳到第 153 行** ——
中间 130 多行代码一行都没跑。而 PowerShell 的语法解析器报 **0 个错误**。

### 根因：不在代码，在文件编码

逐字节验证的结论：

Windows PowerShell 5.1 在文件**没有 UTF-8 BOM** 时，
按系统 ANSI 代码页（简体中文机器 = **GBK/936**）解码脚本，而不是 UTF-8。
GBK 是双字节编码，规则是**任何 ≥ 0x80 的字节都无条件吃掉紧随其后的一个字节**
—— 包括 ASCII 引号。

而一个 UTF-8 中文字符是 **3 字节（奇数）**，GBK 按 2 字节一组啃，
于是"结束引号会不会被吃掉"取决于**它前面有多少字节的中文**：

| 字符串 | 字节数 | 结束引号 |
|---|---|---|
| `"中"` | 3（奇） | **被吃掉** |
| `"中文"` | 6（偶） | 保留 |
| `"中文字"` | 9（奇） | **被吃掉** |
| `"中文字符"` | 12（偶） | 保留 |

引号一被吃掉，字符串就不闭合，继续往下吞，把后面几十行代码全吃进一个
字符串字面量 —— **而且语法完全合法**，所以不报错、不警告，只是静默不执行。

原脚本能正常跑，是因为它原本是 **UTF-16LE**（`FF FE` 开头）；
我重写时存成 UTF-8 无 BOM，就踩了进去。

### 为什么这是最恶劣的一类问题

**这条奇偶性意味着「今天没事」不等于安全。** 同一个文件今天跑得好，
明天在某行日志里加一个字，奇偶性翻转，几十行代码就静默失效。
所以判据不可能是"检查有没有某种危险写法"，只能是"**这个文件必须有 BOM**"。

### 修法

`scripts/check_encoding.py` 新增**第四型**检查：
`.ps1` / `.psm1` / `.bat` / `.cmd` 只要含任何非 ASCII 字节，
就必须带 UTF-8 BOM（`EF BB BF`）。

**零豁免、零登记清单** —— 前三型的登记清单是历史包袱（原字符已不可逆丢失，
只能先挂起来），四型没有这个属性：修法是加 3 个字节。
一旦有豁免清单，"先登记着"就会变成默认选项。
这一点本身也写成了测试（`test_bom_registry_does_not_exist`）。

为什么不改成"脚本里不许写中文"：那是把成本转嫁给可读性，而且挡不住 ——
下一个人照样会写。

### 四型与前三型的性质差别（值得单独记）

前三型是"**文件内容坏了**"。四型是"**文件内容完全正确，是读它的人读坏的**"。

它证明了「文件内容合法」和「文件被正确读取」是两件不同的事，
而前三型只查了前者。这也是为什么四次"以为查完了"之后又查出一种：
**检测判据的盲区就是损坏的藏身处。**

### 实测验证

两个场景都真跑过：

1. Docker 未运行 → exit 1，**不留下**任何带时间戳的空目录，日志正常写入。
2. 预置 2 个空的历史遗留目录 + 1 个含 `airdrop_pg.dump` 的非空目录
   → 空的被回收（日志明确报"清理遗留空目录"），**非空的完好保留**。

---

## 二、核对四型时踩的两个坑（都比原问题更值得记）

### 坑 1：用 Python 的 gbk codec 去验证 .NET 的行为

同一串字节 `EF BC 89 22`（`）` + `"`）：

| 解码器 | 结果 |
|---|---|
| .NET cp936（PowerShell 实际用的） | 引号**被吃掉** |
| Python `gbk` codec + `errors="replace"` | 引号**保留** |

原因：前导字节后跟非法尾字节时两者策略不同 —— .NET **宽容**（无条件消费 2 字节），
Python **严格**（抛 `UnicodeDecodeError`，`replace` 只消费那 1 个前导字节，
于是引号被单独解码出来活了下来）。

**拿 Python 去"验证" .NET 会得到相反的结论。**
我第一版测试就是这么写的，而且当时"通过"了 —— 因为那个样本恰好两边一致。

固化的原则：**验证用的解码器必须和被验证的解码器是同一个**，
否则验证的是另一件事。既然 CI 上跑的是 Python（拿不到 .NET），
真值就只能是**实测记录下来的表**（`_DOTNET_CP936_KEPT_QUOTES`）。

### 坑 2：判据写对了，但没接进主流程

变异测试把 `main()` 里那个 BOM 检查分支整段删掉 —— **39 条测试全绿。**

原因：我只测了判据函数（`needs_utf8_bom` / `describe_bom_hazard`）和全仓扫描，
而当时全仓恰好没有违规文件，所以"全仓扫描通过"照样绿。

**一个只在没有违规时被执行的检查，无法证明它会拦住违规。**

补的是端到端测试：构造真实违规文件，走真正的入口 `main()`，断言返回 1。

### 顺带记：一个"照着猜想构造的样本"只能验证猜想

我最初以为判据是"全角括号后面紧跟引号"，于是拿
`Write-Log "压缩失败（$msg）"` 当样本去证明 —— 断言失败，
因为那一行里括号前面的字节数正好让引号活了下来。
真规律是字节奇偶性，跟"是什么标点"毫无关系。

---

## 三、`rate_limit.py` 的注释否认了它自己在做的事

### 现象

`backend/app/rate_limit.py` 的模块 docstring 写着
「`RATE_LIMIT_*` 三个配置项没有任何代码读取、限流从未实现」，
**而紧接其下的 100 多行就是在读它们**，并且 `main.py:288` 真的把它装上了。

### 根因：一个只匹配一层目录的 glob

当时用 `Select-String -Path "app\**\*.py"` 搜了一遍，0 命中，就据此下了结论。

实测：那个 glob 在 PowerShell 里**只匹配一层目录** ——
递归 `rglob("*.py")` 有 **117** 个文件，它只看到 **66** 个，
漏掉的 51 个恰好是全部顶层模块（`rate_limit.py`、`main.py`、`auth.py`、
`config.py`、`db.py`…），也就是**要找的那个文件自己**。

这个错结论的连锁后果不止那段注释：`SECURITY.md` 的 §4.2 / §8.3 / §10.4 / §10.5
都被照它改成了"❌ 未实现"，之后才逐条实测纠正回来。

### 为什么值得单独立门禁

**一个文件的注释否认自己的实现，是最难被发现的一类错**：
读代码的人先读注释，读完就不往下看了。它也是那次误判的起点。

新增 `TestRateLimitDocstringMatchesItsOwnCode`，**从两侧钉**：

- **不许出现否认句**（"从未实现"/"没有任何代码读取"/"计划实现"/"尚未实现"…）。
  逐行豁免标记 `denial-quote-ok` 只给"引用当年那句错话"的那一行 ——
  引用和主张是两回事，但豁免必须逐行显式、可 grep 审计
  （与本仓 `terminology-ok` 同一套做法）。**豁免的粒度就是漏洞的大小。**
- **必须出现肯定句**（"实现本体"、"由 `main.py` 通过 `add_middleware` 装上"），
  且这句话由另一条测试用真实中间件对象验证过，不是自说自话。
- 必须保留事故成因：那条**具体命令**、"只匹配一层"、实测数字 **117/66**、
  `rglob` 替代做法。
- 必须区分**入站**限流（本文件）与**出站**限流（`collectors/rate_limiter.py`）——
  仓里两套同名机制方向相反，不写清会有人改错文件。

### 只禁止否认句是不够的（变异测试证明）

把肯定句换成「本文件计划实现限流」之后，**全部测试仍然绿**
（当时否认词表里没有"计划实现"）。

**补词表能挡住已知说法，挡不住下一种措辞。** 所以要两侧都钉：
既要求没有否认句，也要求有肯定句。

### 另一个被咬了两次的坑：同一个词在两处出现

想钉住"成因不能被删掉"，第一版写 `"glob" in doc` —— 但"不用 shell glob"
那句里也有 glob，删掉成因整句照样绿。改成 `"0 命中" in doc` 后仍然存活，
因为下面那条教训里也写着"0 命中"。

**钉"某个词出现过"永远不够**，要钉只可能出自那一句的内容
（具体命令名、"只匹配一层"、实测数字），或者显式要求出现次数
（`doc.count("0 命中") >= 2`，一次叙述 + 一次教训）。

---

## 四、pre-commit 钩子的触发条件漏了

四型判据管 `.ps1/.psm1/.bat/.cmd`，但 pre-commit 的 `files` 正则只写了
`ps1|bat` —— **判据是对的，触发条件漏了**：新增一个 `.psm1` 脚本时
钩子根本不会被触发。

已补齐，并加了一条测试从 pre-commit 配置**反向核对**判据清单
（双向登记表的另一半）。

定位钩子必须**整行精确匹配**：变异测试把 id 改成 `check-encoding-DISABLED`
（在 pre-commit 里等于停用这个钩子），原来的子串匹配照样命中、测试全绿。
**子串匹配会把"改了名字的东西"当成原来那个。**

---

## 本轮的验证记录（实际跑过的命令）

| 检查 | 结果 |
|---|---|
| `pytest tests/test_encoding_mojibake.py` | **40 passed** |
| `pytest tests/test_security_doc_parity.py` | **33 passed** |
| 完整后端套件（含 CI 的三个 `-W error::`） | **2847 passed / 6 skipped**，覆盖率 **88.38%**，35m42s |
| 变异测试（四型门禁 + docstring 门禁 + pre-commit 门禁） | 共 **24** 个变异，全部变红；字节级还原 `RESTORED True` |
| `ruff check .` / `ruff format --check .`（cwd=backend） | 通过 / **261 files** |
| `mypy app --config-file pyproject.toml` | **117 源文件**无问题 |
| `scripts/check_encoding.py --strict` | **501 文件**全部正常，exit 0 |
| `scripts/check_terminology.py --all` | exit 0 |
| 前端 `tsc --noEmit` / `node test.mjs` | exit 0 / **20 passed, 0 failed** |
| `auto_backup.ps1` 两个场景实跑 | 均符合预期（见上） |

**变异测试这一轮抓到 5 个假绿**（判据没接主流程、Python codec 当真值、
只禁否认句不要求肯定句、同词两处出现 ×2、子串匹配认错改名的钩子）。
这五个如果没做变异，全部都会以"本地绿"的形式交付出去。

---

## 已合并 / 待处理

- **PR #17 已合并**（merge commit `9a6d81b`），18 项检查全绿。
- 当前在分支 `fix/encoding-type4-bom`，7 个文件改动待提交推 PR。

## 需要所有者决定的事（一直挂着，我不擅自动）

这些都不是"没做完"，而是**需要你拍一个方向**：

1. **Python 版本口径不一致**：Docker 用 3.12、CI 用 3.12、mypy 配 3.12，
   本地 venv 是 3.11.9，而 `pyproject.toml` 写 `>=3.11`。统一到哪个？
2. **上线前应该锁的两个接口**：`/api/v1/collections` 和
   `PATCH /api/v1/projects/{id}/funding` 现在**匿名 token 就能调**
   （实测返回 200，真跑了一次采集）。要不要加进管理员白名单？
3. **生产环境两个开关**：`SEED_FALLBACK_ENABLED` 和 `SEED_ON_STARTUP`
   现在都是 `True`，上线前建议都关掉。
4. **`LLM_DAILY_BUDGET_USD` 是个装饰品**：会在两个接口里显示出来，
   但**从不拦截**。实现它，还是删掉这个配置项？
5. **域名白名单**（SECURITY.md §10.2）：一直只是设计意图。
   要实现（必须用已纠正的那张表，否则 Galxe 和 RootData 会被误挡），
   还是正式降级为"人工审查清单"？
6. **Agent 工具权限**（`allowed_tools`）：实现还是删掉这条要求？
7. `metrics` 数据表是死的（0 行、除一处外无写入方）：接上还是删掉？
8. 生产要不要关掉 `/docs`、`/redoc`、`/openapi.json`？
9. nginx 把 `/metrics` 公开代理出去了、没有任何访问限制，要不要加？
10. 生产 compose 里 `DB_PATH=/app/data/app.db` 在 Windows 上会解析到
    仓库外的 `D:\app\data\app.db`。
11. **完全没有日志轮转**（`logs/backend.log` 6 天涨到 3.97 MB）。
