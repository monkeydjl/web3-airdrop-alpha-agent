# 2026-08-24

> 这一天很长，分了几轮。前半段是编码四型 + 备份脚本（PR #17），
> 后半段是所有者一次性拍了三个方向，我照着做完（PR #19、#20，以及第三项）。
> 下面先记后半段（今天的主要工作），前半段附在文末「附：上午那一轮」。

---

## 这一天真正学到的一条

**「一个能填但什么也不做的配置键，比缺一个更坏。」**

这句话今天被三次验证，且每次的伪装方式都不一样：

| 配置键 | 伪装方式 | 为什么难发现 |
|---|---|---|
| `SEED_ON_STARTUP` | 完全没人读 | grep 得 0 命中，其实最容易查 |
| `SEED_FALLBACK_ENABLED` | 读了、生效了，但文档只写"生产**建议**关" | 建议的执行率不可观测 |
| `LLM_DAILY_BUDGET_USD` | **被读了 3 处，全是回显** | grep 有命中，看着像实现了 |

最后那个是今天最主要的工作。它的教训单独写：
**判据必须落在"有没有人在累计花费"上，而不是"有没有人读这个配置"。**
没有累计就无从超限，而"有 3 处引用"这个信号会让检查者停下来。

---

## 一、所有者拍的三个方向（都已做完）

### 决策 1：匿名可调的两个写接口 → 全部锁成管理员专用

`POST /api/v1/collections`（真跑一次采集）与
`PATCH /api/v1/projects/{id}/funding` 此前匿名 token 就能调，实测返回 200。

已合并 **PR #19**（merge commit `ad9fd19`）。做法是在 `app/auth.py` 里加
`ADMIN_ONLY_METHOD_RULES` —— 按 (方法, 路径) 而不是只按前缀，
因为这两条路径的 GET 是要对匿名开放的，只有写操作要锁。

顺带记一条排查经验：**403 比 404 更能把人骗住。**
`re-score` 在管理员前缀里，匿名打过去拿 403，读起来像"接口存在，只是我权限不够"，
所以文档里那条错记录一直没人发现 —— 用管理员凭据打才露出真相：404。

### 决策 2：生产配置全部按生产标准改

已合并 **PR #20**（merge commit `978e6606`）。三件事：

1. **生产环境强制关闭两个种子开关**，配置里写 `true` 也不生效，
   并写回字段本身，所以 `/api/v1/settings/config` 回显的就是真实生效值 ——
   不留"配置说开着、实际关着"的落差。
2. **日志轮转**（`LOG_MAX_BYTES` / `LOG_BACKUP_COUNT`）+ compose 全 11 个服务
   加 `max-size` —— 两条无界写入路径都堵上。
3. 清理 OPERATIONS §11 里三条假记录，并新增 §11.1 记录"移出去了什么"。

**强制修正 vs 拒绝启动，判据是这个**：只有一个正确的生产值时就强制修正
（种子开关），值必须由人提供时才拒绝启动（API key、域名）。

为什么种子开关值得动这么大：**静默的错误状态比明确的空状态坏得多。**
采集全线挂掉时，种子数据会让系统看起来在正常工作。

日志轮转为什么自己写 `_RotatingLogStream` 而不用标准库的 `RotatingFileHandler`：
**日志渲染路径每多一条，脱敏就多一处可能漏掉的地方。**
另外 `write()` 里绝不抛异常 —— **磁盘满是慢性问题，请求 500 是即时问题。**

### 决策 3：`LLM_DAILY_BUDGET_USD` → 真正实现拦截（不是删掉）

这是今天工作量最大的一项，细节见下一节。

所有者选实现而非删除的理由站得住：这个系统确实需要一个成本上限，
而在此之前 `/api/v1/run` 的频率限制（LLM 开启时 1 次/小时）是**唯一**的成本闸门 ——
**它管的是请求频率，不是花了多少钱。**

还有一个更隐蔽的连带问题：`API_SPEC.md` 里写着
"这两条 LLM 路径不按角色锁，成本由预算门统一拦截"——
**这是把一个鉴权决策的理由，建立在一个尚不存在的控制上。**
这类句子最危险，因为读者看到"由预算门拦截"就会认为成本已被管住。

---

## 二、预算拦截：成本估算有一种特有的失效方式

**算错会被发现，算成 0 不会。**

成本永远 0 → 日累计永远 0 → 预算永不超 → 拦截逻辑写了、测了、跑着，
效果和没写一样，**而且比没写更坏，因为文档会说它在保护你**。

三条静默归零的路径，逐条堵掉：

| 路径 | 不处理的后果 | 处理方式 |
|---|---|---|
| 模型不在价格表里 | 换个模型名就等于关掉预算 | 走 `LLM_FALLBACK_PRICE_PER_1M_USD`（新配置，默认 10.0），且配成 0/负数时仍有非零下限 |
| 接口不返回 `usage` | 缺 usage 就当 0 token | 按字符估（2 字符/token，中文偏保守），记账标记为 `estimated_tokens` |
| 金额在 SQL 里累加漂移 | 预算判断那个 `>=` 不再可信 | 账本存**纳美元整数** |

一律取向**宁可高估**：兜底价偏高、缺 usage 按保守字符数估、纳美元换算向上取整。
高估 → 少花钱 + 一条明确日志；低估 → 账单。

### 第三行是测试当场抓到的，不是预防性设计

第一版账本用 REAL 存美元，Python 侧全程 `Decimal`。
测试第一次跑就红了：`0.1 + 0.2` 存回来是 `0.30000000000000004`。

**教训：`Decimal` 只保证"读出来的类型对"。累加发生在 SQL 的 UPSERT 里，
Python 的类型管不住那个加号。**

改成纳美元整数存储后才真正精确。单位选纳而不是微，是因为一次便宜调用约
1.5e-5 美元，微美元下会被舍成个位数 —— **任何舍到 0 的路径都回到"成本静默归零"。**

### 三个诚实的边界（都写进文档，免得被当 bug 排查）

1. **软上限。** 拦截在调用前、成本在调用后才知道，所以最后一次被放行的调用
   必然把当日花费推过预算线，超出量上界 = 单次调用成本。
   配置 1.0 而账单 1.003 是正常的，这是"事前拦截 + 事后计费"的必然结果。
2. **价格表是手工维护的近似值**，会过时。用途是"够准地估出能触发熔断的量级"，
   不是账单核对。
3. **账本读不出来时拒绝调用（fail closed）。**
   理由不是"安全优先"这种口号，而是这个系统的具体形状：LLM 是可选增强、
   规则引擎是永远可用的默认路径（ADR-001），拒绝一次 LLM 的代价 ≪ 放行一次超预算的账单。

### 两个结构性决定

- **按进程计的预算不是预算** → 账本必须落库。内存计数在重启时归零，
  而"花超了"恰好最可能伴随重启；多 worker 时每个进程各记一份、每份都没超，
  合起来是 N 倍预算。
- **累加必须 UPSERT 单语句。** 先 SELECT 再 UPDATE 在并发下丢记账
  （两个请求都读到 1.0，都写成 1.5，实际应是 2.0），
  而路由处理器跑在线程池上 —— 这是真实存在的并发，不是理论风险。

### 顺带修掉一个"注册了但从不递增"的指标

`airdrop_llm_requests_total` / `_errors_total` / `_duration_seconds`
三个指标从注册那天起**从未被递增过一次**。它们注册了、暴露在 `/metrics` 里、
文档记录了、还有一条 `HighLLMErrorRate` 告警建立在其上。

**一个存在但永不增长的指标，在面板上是平直的 0 线、在告警里是永不触发，
两者看起来都像"系统很健康"。这比指标名写错更坏** ——
名字写错时查询查不到数据，还有机会被发现。

---

## 三、两条 CI 门禁原本会**阻止**这次修复

`test_operations_doc_parity.py` 和 `test_security_doc_parity.py` 里各有一条门禁，
断言"预算拦截必须**仍然不存在**"—— 当初写它们是为了保护文档里
「只展示不拦截」那句话的真实性，这个动机是对的。

但**一个断言"这个功能必须仍然缺失"的测试，在功能实现后就成了反向的假绿：
它会让 CI 拒绝真正的修复。**

两条都改成断言拦截真的接在调用路径上，而且**在发请求之前**
（顺序反了就是先花钱再检查，预算形同虚设）。

这类门禁以后写的时候要想清楚：它记录的是"当前事实"还是"应有状态"。
记录事实的门禁在事实变化时必须一起转向，而转向的时机就是有人来修这个功能的时候——
所以门禁的失败信息里必须写清"如果你是来实现它的，请改这条测试"。
原来那两条写了这句话，所以我一眼就知道该怎么办；这个习惯值得保持。

---

## 四、这一轮我自己犯的两个错（都是假绿）

### 错 1：门禁的解析器切错了对象，给出一个完全错误的诊断

新写的"拦截必须在发请求之前"那条断言，第一版直接在整个 `client.py` 里
找 `await _try_single(` 的位置 —— 命中的却是 `_RawCompletion` docstring 里
举例用的那一行（第 169 行），远在真正的调用点之前。
于是断言失败，报告"预算检查在调用之后"。

**解析器出错时的表现（断言失败）和被测对象真的有问题长得一模一样。**
如果我当时相信了这个报告，会去"修"一个根本不存在的问题。

修法：先切出 `llm_chat` 函数体，并断言切片长度合理（解析器自检）。
这跟这个仓里反复出现的"解析器必须大声失败"是同一条。

### 错 2：禁词禁得太宽，误伤了正确的句子

写了一条门禁禁止 SECURITY.md 出现"装饰性配置"，结果打中了
`DUNE_API_KEY` 那一行 —— 它**确实**还是装饰性配置，那句话没错。

**一个过宽的禁词会把正确的句子也判成错的，然后逼人去改一句本来对的话。**
改为只检查提到 `LLM_DAILY_BUDGET_USD` 的那几行。

### 还有一个反向的坑：纠错清单里不能写真名字

OBSERVABILITY §3.3 和 OPERATIONS §12.1 是两份"这些指标不存在"的清单，
而它们**整块会被门禁当作真值核对**。我想在里面加一句"这两个已经移出了"，
写上真实指标名 —— CI 立刻变红。

这正是它该做的事。所以移出记录只能描述、不能写名字。

---

## 五、顺手修了一个「降级说错话」的问题

预算做完之后发现一个连带缺口：项目解读接口回退到规则引擎时，
响应里只有 `mode: "rule"`，而回退原因有**四种** ——
没配密钥、日预算用完、账本故障、接口全挂。

四种长得完全一样，于是前端对所有降级一律显示「当前未配置大模型密钥」。
**在密钥配好、只是预算耗尽的时候，这句话是错的，而且会让人去查密钥
（那里没问题），真正的问题在预算。**

这个缺口有意思的地方在于：它是**预算功能做成之后才产生**的。
在预算不生效的年代，"降级"只可能因为没配密钥或接口挂了，那句文案基本够用。
**一个新控制上线，会让旧的错误提示从"够用"变成"误导"。**

新增 `degraded_reason` 字段，四个取值各给一句"该怎么办"。
顺带发现 `llm_chat_simple()` 只返回文本，`refused_reason` 在那一层就丢了 ——
改回用 `llm_chat()`。

变异测试 4 个全抓到，其中一个专门盯**"后端算对了但路由没透传"**：
前 4 条断言测的是服务函数的返回值，而前端读的是 HTTP 响应体，
中间隔着一层字段拼装。路由里漏一行，那 4 条断言全绿而前端什么都拿不到。
**判据必须落在最终交付物上。**

---

## 六、又抓到一处虚构的文档

核对 `ai-brief` 接口时发现 API_SPEC 里那段响应示例是编的：
写着 `brief` / `generated_at` 两个字段，实测**都不存在**
（正文在 `display_text`，而且根本没有生成时间字段）。

**一个字段名写错的文档，产生的是空白页面，不是错误信息。**
照文档写的前端拿到 `undefined`，渲染成空白，没有任何报错 ——
所以这种错能活很久。

同时更正：`GET /ai-brief` 在代码里就是 POST 的别名，**每次都重新生成、
每次都花额度**，不是"读缓存"。这一条值得单独写，因为
「GET 安全、无副作用」是个很强的默认预期，而这里它不成立。

---

## 七、我自己踩的一个新坑：用 PowerShell 改 UTF-8 文件

想批量把测试里的一处调用改个写法，用了
`Get-Content -Raw | ... | Set-Content -Encoding UTF8` ——
文件里所有中文变成乱码，Python 直接语法错误。

原因：PS 5.1 的 `Get-Content` **不带 `-Encoding` 时按 ANSI（GBK）解码**，
UTF-8 的中文被解成乱码字符，再按 UTF-8 编码写回，损坏就固化了。

**这和今天上午那个坑是同一个根源的两面**：上午是 PowerShell 按 GBK
解码 `.ps1` 导致代码静默失效，这次是我改文件时踩了同一个默认值。
我当时只记住了结论"脚本要加 BOM"，没记住更一般的那条：
**PowerShell 5.1 读写文本的默认编码不是 UTF-8。**

还试图写脚本逆向还原（cp936 往返），失败两次：先是 `€`（0x80）cp936
编不回去，改成 cp1252 优先后又遇到私用区字符 `U+E749` ——
**乱码不总是可逆的。** 最后用 `git checkout --` 拿回原文件重写了一遍。

以后的做法：文本替换一律用文件编辑工具，不走 shell 管道。

---

## 八、文档改动的一条原则（今天用了三次）

**移出清单要留痕。直接删行是最省事也最糟的做法 —— 读者分不清"修好了"和
"被悄悄拿掉了"。**

今天从"未实现/不存在"清单里移出四条，每条都留了记录：
- OPERATIONS §11.1 从三条扩到五条
- OPERATIONS §12.1、OBSERVABILITY §3.2 各加一段移出说明
- SECURITY §10.7 那条测试清单项改成已覆盖、§11 那行改成"已补齐实现"

背后的道理：**清单的可信度是有限资源。一条假行会让读者怀疑其余每一行。**
而且两个方向的错误代价不对称但都真实 ——
把已实现写成未实现 → 有人重做一遍，或放弃一个可用的控制；
把未实现写成已实现 → 有人在风险评估里数进一个不存在的控制。

---

## 九、三个部署脚本：同一个失效形态的三种写法

PR #21 合并（merge commit `fd224fb`）之后接着修了 `scripts/` 下的三个 shell 脚本。
它们各带一个缺陷，**没有一个会报错**：

| 脚本 | 你会看到什么 | 真实原因 |
|---|---|---|
| `deploy.sh` | 「服务启动超时」，65 秒后失败 | 健康检查打的是 8000，服务在 8002 上，早起来了 |
| `deploy.sh` | 完全正常，无任何输出异常 | **5 行 `sed 's/X/X/'`，把 X 替换成 X，零个生效** |
| `deploy.sh` | 「✅ .env 已创建」→ 60 秒后「启动超时」 | 模板里 `API_KEY` / `AUTH_TOKEN_SECRET` 是空的，生产自检拒绝启动，容器 CrashLoop |
| `health-check.sh` | 一个健康的系统被**持续报成不健康** | 默认 `API_URL` 是 8000。而这是给监控告警用的脚本 |
| `backup.sh` | **「✅ 备份成功！」** | 复制的是 `data/airdrop.db` —— 94 项目的过期副本（真库 288） |
| `backup.sh` | **「✅ 备份成功！」** | 找不到库时"跳过并继续"，压缩包里只剩一个 `backup-info.txt` |

**共同点不是功能缺失，而是把真实原因掩盖成另一个原因。**
照原来的 `deploy.sh` 跑，你会去查容器日志、查依赖、查数据库 ——
而问题在脚本自己那一行。这类错误最贵的地方不是它失败，是它**指错方向**。

`backup.sh` 那条最严重：**备份的失败方式里最坏的一种，就是它看起来成功了。**
你不会去查一个报告成功的备份，直到需要恢复的那天。

### 一个新判据：不猜文件名

`backup.sh` 原来有两级猜测（`data/airdrop.db` → `backend/data/airdrop.db`）。
新版本只认 `.env` 里的 `DB_PATH`，找不到就 `exit 1`。
道理是：**猜中一个过期副本比找不到更坏** —— 前者会给你一份看起来成功的、
没用的备份，后者至少会让你停下来。

同理把 `cp` 换成 `sqlite3 .backup`：`cp` 一个正在被写入的 SQLite 文件
可能拿到撕裂快照，而它**照样能被打开**，只是内容不一致。又一种"看起来成功"。

### 生产预检：为什么是"停下"而不是"自动补"

新增四项启动前预检（`APP_ENV` / `API_KEY` 长度 ≥32 / `AUTH_TOKEN_SECRET` 非空 /
`CORS_ORIGINS` 不含 localhost），不通过就 `exit 1` 并说清要填什么。
**不自动生成密钥。**

这跟 `config.py` 里那条判据是同一条：
**能推断出唯一正确值的（种子开关）强制改，推断不出的（密钥、域名）拒绝启动。**
自动塞一个密钥进去，会让一个配错的生产环境**看起来部署成功了**。

### 这一轮我又写出三个假绿判据（变异测出来的）

12 个变异跑下来，前两轮各漏 2 个，全是我自己的门禁写错：

1. **拿中文段落标题当锚点** —— `assert "生产配置预检" in text`。
   改个标题这条就静默跳过了。改成盯实际控制流：`PRECHECK_FAILED=0` 的位置
   必须在 `up -d` 之前，且区间内必须有 `exit 1` 和一次结果读取。
   **锚点选在能被无痛改掉的字符串上，等于没有锚点。**
2. **只断言 `PRECHECK_FAILED` 出现过** —— 把初始化行删掉、只留置位行，
   断言照样绿，而脚本已经坏了（未定义变量参与数值比较）。
   改成三样都要有：初始化、置位、读取。
3. **`ledger_error` 只在注释里出现也算通过** —— 这个文件的注释本来就解释了
   为什么要看 `ledger_error`。把代码里的检查全删掉、只留注释，断言全绿。
   **注释提到 ≠ 代码在做。今天第三次栽在"词出现过就算实现了"上。**

还加了一个**预期存活**的变异（改段落标题，门禁不该拦）——
光测"改坏了会不会被拦"不够，还得测"无害重构不会被误拦"。
一个把标题当锚点的门禁两个方向都错：改标题假红，真挪走预检假绿。

### 我手写的 `if`/`fi` 计数器错了两次，两次都报出不存在的问题

临时校验脚本里想数关键字配平，两版都错：

- 第一版 `(?:^|\s)fi(?:\s|$)` —— 分隔符被消耗掉，连续两行 `    fi` + `fi`
  里第二个数不到，少一个。
- 第二版改用零宽断言后又画蛇添足减掉 `elif` 的数量，可 `(?<![\w-])if`
  本来就不匹配 `elif` 里的 `if`（前面是 `l`）。多减了。

两次都报出「`backup.sh` if 与 fi 不配平」这个**完全不存在的问题**，
而脚本一直是配平的。**解析器出错时的表现，和被测对象真有问题长得一模一样。**
这是今天第三次踩这个坑（前两次：`index()` 命中 docstring 示例、
`index("---")` 被表格分隔线截断）。

结论写进测试文件了：**能用真解析器的时候就别自己写一个。**
CI 跑在 Linux 上，直接 `bash -n`。

### 本地跑不了，这一点没有含糊过去

这台 Windows 机器上 bash 完全不可用：Git bash 报
`CreateFileMapping ... Win32 error 5`，WSL 报 `E_ACCESSDENIED`。
Docker 守护进程也连不上。所以：

- `bash -n` 语法校验只在 CI（Linux）上跑，本地自动 skip（实测 3 skipped）。
- 其余 20 条门禁（端口、空操作 sed、预检控制流、CRLF/BOM、账本检查）本地能跑。
- **首次在真 Linux 环境跑 `deploy.sh prod` 仍需人工盯一遍。**

静态门禁能证明它不再犯已知的那几个错，**不能证明它在真容器里跑得通**。
这句话写进了 `docs/OPERATIONS.md` §3.4，没有说成"已验证可用"。

### 又移出两条过宽的历史检查

`docs/GO_LIVE_REPORT.md` 9.1「备份脚本存在 PASS」—— 只核对了文件存在，
而当时那个脚本会备份过期副本。改成 ⚠️ 标注并写清重跑时该改成什么判据
（核对备份产物里真的有 `app.db` 且行数与线上一致）。

**移出清单要留痕**，这是今天第八次用这条原则。

---

## 十、`docs/DEPLOYMENT.md`：一份没有一条命令能跑通的部署文档

修完脚本顺手核对了它们的配套文档，结果发现比脚本严重得多 ——
**照 `DEPLOYMENT.md` 做，没有一步能成功**：

- `python run.py` —— 这个文件不存在
- `docker build ... .` —— Dockerfile 在 `docker/` 下，不在根目录
- compose 服务名写 `web` —— 真名 `backend`（`docker compose logs web` 直接报错）
- `frontend/index.html` —— 不存在，前端是 Next.js
- 四个环境变量名（`CRON_HOUR`/`CRON_MINUTE`/`DEFILLAMA_BASE`/`TWITTER_BEARER`）全是错的
- 四个指标名（`run_total`/`run_errors`/`analyze_latency_seconds`/`db_write_errors`）全不存在
- `/health` 的响应体结构写错（说有 `data` 包装、`db:"connected"`，实测扁平 + `db:"ok"`）
- CI 一节是**示例 YAML**，而真实 CI 有 6 个作业 + 4 个工作流
- 「MVP 无迁移需求，V2 引入 Alembic」—— Alembic 早已启用，4 个版本

最坏的三条，按危害排序：

1. **`-v ./data:/app/backend/data`** —— 真实挂载点是 `/app/data`。
   挂错位置**不会报任何错**，容器跑得好好的，直到容器重建那天数据没了。
2. **四个错的环境变量名** —— 填了不报错也不生效。
   这就是今天那条主线的又一次出现：**一个填了不生效的配置键，比缺一个更坏。**
   缺的会被发现，填错的会让人以为自己配好了。
3. **四个幻影指标名** —— 照它建的面板是空的、告警永不触发。
   **空面板和"系统很健康"长得一模一样。**

### 为什么重写而不是打补丁

改到第五处的时候意识到：错的比对的多。
**清单的可信度是有限资源 —— 一条假行会让读者怀疑其余每一行**，
而这份文档已经过了那个临界点。

新版每个数字都实测过：28 张表、39 个指标、4 个 Alembic 版本、
380 行 `.env.example`、三个 compose 服务名与容器名、`/health` 的真实响应体、
`x-logging` 锚点覆盖 11 个服务、CI 的 `PYTHON_VERSION: "3.12"`。

### 一个刻意的取舍：环境变量一节不再复制全表

上一版把环境变量抄成一张表，然后那张表长出了四个不存在的键。
新版只列启动必看的十几项，**其余指向 `.env.example` 作为唯一权威**
（那份有 `test_env_example_parity.py` 守着，与 `config.py` 的默认值不允许漂移）。

理由写进文档里了：**复制出来的表会过期，而一个过期的配置表比没有表更坏** ——
它会让人填一个不存在的键，然后以为自己配好了。
这正是上一版第 2 条错误的成因，不能重复一遍。

### 顺带查出来两个此前没记过的事实

- `API_PORT` 与 `NGINX_PORT` **不在 `.env.example` 里**，
  只在 compose 的 `${VAR:-默认值}` 里出现。要改宿主端口得自己加这两行。
- **只有生产 compose 有 logging 驱动限制**（`x-logging` 锚点，11 个服务）；
  开发 `docker-compose.yml` 完全没配，本地日志由 docker 默认驱动无界累积。
  这一条以前笼统写成"compose 里 11 个服务都加了"，实际是两个文件的差别。

§12 整节留痕记下上一版错在哪 —— 今天第九次用「移出清单要留痕」这条原则。

---

## 十一、归档功能：把"从没跑过"的原因查错了

`archive_runs` 表 0 行，这个事实之前记过好几次，但一直沿用一个**错的解释**：
「数据还没超过 30 天保留期，所以每次触发都无事可做」。

今天拿线上库副本（用 `sqlite3.backup()` 复制，不动线上库）真跑一次，
半分钟就推翻了这个解释：

> `run_and_record()` 即使一行都没归档，**也会写下一条 `status=success` 的记录**。
> 实测 `raw_archived=0`，`archive_runs` 照样 +1，
> 内容 `{'id': 1, 'trigger': 'manual', 'status': 'success', 'raw_archived': 0, ...}`。

所以 0 行只可能是**一次都没被触发过**。本机后端是手工起停的开发进程，
从没在 UTC 03:00（本地 11:00）那一刻活着，`misfire_grace_time=3600` 也没兜住。
**跟保留期毫无关系。**

### 为什么这个错解释比"不知道"更坏

「无事可做」和「从没触发」的处置动作完全相反：
前者是"再等等就好"，后者是"这条路径的调度部分从未被验证"。
写成前者，就没有人会去手工验证 —— 一个自我实现的盲区。

而且这个错解释**听起来很合理**，还有数据支撑（数据确实只到 08-09，
确实不够 30 天）。它错的地方不在事实，在**推理方向**：
从"数据不够老"推不出"所以没记录"，中间少了一步"空转会不会留痕"。
这一步我一直没查，因为答案看起来是显然的。

### 顺带把逻辑本身验了

同一份副本上把保留期全设 0 天再 dry-run：

| 分项 | 命中 |
|---|---|
| `raw_processed` | 106 |
| `raw_unprocessed` | 509 |
| `signals` | 2261 |
| `logs` | 20 |

且 dry-run 后所有表行数不变（这一条也是实测的，不是看代码推的）。
**所以待验证的只剩"调度会不会按时把它叫起来"这一段** ——
范围从"整个归档功能未验证"缩小到"一个 cron 触发未验证"。

### 加的门禁

`test_run_with_nothing_to_archive_still_records_a_row` —— 锁住"空转也留痕"。
如果哪天有人"优化"成"没活干就不写记录"，`archive_runs = 0` 会重新变成二义的，
而它是运维唯一的可观测入口（`GET /api/v1/archive/runs` 的 `summary.total_runs`）。

这个门禁保护的不是功能，是**一个诊断信号的无二义性**。

### 中间一个小插曲

探针脚本第一版 `from app.archive import ArchivePolicy` —— 类名我猜的，
真名是 `RawDataArchiver`，`ImportError` 立刻报出来。
查列名时同样：猜 `collected_at` / `created_at`，真名是
`discovered_at` / `captured_at`，`no such column` 立刻报出来。

两次都是**猜错了立刻失败**，代价接近零。
今天大部分时间栽的都是另一类：**猜错了静默通过**。
这个对比值得记 —— 我该主动把判据往"猜错会立刻失败"那一侧设计。

---

## 十二、回滚步骤里少了一个 Alembic 版本

`OPERATIONS.md` §3.5 写「只有 3 个版本」，实际 4 个 —— 我自己当天加的
`0004_llm_spend_daily` 忘了回来改这句话。

**这条的危害在于它出现的位置**：回滚步骤是出事时照着做的，那时没人核对。
而 `alembic downgrade -1` **不会因为文档写错而报错** ——
它老老实实退一步，只是运维以为自己退到了别的位置。

顺手补了一句以前完全没写的东西：回滚到 `0003` 之前会丢掉花费账本表，
预算拦截转为 **fail-closed**（拒绝所有 LLM 调用），**不是静默放行**。
这个方向必须写清楚，否则运维会以为回滚后 LLM 照常工作。

### 门禁写了两条，因为一条不够

- `test_documented_migration_count_matches_reality` —— 数字对齐。
- `test_every_migration_file_is_named_in_the_doc` —— **逐个核对文件名**。

第二条是必要的：**4 个版本里换掉一个，数字还是 4**。
只对数量的门禁在"改名"这类变异下是假绿的 —— 变异测试里专门放了这一项，确认它会被拦。

顺便让锚点失效时**报「解析不到」而不是静默通过** ——
这是今天反复出现的那个教训的第 N 次应用。

---

## 十三、我今天连续三次废掉了完整套件的运行

完整后端套件跑 38 分钟。今天启动了三次，**前两次都被我自己废掉**：
跑到一半又去改 `OPERATIONS.md` §7.3、又去改
`test_operations_doc_parity.py` 的 docstring、又跑了一次 `ruff format`。

pytest 在启动时收集文件，**中途改源文件不会报错**，
它会给你一个看起来正常的 38 分钟结果 —— 而那个结果对应的是**新旧混合的源码**，
既不是改前也不是改后。这种结果最坏的地方是它**看起来完全正常**，
既不报错也不警告，只是没有意义。

（今天这条主线的第 N 次出现：**不产生错误信号的错误最贵。**）

### 改法不是"下次注意"，是改工作顺序

新规则：**完整套件只在改动全部落定后跑，一次。**
中途想到要改什么，先记下来，不要动文件。

代价对比很清楚：白跑一次 = 38 分钟；
一次可信的运行 = 38 分钟。差别只在于跑之前有没有停手。

短反馈的检查（单个测试文件、ruff、mypy、encoding）该随时跑 —— 它们秒级返回，
中途改文件的风险窗口小到可以忽略。**长任务和短任务需要不同的纪律**，
把这两类混在一个习惯里就是今天这个结果。

---

## 十四、Python 版本口径 —— 原来以为要问你，实测后发现是 bug

这条本来在"待所有者决定"清单里，写着"统一到哪个版本，两种选择代价不同"。
真去测了一下，发现根本不是取舍问题，是一个**无人看守的缺口**。

`backend/pyproject.toml` 的 mypy 配的是 3.12，而 `requires-python` 声明
`>=3.11`。拿 `itertools.batched`（3.12 才有的标准库函数）做探针：

- `mypy --python-version 3.12` → 通过（放过了）
- `ruff --target-version py311` → 通过（它只管语法，管不了标准库）
- 真 3.11.9 解释器 → 直接 `AttributeError`

也就是说，如果哪天有人用了个 3.12 新 API，全套 CI 会放它过，
然后在 3.11 上运行时才炸 —— 而且报错完全不提"是版本问题"。
`requires-python = ">=3.11"` 这句话此前**没有任何东西在守**。

改法不用问你：**检查器按最低支持版本（3.11）来查，运行镜像照旧用 3.12。**
在新解释器上按旧版本查是"多拦一点"，安全；反过来才漏。
真要放弃 3.11、只支持 3.12，那该改的是 `requires-python` 那行，
不是把检查器放松到跟镜像一样 —— 那等于把承诺偷偷改小。

加了 8 条门禁盯着这套口径不再漂移，8 个变异都验过会拦。

### 附带查出一个死配置

根目录 `pyproject.toml` 里那份 `[tool.mypy]`（`strict = true`，很严）
**根本没在跑**。CI 是进 `backend/` 目录、指定用 backend 那份配置跑的，
根那份谁都没读。真按它跑是 373 个错误。这条我没动，记在 PHASES.md 里 ——
要不要把整个后端提到 strict 是另一件大事，值得单独拍板。

---

## 十五、安全文档 §6.3 又抓出两处"说了没做"

镜像安全那一小节三条里错了两条：说基础镜像是 3.11（实际 3.12）、
说"固定了 digest"（实际没有）。第三条"用 Trivy/Grype"里的 Grype 也不存在。

**为什么盯安全文档的这类错**：安全文档里写"已经做了 X"，
读的人会直接把 X 算进防护里。"固定了 digest"意味着"基础镜像不会
在我不知情时偷偷变" —— 而实际用的是会被上游重新推送的普通 tag。
按文档做风险评估的人，会整整漏掉一类供应链风险。

还补了一件以前完全没提的事：镜像扫描开了 `ignore-unfixed`，
意思是"上游还没出补丁的高危漏洞，不算 CI 失败"。这个设置本身合理
（拿没法修的漏洞挡住合并，只会逼人干脆关掉扫描），但它让"CI 绿"
的含义变了 —— 绿只代表"能修的高危都修了"，不代表"镜像干净"。
不写出来，绿灯会被当成"没问题"。

### 两次自我纠错，都是老坑复发

修这一节时门禁两次拦住我自己：一次是我写"仓库里没有 Grype"这句话，
被判成"你点名了一个不存在的扫描器"（描述错误的话和犯错的话长得一样，
这已经是第 N 次）；一次是版本号门禁我第一版写松了，"至少有一处写对"
就算过 —— 可读者看到的偏偏是错的那处。两次都靠变异测试兜住了，
说明"写完门禁必须拿变异验一遍它真会拦"这条纪律是有用的。

---

## 本轮的验证记录（实际跑过的命令）

| 检查 | 结果 |
|---|---|
| `pytest tests/test_llm_budget_enforcement.py` | **35 passed**（新写） |
| `pytest tests/test_ai_brief.py` | **7 passed**（新增 5 条降级原因用例） |
| `pytest tests/test_shell_scripts.py`（新写，20 条） | **20 passed / 3 skipped**（skip = 本机无 bash） |
| `pytest tests/test_shell_scripts.py + test_deployment.py` | **49 passed / 4 skipped** |
| `pytest tests/test_operations_doc_parity.py`（改了 §3.4 / §12.4 / §12.5 后） | **40 passed** |
| `DEPLOYMENT.md` 重写后的 7 个相对链接 | **全部可解析** |
| `DEPLOYMENT.md` 里每个数字逐项实测 | 28 表 / 39 指标 / 4 迁移 / 380 行 env / 3 服务名 / `/health` 真实响应体 |
| **线上库副本上真跑归档**（`sqlite3.backup()` 复制，不动线上库） | dry-run 零改动；真跑写下 `archive_runs` 第 1 行；0 天保留期命中 106/509/2261/20 |
| `pytest tests/scripts/test_archive.py`（新增空转留痕断言） | **29 passed** |
| `pytest tests/test_operations_doc_parity.py`（新增迁移数量 2 条） | **42 passed** |
| **变异测试：本轮新门禁 5 个（4 该拦 + 1 反向）** | **0 个结果与预期不符** |
| **完整后端套件**（改动全部落定后跑的那一次） | **2991 passed / 9 skipped，88.70% 覆盖，42m22s，exit 0** |
| `mypy app`（改成 python_version=3.11 后） | Success，119 source files |
| `pytest tests/test_toolchain_version_parity.py`（新增） | **8 passed** |
| **变异测试：工具链版本门禁 8 个（7 该拦 + 1 反向）** | **0 个结果与预期不符** |
| `pytest tests/test_security_doc_parity.py`（新增 §6.3 四条） | **38 passed** |
| **变异测试：镜像安全门禁 8 个（6 该拦 + 2 反向）** | **0 个结果与预期不符**（其中 2 条是修好我自己的门禁后才过的） |
| 四份 doc-parity 套件合跑 | **117 passed / 1 skipped** |
| `pytest` 四份 parity 套件 + env_example | **149 passed** |
| `pytest test_admin_only_rules + production_hardening + api_spec_parity + env_example_parity` | **82 passed**（4m01s） |
| `pytest test_data_source_strategy_parity + utf8_repair_tools + encoding_mojibake` | **95 passed / 2 skipped** |
| **变异测试：预算 11 个 + 降级原因 4 个，全部被抓到（0 漏）** | 见下 |
| **变异测试：部署脚本 12 个，结果全部符合预期（11 该拦的拦住 + 1 无害重构确认不误拦）** | 见「九」 |
| 端到端实跑 `/api/v1/llm/status`（真 TestClient + 匿名 token） | 200，字段齐全；记一笔账后 `spend_today_usd` 跟着变；**删掉账本表后返回 `null` + `ledger_error`，不是 0** |
| 账本 SQL 累加精度实测 | `0.1234 + 0.1 + 0.2` → 纳美元 `423400000`，**与期望精确相等** |
| `ruff check .` / `ruff format --check`（cwd=backend） | All checks passed / 242 files |
| `mypy app --config-file pyproject.toml` | **119 源文件**无问题 |
| `scripts/check_encoding.py --strict` | **508 文件**全部正常 |
| `scripts/check_terminology.py --all` | exit 0 |
| 前端 `tsc --noEmit` / `eslint` / `node test.mjs` | 0 / 0 / **20 passed, 0 failed** |
| 完整后端套件（含 CI 的三个 `-W error::`） | 见交接说明 |

**预算的 11 个变异**：移除 `check_budget` 调用、成本估算恒为 0、兜底价降为 0、
边界 `>=` 改 `>`、账本失败改 fail open、不再记账、缺 usage 时 token 估 0、
账本改浮点存、以及三个指标的递增点各删一个。

**降级原因的 4 个变异**：预算拒绝报成 `llm_error`、所有失败都报成
`budget_exceeded`（反向）、没配密钥报成 `llm_error`、路由不透传字段。

最关键的一条断言是**超预算时零次网络请求** ——
一个正确的 `check_budget()` 加上一个忘了调用它的 `llm_chat()`，
会让所有单元断言全绿而预算毫无作用。**判据必须接进入口。**
反向断言同样必要：只测"超了被拦"分不清"拦对了"和"全都拦了"。

**变异脚本自己也出过一次错，值得记**：有个变异的锚点
`    return (None, "llm_error")`（4 空格缩进）是 except 块里那行（8 空格）的
**子串**，于是变异实际打在了错误的位置，测试照样绿，脚本报出
"这个变异没被抓到"这个**错误结论**。
**锚点必须整行匹配** —— 这和上午 pre-commit 钩子那次"子串匹配认错改名的钩子"
是同一个错误的第二次出现。

---

## 下一步

决策三项已合并（PR #19 / #20 / #21，最后一个 merge commit `fd224fb`）。
本轮在分支 `fix/deploy-scripts-real-values` 上，攒了四件事：
部署三脚本、DEPLOYMENT.md 重写、归档诊断纠错、Python 版本口径 + 镜像安全。
完整套件已跑完（**2991 passed，88.70%，42m22s，exit 0**）→ 提交 → 开 PR → 合并。

**「刚把门修成真的，不该由我第一个绕过去」** —— 即使是纯文档改动也走 PR。

### 两条遗留风险（都记明确了，不靠记性）

1. **`deploy.sh prod` 从未在真 Linux 环境跑过**（本机 bash 与 Docker 都不可用）。
   首次真跑需人工盯一遍。
2. **归档调度从未触发过一次**。逻辑本身已在线上库副本上验证，
   但 cron 那条路没走过 —— 见待决第 9 条。

### 下一轮该继续的（不需要你拍板的部分）

- `/ops` 页面还缺一个后端接口：`UnifiedScheduler.get_jobs()` 已经存在
  且返回每个任务的 `next_run_time`，但**没有任何路由暴露它**。
  这正是"归档从没跑过"当初查不出来的原因 —— 运维台看不到任务表。
  打算加一个只读的管理员接口把它露出来。
- 完整套件那次运行早于新加的两个测试文件，CI 会是最终判据。

---

## 还需要所有者决定的事（今天已消掉 5 条，剩这些）

1. ~~**Python 版本口径不一致**~~ → **已修，不用你拍**。实测后发现它不是
   "选哪个版本"的取舍，而是 `requires-python = ">=3.11"` 没有任何门在守
   （详见 §十四）。规则定为"检查器按下限 3.11、运行时用 3.12"，
   加了 8 条门禁。真要放弃 3.11 只支持 3.12 才需要你拍 —— 那时改的是
   `requires-python` 那一行。
2. **域名白名单**（SECURITY §10.2）：实现（必须用已纠正的那张表，
   否则 Galxe 和 RootData 会被误挡），还是正式降级为"人工审查清单"？
3. **Agent 工具权限**（`allowed_tools`）：实现还是删掉这条要求？
   现在 §10.7 里那条测试写了必挂。
4. `metrics` 数据表是死的（0 行、除一处外无写入方）：接上还是删掉？
5. 生产要不要关掉 `/docs`、`/redoc`、`/openapi.json`？
6. nginx 把 `/metrics` 公开代理出去了、无任何访问限制，要不要加？
7. 生产 compose 里 `DB_PATH=/app/data/app.db` 在 Windows 上会解析到
   仓库外的 `D:\app\data\app.db`。
8. 要不要删掉那 8 个真正没人读的 Settings 字段（`app_name`、
   `competition_cache_ttl`、`cryptorank_retry`、`dune_enabled`、
   `rootdata_retry`、`seed_data_path`、`seed_on_startup`、`twitter_api_secret`）？
9. **归档要不要现在手工跑一次**（`scripts/archive_raw_data.py`）？
   逻辑已在线上库副本上验证（0 天保留期命中 106/509/2261/20），
   但**调度从未触发过一次**。首次真实执行留给生产 vs 现在手工验证一次。
   我倾向后者，但这会真的移动数据，所以问一句。
10. **要不要把后端提到 mypy strict**？根 `pyproject.toml` 里那份
    `[tool.mypy]`（`strict = true`）是**死配置** —— CI 读的是 backend 那份，
    根那份谁都没读。真按它跑是 **373 个错误**。
    两条路：把根那份删掉（承认只用宽松口径），或者立项把 373 个改完。
    我没擅自选，因为前者是缩小承诺、后者是一大笔工作量。

**已消掉**：匿名写接口（决策 1）、两个种子开关（决策 2）、
日志轮转（决策 2）、`LLM_DAILY_BUDGET_USD`（决策 3）、Python 版本口径（§十四）。

---
---

# 附：上午那一轮（编码四型 + 备份脚本，PR #17 已合并 `9a6d81b`）

一句话：**修备份脚本时踩进一个"改完跑成功、实际什么都没做"的坑，
根因是文件编码而不是代码逻辑；由此发现并封堵了第四型编码损坏，
顺带修掉一段"注释否认自己实现"的 docstring。**

那一轮的三件事有一条共同的线：**「跑通了」和「做对了」是两件事。**

- 备份脚本 exit 0 说"备份成功"，实际一个文件都没生成。
- 编码门禁的判据写得完全正确，但没接进主流程，删掉它 39 条测试全绿。
- `rate_limit.py` 的注释说限流从未实现，而它自己就是实现本体。

三件都不是"功能缺失"，而是**"看起来没问题"**——
这类问题不会被人发现，因为它们不产生任何报错信号。

## A. 备份脚本"跑成功了"，实际什么都没备份

修完空目录问题后跑验证：脚本输出正常、返回 exit 0、日志写着"备份成功"，
但盘上没有任何产物。开 `Set-PSDebug -Trace 1` 看轨迹，
发现**执行从第 20 行直接跳到第 153 行** —— 中间 130 多行一行都没跑，
而语法解析器报 **0 个错误**。

### 根因：不在代码，在文件编码

Windows PowerShell 5.1 在文件**没有 UTF-8 BOM** 时，
按系统 ANSI 代码页（简体中文机器 = **GBK/936**）解码脚本。
GBK 是双字节编码，**任何 ≥ 0x80 的字节都无条件吃掉紧随其后的一个字节** ——
包括 ASCII 引号。

而一个 UTF-8 中文字符是 **3 字节（奇数）**，于是"结束引号会不会被吃掉"
取决于**它前面有多少字节的中文**：

| 字符串 | 字节数 | 结束引号 |
|---|---|---|
| `"中"` | 3（奇） | **被吃掉** |
| `"中文"` | 6（偶） | 保留 |
| `"中文字"` | 9（奇） | **被吃掉** |
| `"中文字符"` | 12（偶） | 保留 |

引号一被吃掉，字符串就不闭合，把后面几十行代码全吃进一个字符串字面量 ——
**而且语法完全合法**，所以不报错、不警告，只是静默不执行。

原脚本能正常跑是因为它原本是 **UTF-16LE**；我重写时存成 UTF-8 无 BOM 就踩了进去。

### 为什么这是最恶劣的一类问题

**这条奇偶性意味着「今天没事」不等于安全。** 同一个文件今天跑得好，
明天在某行日志里加一个字，奇偶性翻转，几十行代码就静默失效。
所以判据不可能是"检查有没有某种危险写法"，只能是"**这个文件必须有 BOM**"。

### 修法

`scripts/check_encoding.py` 新增**第四型**：`.ps1` / `.psm1` / `.bat` / `.cmd`
只要含任何非 ASCII 字节，就必须带 UTF-8 BOM。

**零豁免、零登记清单** —— 前三型的登记清单是历史包袱（原字符不可逆丢失），
四型没有这个属性：修法是加 3 个字节。
**一旦有豁免清单，"先登记着"就会变成默认选项。**
这一点本身也写成了测试（`test_bom_registry_does_not_exist`）。

为什么不改成"脚本里不许写中文"：那是把成本转嫁给可读性，而且挡不住。

### 四型与前三型的性质差别

前三型是"**文件内容坏了**"。四型是"**文件内容完全正确，是读它的人读坏的**"。
它证明了「文件内容合法」和「文件被正确读取」是两件不同的事，而前三型只查了前者。
这也是为什么四次"以为查完了"之后又查出一种：
**检测判据的盲区就是损坏的藏身处。**

## B. 核对四型时踩的两个坑（都比原问题更值得记）

### 坑 1：用 Python 的 gbk codec 去验证 .NET 的行为

同一串字节 `EF BC 89 22`（`）` + `"`）：

| 解码器 | 结果 |
|---|---|
| .NET cp936（PowerShell 实际用的） | 引号**被吃掉** |
| Python `gbk` codec + `errors="replace"` | 引号**保留** |

原因：前导字节后跟非法尾字节时策略不同 —— .NET **宽容**（无条件消费 2 字节），
Python **严格**（抛 `UnicodeDecodeError`，`replace` 只消费那 1 个前导字节）。

**拿 Python 去"验证" .NET 会得到相反的结论。**
我第一版测试就是这么写的，而且当时"通过"了 —— 因为那个样本恰好两边一致。

固化的原则：**验证用的解码器必须和被验证的解码器是同一个。**
既然 CI 上跑的是 Python，真值就只能是实测记录下来的表（`_DOTNET_CP936_KEPT_QUOTES`）。

### 坑 2：判据写对了，但没接进主流程

变异测试把 `main()` 里那个 BOM 检查分支整段删掉 —— **39 条测试全绿。**

原因：只测了判据函数和全仓扫描，而当时全仓恰好没有违规文件。
**一个只在没有违规时被执行的检查，无法证明它会拦住违规。**

补的是端到端测试：构造真实违规文件，走真正的入口 `main()`，断言返回 1。

（这一条今天下午又用上了一次 —— 预算那边的"超预算时零次网络请求"是同一个道理。）

### 顺带记：照着猜想构造的样本只能验证猜想

我最初以为判据是"全角括号后面紧跟引号"，拿
`Write-Log "压缩失败（$msg）"` 当样本去证明 —— 断言失败，
因为那一行里括号前面的字节数正好让引号活了下来。
真规律是字节奇偶性，跟"是什么标点"毫无关系。

## C. `rate_limit.py` 的注释否认了它自己在做的事

模块 docstring 写着「`RATE_LIMIT_*` 三个配置项没有任何代码读取、限流从未实现」，
**而紧接其下的 100 多行就是在读它们**，`main.py:288` 也真的把它装上了。

### 根因：一个只匹配一层目录的 glob

当时用 `Select-String -Path "app\**\*.py"` 搜了一遍，0 命中，据此下了结论。
实测那个 glob 在 PowerShell 里**只匹配一层目录** ——
递归 `rglob("*.py")` 有 **117** 个文件，它只看到 **66** 个，
漏掉的 51 个恰好是全部顶层模块，也就是**要找的那个文件自己**。

连锁后果不止那段注释：`SECURITY.md` 的 §4.2 / §8.3 / §10.4 / §10.5
都被照它改成了"❌ 未实现"，之后才逐条实测纠正回来。

### 为什么值得单独立门禁

**一个文件的注释否认自己的实现，是最难被发现的一类错**：
读代码的人先读注释，读完就不往下看了。

新增 `TestRateLimitDocstringMatchesItsOwnCode`，**从两侧钉**：
不许出现否认句 + 必须出现肯定句 + 必须保留事故成因（具体命令、
"只匹配一层"、实测数字 117/66）+ 必须区分入站/出站两套同名机制。

### 只禁止否认句是不够的（变异测试证明）

把肯定句换成「本文件计划实现限流」之后，**全部测试仍然绿**
（当时否认词表里没有"计划实现"）。
**补词表能挡住已知说法，挡不住下一种措辞。** 所以要两侧都钉。

### 另一个被咬了两次的坑：同一个词在两处出现

想钉住"成因不能被删掉"，第一版写 `"glob" in doc` —— 但"不用 shell glob"
那句里也有 glob。改成 `"0 命中" in doc` 后仍然存活，因为教训段里也写着"0 命中"。

**钉"某个词出现过"永远不够**，要钉只可能出自那一句的内容，
或者显式要求出现次数（`doc.count("0 命中") >= 2`）。

## D. pre-commit 钩子的触发条件漏了

四型判据管 `.ps1/.psm1/.bat/.cmd`，但 pre-commit 的 `files` 正则只写了
`ps1|bat` —— **判据是对的，触发条件漏了**。已补齐，并加了一条测试从
pre-commit 配置**反向核对**判据清单（双向登记表的另一半）。

定位钩子必须**整行精确匹配**：变异测试把 id 改成 `check-encoding-DISABLED`
（等于停用），原来的子串匹配照样命中、测试全绿。
**子串匹配会把"改了名字的东西"当成原来那个。**

## 上午那一轮的验证记录

| 检查 | 结果 |
|---|---|
| `pytest tests/test_encoding_mojibake.py` | **40 passed** |
| 完整后端套件（含三个 `-W error::`） | **2847 passed / 6 skipped**，覆盖率 **88.38%**，35m42s |
| 变异测试（四型 + docstring + pre-commit 三组门禁） | **24** 个变异全部变红；字节级还原 `RESTORED True` |
| `auto_backup.ps1` 两个场景实跑 | Docker 未运行 → exit 1 且不留空目录；预置 2 空 + 1 非空目录 → 空的被回收、非空完好 |

**那一轮变异测试抓到 5 个假绿**（判据没接主流程、Python codec 当真值、
只禁否认句不要求肯定句、同词两处出现 ×2、子串匹配认错改名的钩子）。
如果没做变异，全部都会以"本地绿"的形式交付出去。
