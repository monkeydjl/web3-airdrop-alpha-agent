# 2026-08-24

> 这一天很长，分了几轮。前半段是编码四型 + 备份脚本（PR #17），
> 后半段是所有者一次性拍了三个方向，我照着做完（PR #19、#20，以及第三项）。
> 下面先记后半段（今天的主要工作），前半段附在文末「附：上午那一轮」。

---

## 这一天真正学到的一条

**「一个能填但什么也不做的配置键，比缺一个更坏。」**

这句话今天被三次验证，且每次的伪装方式都不一样：

| 配置键 | 伪装方式 | 为什么难发现 |
|---|---|---|
| `SEED_ON_STARTUP` | 完全没人读 | grep 得 0 命中，其实最容易查 |
| `SEED_FALLBACK_ENABLED` | 读了、生效了，但文档只写"生产**建议**关" | 建议的执行率不可观测 |
| `LLM_DAILY_BUDGET_USD` | **被读了 3 处，全是回显** | grep 有命中，看着像实现了 |

最后那个是今天最主要的工作。它的教训单独写：
**判据必须落在"有没有人在累计花费"上，而不是"有没有人读这个配置"。**
没有累计就无从超限，而"有 3 处引用"这个信号会让检查者停下来。

---

## 一、所有者拍的三个方向（都已做完）

### 决策 1：匿名可调的两个写接口 → 全部锁成管理员专用

`POST /api/v1/collections`（真跑一次采集）与
`PATCH /api/v1/projects/{id}/funding` 此前匿名 token 就能调，实测返回 200。

已合并 **PR #19**（merge commit `ad9fd19`）。做法是在 `app/auth.py` 里加
`ADMIN_ONLY_METHOD_RULES` —— 按 (方法, 路径) 而不是只按前缀，
因为这两条路径的 GET 是要对匿名开放的，只有写操作要锁。

顺带记一条排查经验：**403 比 404 更能把人骗住。**
`re-score` 在管理员前缀里，匿名打过去拿 403，读起来像"接口存在，只是我权限不够"，
所以文档里那条错记录一直没人发现 —— 用管理员凭据打才露出真相：404。

### 决策 2：生产配置全部按生产标准改

已合并 **PR #20**（merge commit `978e6606`）。三件事：

1. **生产环境强制关闭两个种子开关**，配置里写 `true` 也不生效，
   并写回字段本身，所以 `/api/v1/settings/config` 回显的就是真实生效值 ——
   不留"配置说开着、实际关着"的落差。
2. **日志轮转**（`LOG_MAX_BYTES` / `LOG_BACKUP_COUNT`）+ compose 全 11 个服务
   加 `max-size` —— 两条无界写入路径都堵上。
3. 清理 OPERATIONS §11 里三条假记录，并新增 §11.1 记录"移出去了什么"。

**强制修正 vs 拒绝启动，判据是这个**：只有一个正确的生产值时就强制修正
（种子开关），值必须由人提供时才拒绝启动（API key、域名）。

为什么种子开关值得动这么大：**静默的错误状态比明确的空状态坏得多。**
采集全线挂掉时，种子数据会让系统看起来在正常工作。

日志轮转为什么自己写 `_RotatingLogStream` 而不用标准库的 `RotatingFileHandler`：
**日志渲染路径每多一条，脱敏就多一处可能漏掉的地方。**
另外 `write()` 里绝不抛异常 —— **磁盘满是慢性问题，请求 500 是即时问题。**

### 决策 3：`LLM_DAILY_BUDGET_USD` → 真正实现拦截（不是删掉）

这是今天工作量最大的一项，细节见下一节。

所有者选实现而非删除的理由站得住：这个系统确实需要一个成本上限，
而在此之前 `/api/v1/run` 的频率限制（LLM 开启时 1 次/小时）是**唯一**的成本闸门 ——
**它管的是请求频率，不是花了多少钱。**

还有一个更隐蔽的连带问题：`API_SPEC.md` 里写着
"这两条 LLM 路径不按角色锁，成本由预算门统一拦截"——
**这是把一个鉴权决策的理由，建立在一个尚不存在的控制上。**
这类句子最危险，因为读者看到"由预算门拦截"就会认为成本已被管住。

---

## 二、预算拦截：成本估算有一种特有的失效方式

**算错会被发现，算成 0 不会。**

成本永远 0 → 日累计永远 0 → 预算永不超 → 拦截逻辑写了、测了、跑着，
效果和没写一样，**而且比没写更坏，因为文档会说它在保护你**。

三条静默归零的路径，逐条堵掉：

| 路径 | 不处理的后果 | 处理方式 |
|---|---|---|
| 模型不在价格表里 | 换个模型名就等于关掉预算 | 走 `LLM_FALLBACK_PRICE_PER_1M_USD`（新配置，默认 10.0），且配成 0/负数时仍有非零下限 |
| 接口不返回 `usage` | 缺 usage 就当 0 token | 按字符估（2 字符/token，中文偏保守），记账标记为 `estimated_tokens` |
| 金额在 SQL 里累加漂移 | 预算判断那个 `>=` 不再可信 | 账本存**纳美元整数** |

一律取向**宁可高估**：兜底价偏高、缺 usage 按保守字符数估、纳美元换算向上取整。
高估 → 少花钱 + 一条明确日志；低估 → 账单。

### 第三行是测试当场抓到的，不是预防性设计

第一版账本用 REAL 存美元，Python 侧全程 `Decimal`。
测试第一次跑就红了：`0.1 + 0.2` 存回来是 `0.30000000000000004`。

**教训：`Decimal` 只保证"读出来的类型对"。累加发生在 SQL 的 UPSERT 里，
Python 的类型管不住那个加号。**

改成纳美元整数存储后才真正精确。单位选纳而不是微，是因为一次便宜调用约
1.5e-5 美元，微美元下会被舍成个位数 —— **任何舍到 0 的路径都回到"成本静默归零"。**

### 三个诚实的边界（都写进文档，免得被当 bug 排查）

1. **软上限。** 拦截在调用前、成本在调用后才知道，所以最后一次被放行的调用
   必然把当日花费推过预算线，超出量上界 = 单次调用成本。
   配置 1.0 而账单 1.003 是正常的，这是"事前拦截 + 事后计费"的必然结果。
2. **价格表是手工维护的近似值**，会过时。用途是"够准地估出能触发熔断的量级"，
   不是账单核对。
3. **账本读不出来时拒绝调用（fail closed）。**
   理由不是"安全优先"这种口号，而是这个系统的具体形状：LLM 是可选增强、
   规则引擎是永远可用的默认路径（ADR-001），拒绝一次 LLM 的代价 ≪ 放行一次超预算的账单。

### 两个结构性决定

- **按进程计的预算不是预算** → 账本必须落库。内存计数在重启时归零，
  而"花超了"恰好最可能伴随重启；多 worker 时每个进程各记一份、每份都没超，
  合起来是 N 倍预算。
- **累加必须 UPSERT 单语句。** 先 SELECT 再 UPDATE 在并发下丢记账
  （两个请求都读到 1.0，都写成 1.5，实际应是 2.0），
  而路由处理器跑在线程池上 —— 这是真实存在的并发，不是理论风险。

### 顺带修掉一个"注册了但从不递增"的指标

`airdrop_llm_requests_total` / `_errors_total` / `_duration_seconds`
三个指标从注册那天起**从未被递增过一次**。它们注册了、暴露在 `/metrics` 里、
文档记录了、还有一条 `HighLLMErrorRate` 告警建立在其上。

**一个存在但永不增长的指标，在面板上是平直的 0 线、在告警里是永不触发，
两者看起来都像"系统很健康"。这比指标名写错更坏** ——
名字写错时查询查不到数据，还有机会被发现。

---

## 三、两条 CI 门禁原本会**阻止**这次修复

`test_operations_doc_parity.py` 和 `test_security_doc_parity.py` 里各有一条门禁，
断言"预算拦截必须**仍然不存在**"—— 当初写它们是为了保护文档里
「只展示不拦截」那句话的真实性，这个动机是对的。

但**一个断言"这个功能必须仍然缺失"的测试，在功能实现后就成了反向的假绿：
它会让 CI 拒绝真正的修复。**

两条都改成断言拦截真的接在调用路径上，而且**在发请求之前**
（顺序反了就是先花钱再检查，预算形同虚设）。

这类门禁以后写的时候要想清楚：它记录的是"当前事实"还是"应有状态"。
记录事实的门禁在事实变化时必须一起转向，而转向的时机就是有人来修这个功能的时候——
所以门禁的失败信息里必须写清"如果你是来实现它的，请改这条测试"。
原来那两条写了这句话，所以我一眼就知道该怎么办；这个习惯值得保持。

---

## 四、这一轮我自己犯的两个错（都是假绿）

### 错 1：门禁的解析器切错了对象，给出一个完全错误的诊断

新写的"拦截必须在发请求之前"那条断言，第一版直接在整个 `client.py` 里
找 `await _try_single(` 的位置 —— 命中的却是 `_RawCompletion` docstring 里
举例用的那一行（第 169 行），远在真正的调用点之前。
于是断言失败，报告"预算检查在调用之后"。

**解析器出错时的表现（断言失败）和被测对象真的有问题长得一模一样。**
如果我当时相信了这个报告，会去"修"一个根本不存在的问题。

修法：先切出 `llm_chat` 函数体，并断言切片长度合理（解析器自检）。
这跟这个仓里反复出现的"解析器必须大声失败"是同一条。

### 错 2：禁词禁得太宽，误伤了正确的句子

写了一条门禁禁止 SECURITY.md 出现"装饰性配置"，结果打中了
`DUNE_API_KEY` 那一行 —— 它**确实**还是装饰性配置，那句话没错。

**一个过宽的禁词会把正确的句子也判成错的，然后逼人去改一句本来对的话。**
改为只检查提到 `LLM_DAILY_BUDGET_USD` 的那几行。

### 还有一个反向的坑：纠错清单里不能写真名字

OBSERVABILITY §3.3 和 OPERATIONS §12.1 是两份"这些指标不存在"的清单，
而它们**整块会被门禁当作真值核对**。我想在里面加一句"这两个已经移出了"，
写上真实指标名 —— CI 立刻变红。

这正是它该做的事。所以移出记录只能描述、不能写名字。

---

## 五、顺手修了一个「降级说错话」的问题

预算做完之后发现一个连带缺口：项目解读接口回退到规则引擎时，
响应里只有 `mode: "rule"`，而回退原因有**四种** ——
没配密钥、日预算用完、账本故障、接口全挂。

四种长得完全一样，于是前端对所有降级一律显示「当前未配置大模型密钥」。
**在密钥配好、只是预算耗尽的时候，这句话是错的，而且会让人去查密钥
（那里没问题），真正的问题在预算。**

这个缺口有意思的地方在于：它是**预算功能做成之后才产生**的。
在预算不生效的年代，"降级"只可能因为没配密钥或接口挂了，那句文案基本够用。
**一个新控制上线，会让旧的错误提示从"够用"变成"误导"。**

新增 `degraded_reason` 字段，四个取值各给一句"该怎么办"。
顺带发现 `llm_chat_simple()` 只返回文本，`refused_reason` 在那一层就丢了 ——
改回用 `llm_chat()`。

变异测试 4 个全抓到，其中一个专门盯**"后端算对了但路由没透传"**：
前 4 条断言测的是服务函数的返回值，而前端读的是 HTTP 响应体，
中间隔着一层字段拼装。路由里漏一行，那 4 条断言全绿而前端什么都拿不到。
**判据必须落在最终交付物上。**

---

## 六、又抓到一处虚构的文档

核对 `ai-brief` 接口时发现 API_SPEC 里那段响应示例是编的：
写着 `brief` / `generated_at` 两个字段，实测**都不存在**
（正文在 `display_text`，而且根本没有生成时间字段）。

**一个字段名写错的文档，产生的是空白页面，不是错误信息。**
照文档写的前端拿到 `undefined`，渲染成空白，没有任何报错 ——
所以这种错能活很久。

同时更正：`GET /ai-brief` 在代码里就是 POST 的别名，**每次都重新生成、
每次都花额度**，不是"读缓存"。这一条值得单独写，因为
「GET 安全、无副作用」是个很强的默认预期，而这里它不成立。

---

## 七、我自己踩的一个新坑：用 PowerShell 改 UTF-8 文件

想批量把测试里的一处调用改个写法，用了
`Get-Content -Raw | ... | Set-Content -Encoding UTF8` ——
文件里所有中文变成乱码，Python 直接语法错误。

原因：PS 5.1 的 `Get-Content` **不带 `-Encoding` 时按 ANSI（GBK）解码**，
UTF-8 的中文被解成乱码字符，再按 UTF-8 编码写回，损坏就固化了。

**这和今天上午那个坑是同一个根源的两面**：上午是 PowerShell 按 GBK
解码 `.ps1` 导致代码静默失效，这次是我改文件时踩了同一个默认值。
我当时只记住了结论"脚本要加 BOM"，没记住更一般的那条：
**PowerShell 5.1 读写文本的默认编码不是 UTF-8。**

还试图写脚本逆向还原（cp936 往返），失败两次：先是 `€`（0x80）cp936
编不回去，改成 cp1252 优先后又遇到私用区字符 `U+E749` ——
**乱码不总是可逆的。** 最后用 `git checkout --` 拿回原文件重写了一遍。

以后的做法：文本替换一律用文件编辑工具，不走 shell 管道。

---

## 八、文档改动的一条原则（今天用了三次）

**移出清单要留痕。直接删行是最省事也最糟的做法 —— 读者分不清"修好了"和
"被悄悄拿掉了"。**

今天从"未实现/不存在"清单里移出四条，每条都留了记录：
- OPERATIONS §11.1 从三条扩到五条
- OPERATIONS §12.1、OBSERVABILITY §3.2 各加一段移出说明
- SECURITY §10.7 那条测试清单项改成已覆盖、§11 那行改成"已补齐实现"

背后的道理：**清单的可信度是有限资源。一条假行会让读者怀疑其余每一行。**
而且两个方向的错误代价不对称但都真实 ——
把已实现写成未实现 → 有人重做一遍，或放弃一个可用的控制；
把未实现写成已实现 → 有人在风险评估里数进一个不存在的控制。

---

## 本轮的验证记录（实际跑过的命令）

| 检查 | 结果 |
|---|---|
| `pytest tests/test_llm_budget_enforcement.py` | **35 passed**（新写） |
| `pytest tests/test_ai_brief.py` | **7 passed**（新增 5 条降级原因用例） |
| `pytest` 四份 parity 套件 + env_example | **149 passed** |
| `pytest test_admin_only_rules + production_hardening + api_spec_parity + env_example_parity` | **82 passed**（4m01s） |
| `pytest test_data_source_strategy_parity + utf8_repair_tools + encoding_mojibake` | **95 passed / 2 skipped** |
| **变异测试：预算 11 个 + 降级原因 4 个，全部被抓到（0 漏）** | 见下 |
| 端到端实跑 `/api/v1/llm/status`（真 TestClient + 匿名 token） | 200，字段齐全；记一笔账后 `spend_today_usd` 跟着变；**删掉账本表后返回 `null` + `ledger_error`，不是 0** |
| 账本 SQL 累加精度实测 | `0.1234 + 0.1 + 0.2` → 纳美元 `423400000`，**与期望精确相等** |
| `ruff check .` / `ruff format --check`（cwd=backend） | All checks passed / 242 files |
| `mypy app --config-file pyproject.toml` | **119 源文件**无问题 |
| `scripts/check_encoding.py --strict` | **508 文件**全部正常 |
| `scripts/check_terminology.py --all` | exit 0 |
| 前端 `tsc --noEmit` / `eslint` / `node test.mjs` | 0 / 0 / **20 passed, 0 failed** |
| 完整后端套件（含 CI 的三个 `-W error::`） | 见交接说明 |

**预算的 11 个变异**：移除 `check_budget` 调用、成本估算恒为 0、兜底价降为 0、
边界 `>=` 改 `>`、账本失败改 fail open、不再记账、缺 usage 时 token 估 0、
账本改浮点存、以及三个指标的递增点各删一个。

**降级原因的 4 个变异**：预算拒绝报成 `llm_error`、所有失败都报成
`budget_exceeded`（反向）、没配密钥报成 `llm_error`、路由不透传字段。

最关键的一条断言是**超预算时零次网络请求** ——
一个正确的 `check_budget()` 加上一个忘了调用它的 `llm_chat()`，
会让所有单元断言全绿而预算毫无作用。**判据必须接进入口。**
反向断言同样必要：只测"超了被拦"分不清"拦对了"和"全都拦了"。

**变异脚本自己也出过一次错，值得记**：有个变异的锚点
`    return (None, "llm_error")`（4 空格缩进）是 except 块里那行（8 空格）的
**子串**，于是变异实际打在了错误的位置，测试照样绿，脚本报出
"这个变异没被抓到"这个**错误结论**。
**锚点必须整行匹配** —— 这和上午 pre-commit 钩子那次"子串匹配认错改名的钩子"
是同一个错误的第二次出现。

---

## 下一步

代码与文档已完成，全套本地验证除完整后端套件外都已通过。
剩下：等完整套件跑完 → 提交 → 开 PR → 门禁全绿后合并。

**「刚把门修成真的，不该由我第一个绕过去」** —— 即使是纯文档改动也走 PR。

---

## 还需要所有者决定的事（今天已消掉 4 条，剩这些）

1. **Python 版本口径不一致**：Docker 3.12、CI 3.12、mypy 配 3.12，
   本地 venv 3.11.9，`pyproject.toml` 写 `>=3.11`。统一到哪个？
2. **域名白名单**（SECURITY §10.2）：实现（必须用已纠正的那张表，
   否则 Galxe 和 RootData 会被误挡），还是正式降级为"人工审查清单"？
3. **Agent 工具权限**（`allowed_tools`）：实现还是删掉这条要求？
   现在 §10.7 里那条测试写了必挂。
4. `metrics` 数据表是死的（0 行、除一处外无写入方）：接上还是删掉？
5. 生产要不要关掉 `/docs`、`/redoc`、`/openapi.json`？
6. nginx 把 `/metrics` 公开代理出去了、无任何访问限制，要不要加？
7. 生产 compose 里 `DB_PATH=/app/data/app.db` 在 Windows 上会解析到
   仓库外的 `D:\app\data\app.db`。
8. 要不要删掉那 8 个真正没人读的 Settings 字段（`app_name`、
   `competition_cache_ttl`、`cryptorank_retry`、`dune_enabled`、
   `rootdata_retry`、`seed_data_path`、`seed_on_startup`、`twitter_api_secret`）？

**已消掉**：匿名写接口（决策 1）、两个种子开关（决策 2）、
日志轮转（决策 2）、`LLM_DAILY_BUDGET_USD`（决策 3）。

---
---

# 附：上午那一轮（编码四型 + 备份脚本，PR #17 已合并 `9a6d81b`）

一句话：**修备份脚本时踩进一个"改完跑成功、实际什么都没做"的坑，
根因是文件编码而不是代码逻辑；由此发现并封堵了第四型编码损坏，
顺带修掉一段"注释否认自己实现"的 docstring。**

那一轮的三件事有一条共同的线：**「跑通了」和「做对了」是两件事。**

- 备份脚本 exit 0 说"备份成功"，实际一个文件都没生成。
- 编码门禁的判据写得完全正确，但没接进主流程，删掉它 39 条测试全绿。
- `rate_limit.py` 的注释说限流从未实现，而它自己就是实现本体。

三件都不是"功能缺失"，而是**"看起来没问题"**——
这类问题不会被人发现，因为它们不产生任何报错信号。

## A. 备份脚本"跑成功了"，实际什么都没备份

修完空目录问题后跑验证：脚本输出正常、返回 exit 0、日志写着"备份成功"，
但盘上没有任何产物。开 `Set-PSDebug -Trace 1` 看轨迹，
发现**执行从第 20 行直接跳到第 153 行** —— 中间 130 多行一行都没跑，
而语法解析器报 **0 个错误**。

### 根因：不在代码，在文件编码

Windows PowerShell 5.1 在文件**没有 UTF-8 BOM** 时，
按系统 ANSI 代码页（简体中文机器 = **GBK/936**）解码脚本。
GBK 是双字节编码，**任何 ≥ 0x80 的字节都无条件吃掉紧随其后的一个字节** ——
包括 ASCII 引号。

而一个 UTF-8 中文字符是 **3 字节（奇数）**，于是"结束引号会不会被吃掉"
取决于**它前面有多少字节的中文**：

| 字符串 | 字节数 | 结束引号 |
|---|---|---|
| `"中"` | 3（奇） | **被吃掉** |
| `"中文"` | 6（偶） | 保留 |
| `"中文字"` | 9（奇） | **被吃掉** |
| `"中文字符"` | 12（偶） | 保留 |

引号一被吃掉，字符串就不闭合，把后面几十行代码全吃进一个字符串字面量 ——
**而且语法完全合法**，所以不报错、不警告，只是静默不执行。

原脚本能正常跑是因为它原本是 **UTF-16LE**；我重写时存成 UTF-8 无 BOM 就踩了进去。

### 为什么这是最恶劣的一类问题

**这条奇偶性意味着「今天没事」不等于安全。** 同一个文件今天跑得好，
明天在某行日志里加一个字，奇偶性翻转，几十行代码就静默失效。
所以判据不可能是"检查有没有某种危险写法"，只能是"**这个文件必须有 BOM**"。

### 修法

`scripts/check_encoding.py` 新增**第四型**：`.ps1` / `.psm1` / `.bat` / `.cmd`
只要含任何非 ASCII 字节，就必须带 UTF-8 BOM。

**零豁免、零登记清单** —— 前三型的登记清单是历史包袱（原字符不可逆丢失），
四型没有这个属性：修法是加 3 个字节。
**一旦有豁免清单，"先登记着"就会变成默认选项。**
这一点本身也写成了测试（`test_bom_registry_does_not_exist`）。

为什么不改成"脚本里不许写中文"：那是把成本转嫁给可读性，而且挡不住。

### 四型与前三型的性质差别

前三型是"**文件内容坏了**"。四型是"**文件内容完全正确，是读它的人读坏的**"。
它证明了「文件内容合法」和「文件被正确读取」是两件不同的事，而前三型只查了前者。
这也是为什么四次"以为查完了"之后又查出一种：
**检测判据的盲区就是损坏的藏身处。**

## B. 核对四型时踩的两个坑（都比原问题更值得记）

### 坑 1：用 Python 的 gbk codec 去验证 .NET 的行为

同一串字节 `EF BC 89 22`（`）` + `"`）：

| 解码器 | 结果 |
|---|---|
| .NET cp936（PowerShell 实际用的） | 引号**被吃掉** |
| Python `gbk` codec + `errors="replace"` | 引号**保留** |

原因：前导字节后跟非法尾字节时策略不同 —— .NET **宽容**（无条件消费 2 字节），
Python **严格**（抛 `UnicodeDecodeError`，`replace` 只消费那 1 个前导字节）。

**拿 Python 去"验证" .NET 会得到相反的结论。**
我第一版测试就是这么写的，而且当时"通过"了 —— 因为那个样本恰好两边一致。

固化的原则：**验证用的解码器必须和被验证的解码器是同一个。**
既然 CI 上跑的是 Python，真值就只能是实测记录下来的表（`_DOTNET_CP936_KEPT_QUOTES`）。

### 坑 2：判据写对了，但没接进主流程

变异测试把 `main()` 里那个 BOM 检查分支整段删掉 —— **39 条测试全绿。**

原因：只测了判据函数和全仓扫描，而当时全仓恰好没有违规文件。
**一个只在没有违规时被执行的检查，无法证明它会拦住违规。**

补的是端到端测试：构造真实违规文件，走真正的入口 `main()`，断言返回 1。

（这一条今天下午又用上了一次 —— 预算那边的"超预算时零次网络请求"是同一个道理。）

### 顺带记：照着猜想构造的样本只能验证猜想

我最初以为判据是"全角括号后面紧跟引号"，拿
`Write-Log "压缩失败（$msg）"` 当样本去证明 —— 断言失败，
因为那一行里括号前面的字节数正好让引号活了下来。
真规律是字节奇偶性，跟"是什么标点"毫无关系。

## C. `rate_limit.py` 的注释否认了它自己在做的事

模块 docstring 写着「`RATE_LIMIT_*` 三个配置项没有任何代码读取、限流从未实现」，
**而紧接其下的 100 多行就是在读它们**，`main.py:288` 也真的把它装上了。

### 根因：一个只匹配一层目录的 glob

当时用 `Select-String -Path "app\**\*.py"` 搜了一遍，0 命中，据此下了结论。
实测那个 glob 在 PowerShell 里**只匹配一层目录** ——
递归 `rglob("*.py")` 有 **117** 个文件，它只看到 **66** 个，
漏掉的 51 个恰好是全部顶层模块，也就是**要找的那个文件自己**。

连锁后果不止那段注释：`SECURITY.md` 的 §4.2 / §8.3 / §10.4 / §10.5
都被照它改成了"❌ 未实现"，之后才逐条实测纠正回来。

### 为什么值得单独立门禁

**一个文件的注释否认自己的实现，是最难被发现的一类错**：
读代码的人先读注释，读完就不往下看了。

新增 `TestRateLimitDocstringMatchesItsOwnCode`，**从两侧钉**：
不许出现否认句 + 必须出现肯定句 + 必须保留事故成因（具体命令、
"只匹配一层"、实测数字 117/66）+ 必须区分入站/出站两套同名机制。

### 只禁止否认句是不够的（变异测试证明）

把肯定句换成「本文件计划实现限流」之后，**全部测试仍然绿**
（当时否认词表里没有"计划实现"）。
**补词表能挡住已知说法，挡不住下一种措辞。** 所以要两侧都钉。

### 另一个被咬了两次的坑：同一个词在两处出现

想钉住"成因不能被删掉"，第一版写 `"glob" in doc` —— 但"不用 shell glob"
那句里也有 glob。改成 `"0 命中" in doc` 后仍然存活，因为教训段里也写着"0 命中"。

**钉"某个词出现过"永远不够**，要钉只可能出自那一句的内容，
或者显式要求出现次数（`doc.count("0 命中") >= 2`）。

## D. pre-commit 钩子的触发条件漏了

四型判据管 `.ps1/.psm1/.bat/.cmd`，但 pre-commit 的 `files` 正则只写了
`ps1|bat` —— **判据是对的，触发条件漏了**。已补齐，并加了一条测试从
pre-commit 配置**反向核对**判据清单（双向登记表的另一半）。

定位钩子必须**整行精确匹配**：变异测试把 id 改成 `check-encoding-DISABLED`
（等于停用），原来的子串匹配照样命中、测试全绿。
**子串匹配会把"改了名字的东西"当成原来那个。**

## 上午那一轮的验证记录

| 检查 | 结果 |
|---|---|
| `pytest tests/test_encoding_mojibake.py` | **40 passed** |
| 完整后端套件（含三个 `-W error::`） | **2847 passed / 6 skipped**，覆盖率 **88.38%**，35m42s |
| 变异测试（四型 + docstring + pre-commit 三组门禁） | **24** 个变异全部变红；字节级还原 `RESTORED True` |
| `auto_backup.ps1` 两个场景实跑 | Docker 未运行 → exit 1 且不留空目录；预置 2 空 + 1 非空目录 → 空的被回收、非空完好 |

**那一轮变异测试抓到 5 个假绿**（判据没接主流程、Python codec 当真值、
只禁否认句不要求肯定句、同词两处出现 ×2、子串匹配认错改名的钩子）。
如果没做变异，全部都会以"本地绿"的形式交付出去。
