# 2026-08-21

## 本次做了什么

上一轮（08-20）修完上线阻断项后，我给出了功能建议。用户选了优先级 1 和 2：**打通「行动 → 复盘 → 校准」闭环**。

### 先更正了我自己上一轮的判断

我上一轮说「要加两个功能」，实测后发现**这个说法不准确**：参与清单、结果反馈、校准状态**后端全都已存在**（`participation_tasks.py` 424 行 + `POST /feedback` + `GET /calibration/status`，实测都能用）。真正缺的是另外三件事，都是实测发现的。

### 缺口一（地基）：参与清单的信号全部读不到

**这是本次最关键的发现。** 扩展信号（`has_testnet` / `has_task_portal` / `explicit_airdrop_mention` 等 27 个）存在 `projects.meta.signals` 里，而 `projects` 表**没有这些列**。`generate_participation_tasks()` 直接读顶层键 → 281 个项目的信号判断**恒为 False** → 全部落进「无信号」兜底分支。

实测：最高分项目（Scroll zkEVM, 83 分）也只拿到 5 条与项目无关的通用任务。

修复：新增 `signals_view()` 展平 `meta.signals` 再传入。实测同样 6 个项目产出 **11 种**不同任务组合，单项目任务数从恒定 5 条变为 **5~15 条**。

> **教训**：原有单测传的是**扁平 dict**，所以这个 bug 长期潜伏且测试全绿。补了按真实存储形态断言的回归用例。以后写服务层测试，夹具必须复刻真实存储结构。

### 缺口二：没有跨项目的「今天该做什么」

参与清单此前只在单个项目详情页存在，162 个 FARM 必须逐个点进去。新增 `GET /api/v1/action-queue` 聚合 + 工作台卡片。

两个设计决定：
- **轮转取样**：纯按分数排时 5 个名额只覆盖 3 个项目（实测），改为每项目先出 1 条再回头补第 2 条，5 个名额覆盖 5 个项目
- **只出推进类任务**（official/testnet/mainnet/research/risk/dev）：track/social 每个项目都有，会把清单刷满

完成状态**复用 `interactions` 表**（用户选的方案），不新建状态表。

### 缺口三：校准门禁 200 条，而录入成本让它不可能达到

实测 `feedback` 表 0 条、`samples_needed=199`，校准能力永久空转。新增 `/review` 页 + `POST /feedback/batch` + `GET /feedback/pending-review`：每行三个按钮，选完批量提交。**没有动门禁阈值**，降的是录入成本。

## 自己发现并修掉的 P0（安全）

`POST /feedback/batch` 与既有 `POST /feedback` 一样只需匿名 token。我做穿透测试时发现：**缺少项目存在性校验，一次请求注入 200 条伪造 ID（`ghost-0..199`）就能让 `calibration_ready` 变 True** —— 等于用凭空数据决定真实评分权重。

修复：未知 `project_id` 整批拒绝（404），批量上限从 200 收紧到 50（填满门禁至少需 4 次请求）。前端改为自动分批。

## 独立评审发现的问题（前端 subagent）

**`--brand` CSS 变量从未定义** —— `globals.css` 有 10 处 `rgb(var(--brand))`（5 处是既有代码），但 `:root`/`.dark` 里没这个变量。Tailwind 的 `brand` token 只作用于工具类，不产生 CSS 自定义属性。解析成 `rgb(undefined)` 后颜色被静默丢弃：**结果按钮选中态、进度条填充、hover 边框全都不显示**。按 `--accent` 同值补上，顺带修好 5 处既有失效样式。

评审还指出两处我确实做错的：
- 界面写「整批在同一事务内提交，不会出现部分成功」，但前端是**循环分批**发送，跨批次不原子。已改为如实表述，并修掉「第 2 批失败后重试会重复提交第 1 批」的真实 bug
- `ActionQueue` 的错误只经可选 `onDone` 回调上报，调用方不传时点击失败毫无反馈。已加组件内兜底提示

## 最终验证（全部实跑）

```
pytest -q                    → 2490 passed, 4 skipped, 0 failed（32分55秒，exit 0）
覆盖率                        → 87.85%（门槛 80%）
ruff check / format          → All checks passed / 241 files unchanged
mypy app                     → no issues in 114 source files
前端 tsc / eslint / build     → 全通过，eslint 0 problems，Compiled successfully
端到端 5 步闭环                → 建议 → 标记 → 从清单消失 → 排进待标记 → 校准推进
```

新增测试：`tests/test_action_queue.py`（14）、`tests/api/test_action_and_review.py`（20）、`test_participation_tasks.py` 补 4 个。

## 决定

- **复用 `interactions` 表**而非新建 `action_items`（用户选择）：标记「已做」= 一条交互记录，参与复盘页能看到同一份数据
- **不动校准门禁 200 和评分权重**：那是 ADR-006 / WEIGHT_CALIBRATION 管的，改了历史评分不可比。只降录入成本
- **复用 `generate_participation_tasks()`** 而非重写任务规则：单一事实来源
- **`action-queue` 打分不复用评分决策引擎的权重**：项目价值与「今天该做哪一步」是两回事，混用会让两边都难解释。用线性可解释打分（优先级权重 + 标签 + 分数×0.15 + 必做 + 收藏 − 已参与）
- **批量上限选 50 而非 200**：与门禁同量级会让单次请求填满门禁

## 后续几轮（同日继续）

### 统一 `user_id` 过滤口径

上面「遗留」里记的不一致项，查清了**根因**：两张表的写入约定本来就不同。
实测 `POST /api/v1/interactions` 不传 `user_id` 时落 **NULL**，而
`POST /api/v1/watchlist/{id}` 用 `body.user_id or "default"` 落 **'default'**。

这意味着"统一成严格 `user_id = 'default'`"会**引入真实 bug**：标记过的项目
第二天又出现在今日清单里。抽出 `app/services/user_scope.py`：默认用户同时认
NULL 与 'default'，具名用户严格匹配、不读 NULL。`pending-review` 此前
**完全没有用户过滤**，也补上了。附 8 个测试（含表名白名单拒绝注入字符串）。

### 更正我自己的一条判断：action-queue 不需要缓存

我上面把"无缓存"列为风险。实测后**这条判断是错的**：候选池固定 60，
耗时与库内项目总数无关。端到端中位数 **26ms**（`/dashboard/overview` 是 46ms，
`/projects?limit=20` 是 24ms），纯聚合约 0.04ms/项目。
**刻意不加缓存** —— 缓存会引入失效时机问题（标记"已做"需要立刻反映），
收益只有几毫秒。加了 2 个测试锁死"考察项目数 ≤ 候选池上限"。

### 锁定依赖版本

`requirements.txt` 此前 25 行**全是浮动 `>=`**，等于每次 docker build 都可能
装到与本地测过的不同版本，出问题无法复现。

- 运行时 13 个包全部改精确 `==`，逐包与本机实际安装版本核对一致
- 拆出 `requirements-dev.txt`（7 个，含 ruff/mypy），**不进生产镜像**
- CI 三处散装安装（`pip install ruff` / `pytest pytest-cov` / `mypy`）改为装锁定文件
  —— 散装等于不锁版本，ruff/mypy 小版本间结果会变，这正是"本地绿 CI 红"的来源
- 验证方式：新建干净 venv 只装 `requirements.txt` → 41 个包装成、应用启动、
  `/health` 200、`/metrics` 200；再装 dev 依赖跑 22 个测试通过

**顺带查出的既有问题**：7 个 `opentelemetry-*` 被声明为必需依赖，但
**本机从未安装**、也**没有针对性测试**（`pytest -k "otel or tracing"` 选中 0 个用例）。
实测缺包时应用照常启动（`tracing.py` 降级为 no-op tracer）。已移到可选的
`requirements-otel.txt`。

**刻意没锁 OTel 版本**：本机**无法访问 PyPI**（实测连接被关闭），无从验证。
凭记忆写个版本号会让人以为"已锁定已验证"，实际是未经证实的猜测，比不锁更危险。

### 发现文档编码损坏 1116 处

查 `docs/OPERATIONS.md` 时 read 工具报 `invalid UTF-8`，顺着查出 **3 个文档共
1116 处中文丢字**：每个 3 字节字符的第 3 字节被替换成 `?`，典型的"以非 UTF-8
编码写回文件"。这类损坏**静默** —— 文件照样能提交，只是内容里多了一堆 `?`。
它在 git 历史里潜伏了 3 个提交，且 `DATA_SOURCE_STRATEGY.md` 的**所有历史版本
都已损坏**，git 救不回来。

已定 **629 处（56.4%）**，全部可证明、不含猜测：432 处由 `6823d18` 的干净底本
序列对齐精确恢复，27 处由"该前缀全仓唯一"推断，170 处由上下文规则推断。

**每条上下文规则的准确率都在真实语料上量过。** 被量出 94.4% 的
宽松版括号规则（反例：括号内部本身含逗号）已丢弃并留档。另有一层交叉验证：
规则与底本对齐独立推断同一批位置，105 处可核对、**冲突 0**。

**剩 487 处刻意不修**：前缀对应多个候选，无法证明是哪个。实测 `efbc` 里
`（）：，` 各占 26/25/20/19%，按频率猜错误率接近 3/4。把猜测写进文档比留占位符
更糟 —— 读者无法分辨哪句是原文、哪句是机器编的。已导出受约束选择题清单。

三个工具 + 56 个测试；`check-encoding` 已挂 pre-commit 防复发。
详见 `docs/ENCODING_REPAIR.md`。

### 又查出第二种编码损坏（更隐蔽的一种）

修完一型后我起了个疑问：**会不会还有一种损坏是"合法 UTF-8"因而检查看不见的？**
按这个假设去扫，果然找到了 —— `docs/API_SPEC.md` **70 处**整个中文字符被替换成
一个半角 `?`。文件完全合法，`decode('utf-8')` 毫无问题，一型的检查一个都发现不了。
它在 git 历史里潜伏了 **6 个提交**。

**这一型刻意只检测不修复**，因为实测发现它连一型那点结构约束都没有：
- 没有前 2 字节，候选是**全部汉字**
- 更关键：**一个 `?` 不总是只吃掉一个字符**。逐行对齐底本后，24 处可核对位置里
  恰好 1 个字符的是 **0 处**，多于 1 个的是 **24 处**（比如一个 `?` 吃掉了
  `求/` 两个字符）

既没候选集约束、也没长度约束，任何自动修复都无法用逐字节校验证明没越界。
所以我只把它检测出来、登记、挂进钩子，修复留给人工对照底本重写。

检测判据是"半角 `?` 紧贴中文字符"，并**排除代码块与行内代码** —— mermaid 的
判定节点是正常写法。有意思的是 `check_encoding.py` 自己一度被自己误报：
"描述判据的文字"和"符合判据的损坏"长得一样。新增测试文件也踩了同一个坑两次，
最后改成运行时拼接损坏样本才自洽。

补了 14 个测试，重点全在**防误报**，其中一条断言"全仓不得出现未登记的二型损坏"
—— 把 pre-commit 钩子的效果固化成了测试。

### 又查出第三种编码损坏（这次真的是主动去找的）

二型是靠"会不会有一种损坏因为合法而看不见"这个假设找到的。所以我接着追问：
**那有没有第四、第五种？** 换判据重扫全仓（字面 U+FFFD、中文后连续 `??`、
全仓非 md 文本文件），扫出了**三型**。

`docs/SYSTEM_DIRECTION_CHANGE.md` **2 处**，是两个小节标题的 emoji 被吃掉了
（`## <FFFD> 成功指标（KPI）`、`## <FFFD>️ 实施路线图`；该文档其余标题都带
📋 🔄 🎯 🏗️ 等图标）。成因和前两型不同 —— 是**用 `errors='replace'` 解码后
又写回**，不是编码写错。同样是合法 UTF-8，前两型的检查都看不见它。

**这一型的损失可以忽略**（纯装饰图标，不影响语义），所以它的价值不在损坏本身，
而在证明了一件事：**"我已经查完了"这个判断，本身需要有依据。**
换判据、让工具检查自己、把结论固化成测试 —— 这三样都做了才算查过。

判据反而是三型里最干净的：正常写作绝不会输入 U+FFFD，所以**零误报**，
连代码块都不用排除。处理方式是登记 + 检测防复发，**不写猜测值**
（补哪个 emoji 是猜的；虽然猜错无损失，但那属于内容编辑，不该由修复脚本代劳）。

编码测试从 14 个加到 24 个，其中一条专门断言**三型判据与二型互不重叠** ——
新增一型不能污染既有判据。另在 `docs/ENCODING_REPAIR.md` §6 如实写了
"没有再找第四种形态，但没找到不等于不存在"。

### 顺手查出术语闸门本身是坏的（且没有测试）

收尾跑 `check_terminology.py --all` 时它**直接失败** —— 3 个文件 5 处命中。
这个脚本是防止术语回退的唯一机械闸门，但**它自己一行测试都没有**，
所以坏了也没人知道。

5 处里有 3 处**不该改**：`CLAUDE.md` 那行正在定义禁用词清单本身，
`SESSION_MEMORY_2026-07-26.md` 那行引用的是历史 commit message（改它等于篡改
记录）。所以问题不在文档，在闸门没有"这里必须写出禁用词"的表达手段。

加了**行级豁免**（行尾 `terminology-ok`），刻意做成逐行显式、可 grep 审计，
**不做整文件豁免** —— 否则真的术语回退会藏在被豁免的文件里。
全仓真豁免只有 2 处。另外修掉 1 处真实回退（`DEPLOYMENT_REPORT_FINAL.md:115`）。

补了 27 个测试。有意思的是**测试文件自己一开始被闸门拦下了**（12 处报错）：
它要测禁用术语，就得含禁用术语字面量，而它是 tracked file。
改成运行时拼接（`"评" + "分引擎"`）后自洽，并加了一条断言锁住这个约束。
这和编码检测踩的是同一类坑 —— **"描述规则的文字"和"违反规则的内容"
长得一样**，同一天遇到三次（`check_encoding.py` 的文档字符串、
编码测试文件、术语测试文件）。

### 补上 OTel 降级路径的测试（把假设变成契约）

把 OTel 拆成可选依赖之后我意识到一件事：**"缺包时能降级"从一个隐含假设，
变成了一条必须守住的契约** —— 因为现在生产镜像默认就不装它。
可是这条路径当时没有任何针对性测试。

补了 18 个（`backend/tests/test_tracing_degraded.py`），最关键的一条是：
运维在生产把 `OTEL_ENABLED` 打开了、但镜像没装 OTel 包 —— 此时必须记一条
warning 然后继续跑，绝不能让应用起不来。

顺带把两处注释改了：原文写 `# pragma: no cover - deps always installed`
（依赖总是安装），这话**是错的**，现在指向 `requirements-otel.txt`。

一个自我纠正：我一开始想说这模块"零测试覆盖"，实测发现不对 ——
靠其它测试导入 `app.main` 带来的间接覆盖已有 **44%**，加上新测试升到 **58%**。
准确的说法是"没有针对性测试、关键契约从未被断言"，不是零覆盖。

### 更正：我说规则"100% 准确"是小样本给的假象



我在上面写过"每条上下文规则都量过准确率、只留 100% 的"。那是在 `6823d18` 的
**5 个**文档上量的。后来扩到全仓 **140 个既有文档**复测，数字掉下来了：

| 规则 | 5 文档 | 140 文档 |
|---|---|---|
| 括号 + 后紧跟表格竖线 | — | **100%**（518/518） |
| 括号 + 之后无括号 | 100% | 99.57% |
| 句末句号 | 100% | 99.37% |
| 箭头全填 `→` | 100% | **92.21%** |

反例是真实存在的：括号内部本身含逗号、行尾恰好是右书名号 `」`、缩进架构图里
的 `↓`。**小样本给了虚假的安全感。**

本次的实际输出没受影响 —— 因为还有一道更硬的检验：规则推断与底本对齐**独立**
推断同一批位置，105 处可核对、冲突 0。但发现问题不等于解决问题，所以接着
真的把箭头那条改了（见下）。括号与句号两条留着但明确标注"不算已证明"，
记在 `docs/ENCODING_REPAIR.md` §6，没有假装它已解决。

### 收紧箭头规则：进度主动倒退 17 处

92.21% 意味着平均每 13 处写错 1 个字，这个错误率不能接受。我先试了两种
"限缩语法位置"的收紧，**都没用**：

| 判据 | 可填 | 正确率 | 弃权 |
|---|---:|---:|---:|
| 原规则（无条件填 `→`） | 992 | 92.34% | 0 |
| 只在非缩进行填 | 816 | 93.14% | 176 |
| 只在左右紧邻非空白时填 | 197 | 94.92% | 795 |
| **本文档证据（含 git 底本）全为 `→`** | 583 | 99.83% | 409 |
| **上一条 + 独行箭头弃权** | 582 | **100%** | 410 |

关键是**换判据的维度**：全仓分布不可靠（`→` 92%、`←`/`↓`/`↔`/`↑` 各几个百分点），
但**同一份文档内部用法高度一致** —— 62 个含箭头的文档里 56 个只用 `→`。
最后一处残余错误是 `DATA_QUALITY.md` 里整行只有一个 `↓` 的图示连接符，
补上"独行箭头弃权"后打满。

**代价是进度倒退**：`DATA_SOURCE_STRATEGY.md` 无底本、存活箭头 0 个，
17 处箭头全部退回人工，已定 646 → **629**、待定 470 → **487**、58% → **56.4%**。
这是有意的 —— 宁可多留 17 个显眼占位符，也不写 1 个看似通顺的错字。
新增 8 条测试锁住这个行为，其中一条断言"没给 `allow_arrow` 就绝不填"
（默认弃权，不是默认填）。

**自己写出又修掉的两个 bug**（都靠实测发现）：
- 按"第 n 个待定"索引损坏点，前面轮次填掉一些后序号漂移，后面轮次读到了别的
  位置的前缀。改为按字符下标索引。
- 半成品写盘用了 `Path.write_text()`，Windows 上把 `\n` 转成 `\r\n`，而原文
  本就是 CRLF → 变成 `\r\r\n`，521 处全中，校验器直接判"正文被改动"。
  改为按字节写。

### 本轮最终验证（全部实跑）

```
pytest -q（全量）        → 2524 passed, 4 skipped, 87.86% cov, 35m10s, exit 0
                           （本轮新增 101 个测试后的全量跑在后台进行中）
本轮新增测试             → 101 passed（编码 24 + 修复工具 32 + 术语 27 + OTel 18）
ruff check               → All checks passed!
ruff format --check      → 已格式化（须按目录跑，见下）
mypy app                 → no issues found in 115 source files
check_encoding.py        → 484 个文件正常，5 个已登记损坏文件按预期警告；
                           --strict 时 5 个全部判失败（退出 1）
check_terminology.py     → 退出码 0；构造违规文件实测被拦（退出 1）
verify_utf8_repair.py    → 3 个半成品全部 [OK]：未损坏正文逐字节一致
前端 tsc / eslint        → 全通过
干净 venv 装 requirements → 41 包装成，应用启动，/health 200，/metrics 200
```

**闸门的反向验证**（证明它们不是"一律放行"也不是"一律拒绝"）：
构造 3 个违规文件（术语回退 / 一型编码 / 三型编码）→ 全部退出 1；
构造 1 个干净文件 → 两个闸门都退出 0。

> `ruff format --check .`（带点跑全仓）在 ruff 0.16.1 上会 panic，
> 必须按目录跑 `app tests scripts alembic`。



## 遗留/风险（收工时的真实状态）

先把**当天早些时候记下、后来被实测推翻**的两条留在这里，免得后人以为还没处理：

- ~~**`action-queue` 无缓存**，项目数上万时需要加缓存~~ →
  **判断有误，已核实无需优化**（详见上文"更正我自己的一条判断"）。
- ~~**`user_id` 过滤不一致**，启用多用户前必须统一~~ →
  **已统一**（详见上文"统一 `user_id` 过滤口径"），根因是两张表写入约定不同。

仍然开着的：

- **30 个本地 commit 未推远程**（实测 `git rev-list --count origin/master..HEAD`）。
  推送方式（直推 master vs 开分支走 PR）**还没得到你的确认**，所以我一直没推。
- **文档编码损坏还剩三批**。这是本次唯一的"未完成品"：
  - 一型 487 处**故意留白**等语义判定（已定 629 处、可证明）。
    `DATA_SOURCE_STRATEGY.md` 占 367 处且无干净底本，最难。
  - 二型 70 处（`docs/API_SPEC.md`）只检测不修 —— 它连"1 字符换 1 字符"都不成立，
    自动修复无法证明没越界，只能人工对照底本重写。
  - 三型 2 处（`docs/SYSTEM_DIRECTION_CHANGE.md`）只登记不修 —— 丢的是两个
    小节标题的 emoji，**损失可忽略**，手工补个图标即可（属内容编辑）。
- **括号与句号两条推断规则仍不是 100%**（99.57% / 99.37%），
  留着是因为错误率在 0.5% 量级且被底本交叉验证兜住，但不算"已证明"。
  箭头那条本轮已换判据收紧到留一法 100%。
- **Python 版本口径不一致**（需要你决定，我没擅自改）：
  `docker/Dockerfile` 与 CI 用 **3.12**，mypy 配置也写 3.12，
  但本地 venv 是 **3.11.9**，`pyproject.toml` 只声明 `>=3.11`。
  也就是说**本地测过的解释器和镜像里跑的不是同一个**。
  两条路：统一到 3.12（要重建本地 venv 重跑全套）或把镜像降到 3.11。
- **`SEED_FALLBACK_ENABLED` 生产建议设 `false`**（默认 `true`，尚未改）。
- **数据已过期**：最新 `updated_at` 是 08-18，最后一次采集 08-15
  —— 调度器只在服务常驻时才跑。上线前要确认服务是长驻还是需要外部定时。
- **跨批次非原子**：勾选 > 50 条时分多批，批次内原子、跨批次不保证。界面已如实说明。
- `/archive` 与 `/ops` 部分区块仍无后端接口（当前为诚实占位，非假数据）。
- **独立评审派不出去**：四次尝试派 subagent 做全新上下文评审，**全部异常退出、
  没留下任何结果**（三次编码判定分片 + 一次代码评审）。改成自己用探针脚本
  对抗性打靶，结论写进了 `CODE_REVIEW_REPORT.md`，并在报告里注明了评审方式的
  这个局限 —— 它不是"全新上下文"的评审，这一点没有掩饰。

## 相关

- 变更记录：`CHANGELOG.md`（本日 9 节：Added / Security×2 / Fixed×4 / Changed / Performance）
- 编码损坏专题：`docs/ENCODING_REPAIR.md`
- 本轮评审：`CODE_REVIEW_REPORT.md`（已重写，覆盖 08-21 改动）
- 上线结论：`GO_LIVE_AUDIT_REPORT.md`（08-20 那轮，仍有效）
- 交接：`HANDOFF.md`
